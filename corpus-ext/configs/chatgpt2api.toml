# gauntlet round-3 corpus config: chatgpt2api
# python-env points the oracle at the clone's own venv so third-party imports resolve. No vendored dirs (manifest3).
[tool.sightline]
python-env = "<GAUNTLET_CORPUS_ROOT>/.venv-chatgpt2api"
