# gauntlet round-4 corpus config: ComfyUI-SeedVR2_VideoUpscaler
# python-env points the oracle at the clone's own venv so third-party imports resolve. No vendored dirs (manifest4).
[tool.sightline]
python-env = "<GAUNTLET_CORPUS_ROOT>/.venv-ComfyUI-SeedVR2_VideoUpscaler"
