# tower (rs2)

Repo: `../gauntlet-corpus/tower` (workspace: `tower/`, `tower-layer/`,
`tower-service/`, `tower-test/`). Read cold, no checker output consulted.

## Phase 1 - blind ideal sites

| id | site (path:line) | rule | claim | excerpt (<=3 lines) |
|----|------------------|------|-------|---------------------|
| P1-1 | tower/src/hedge/rotating_histogram.rs:31 | #6 | `read(&mut self)` is a getter that first calls `maybe_rotate()`, which can swap the read/write histograms or `clear()` both. A caller reading a percentile can silently destroy a period of samples. | `pub fn read(&mut self) -> &mut Histogram<u64> {`<br>`    self.maybe_rotate();` |
| P1-2 | tower/src/hedge/rotating_histogram.rs:36 | #6 | Same for `write()`: an accessor whose callee graph clears state and advances `last_rotation`. | `pub fn write(&mut self) -> &mut Histogram<u64> {`<br>`    self.maybe_rotate();` |
| P1-3 | tower/src/load/peak_ewma.rs:137 | #6 | `Load::load(&self)` reads as a pure metric getter but reaches `update_estimate` -> `Mutex::lock` -> `RttEstimate::decay`, which writes `rtt_ns` and `update_at`. Two `load()` calls on the same service return different numbers. | `fn load(&self) -> Self::Metric {`<br>`    let estimate = self.update_estimate();` |
| P1-4 | tower/src/hedge/latency.rs:14 | #37 | `pub trait Record` lives in the private `mod latency` and has exactly one impl in the workspace (`impl latency::Record for Histo`, hedge/mod.rs:213) on a repo-owned type. The indirection buys nothing. | `pub trait Record {`<br>`    fn record(&mut self, latency: Duration);` |
| P1-5 | tower/src/hedge/delay.rs:13 | #37 | `pub trait Policy<Request>` in the private `mod delay`, one impl only (`for DelayPolicy`, hedge/mod.rs:245). | `pub trait Policy<Request> {`<br>`    fn delay(&self, req: &Request) -> Duration;` |
| P1-6 | tower/src/hedge/select.rs:11 | #37 | `pub trait Policy<Request>` in the private `mod select`, one impl only (`for SelectPolicy<P>`, hedge/mod.rs:255). Three sibling single-impl traits in one middleware. | `pub trait Policy<Request> {` |
| P1-7 | tower/src/util/boxed_clone_sync.rs:71 | #11 | `trait CloneService<R>` and its blanket impl are a verbatim copy of boxed_clone.rs:111, differing only by `+ Sync`. The two whole modules (136 / 100 lines) are the same file twice. | `trait CloneService<R>: Service<R> {`<br>`    fn clone_box(` |
| P1-8 | tower/src/util/boxed/layer_clone_sync.rs:86 | #11 | 129-line clone of layer_clone.rs:85; the struct, `new`, `layer`, the private `CloneLayer` trait and the `Debug` impl differ only by name and `Sync` bounds. | `pub struct BoxCloneSyncServiceLayer<In, T, U, E> {` |
| P1-9 | tower/src/util/map_err.rs:16 | #11 | The 6-line `fmt::Debug` body (`debug_struct(..).field("inner"..).field("f", &format_args!("{}", type_name::<F>())).finish()`) is repeated verbatim in 9 places: and_then.rs:26, map_err.rs:23, map_future.rs:76 and :110, map_request.rs:22, map_response.rs:23, map_result.rs:23, service_fn.rs:61, then.rs:26. The crate already has `opaque_future!` (src/macros.rs) for exactly this. | `.field("f", &format_args!("{}", std::any::type_name::<F>()))` |
| P1-10 | tower/src/util/map_response.rs:43 | #11 | Whole-module clone family: map_err / map_response / map_result / map_request / then (90-103 lines each) repeat the same six blocks (struct, Debug, Layer struct, `opaque_future!`, `new`+`layer`, `Service` impl, `Layer` impl) with only the mapped position changing. | `pub const fn new(inner: S, f: F) -> Self {`<br>`    MapResponse { f, inner }` |
| P1-11 | tower-layer/src/tuple.rs:11 | #11 | 17 hand-written `impl Layer<S> for (L1, ..., Ln)` blocks, each a mechanical `let (l1, ..) = self; l1.layer((l2, ..).layer(service))`. One `macro_rules!` recursion replaces 330 lines; the workspace already uses macros elsewhere. | `fn layer(&self, service: S) -> Self::Service {`<br>`    let (l1, l2, l3) = self;`<br>`    l1.layer((l2, l3).layer(service))` |
| P1-12 | tower/src/util/call_all/unordered.rs:49 | #11 | `into_inner` (:56) and `take_service` (:69), with their doc comments, are verbatim copies of ordered.rs:115-137. Both are one-line delegations to the shared `common::CallAll`. | `pub fn into_inner(self) -> Svc {`<br>`    self.inner.into_inner()` |
| P1-13 | tower/src/util/call_all/unordered.rs:86 | #11 | The `impl common::Drive<F> for FuturesUnordered<F>` block is a token-for-token copy of ordered.rs:165 apart from `push` vs `push_back`. | `fn poll(&mut self, cx: &mut Context<'_>) -> Poll<Option<F::Output>> {`<br>`    Stream::poll_next(Pin::new(self), cx)` |
| P1-14 | tower/src/filter/mod.rs:124 | #11 | The inherent `impl<T, U> AsyncFilter<T, U>` repeats all six methods of `impl<T, U> Filter<T, U>` (:62) with identical bodies and identical doc comments: `new`, `layer`, `check`, `get_ref`, `get_mut`, `into_inner`. | `pub fn get_ref(&self) -> &T {`<br>`    &self.inner` |
| P1-15 | tower-test/src/mock/mod.rs:265 | #11 | The 11-line poisoned-lock guard in `Handle::drop` is a verbatim copy of `Mock::drop` (:212). One `fn lock_or_bail(&self)` would own it. | `let mut state = match self.state.lock() {`<br>`    Ok(v) => v,`<br>`    Err(e) => { if ::std::thread::panicking() { return; }` |
| P1-16 | tower/src/load/pending_requests.rs:86 | #11 | `poll_ready` and `call` of `Service for PendingRequests` are byte-identical to peak_ewma.rs:121-131, and `PendingRequestsDiscover::poll_next` (:126) is the same shape as `PeakEwmaDiscover::poll_next` (:196). The whole `Discover` wrapper is copied between the two load modules. | `TrackCompletionFuture::new(`<br>`    self.completion.clone(), self.handle(), self.service.call(req),` |
| P1-17 | tower-service/src/lib.rs:369 | #11 | `impl Service<Request> for &mut S` (:369) and `impl Service<Request> for Box<S>` (:386) are identical 16-line blocks; both just forward through `(**self)`. | `fn poll_ready(&mut self, cx: &mut Context<'_>) -> Poll<Result<(), S::Error>> {`<br>`    (**self).poll_ready(cx)` |
| P1-18 | tower/src/hedge/rotating_histogram.rs:68 | #11 | Two private `nanos(Duration)` helpers with the same `as_secs().saturating_mul(NANOS_PER_SEC).saturating_add(subsec_nanos())` body live in one crate (here and peak_ewma.rs:303), each with its own `NANOS_PER_SEC`. Both reimplement `Duration::as_nanos`, stable since 1.33. | `fn nanos(duration: Duration) -> u64 {`<br>`    duration.as_secs().saturating_mul(NANOS_PER_SEC)` |
| P1-19 | tower/src/ready_cache/cache.rs:401 | #11 | The "if a new version of this service has been added to the unready set, don't overwrite it" block (swap_remove_index + expect + pending_contains guard + push_pending) is duplicated verbatim from :350-360 inside `check_ready_index`. | `if !self.pending_contains(&key) {`<br>`    self.push_pending(key, svc, cancel);` |
| P1-20 | tower/examples/tower-balance.rs:200 | #11 | Five copies of `if self.latencies.len() < N { return; } println!("  pXX {:4}ms", value_at_quantile(q));` where one loop over a `[(usize, f64, &str); 5]` table would do. | `if self.latencies.len() < 10 { return; }`<br>`println!("  p90  {:4}ms", self.latencies.value_at_quantile(0.9));` |
| P1-21 | tower/src/util/call_all/common.rs:92 | #18 | `poll_next` narrates four phases in prose inside one loop body: "First, see if we have any responses to yield" (:96), "If there are no more requests coming" (:103), "If not done, and we don't have a stored request" (:112), "Then, see that the service is ready" (:125). Each phase is a named step waiting to be a function. | `// First, see if we have any responses to yield`<br>`// Then, see that the service is ready for another request` |
| P1-22 | tower/src/hedge/mod.rs:129 | #18 | `new_with_histo` is four labeled construction phases in sequence: "Clone the underlying service..." (:141), "Check policy to see if..." (:146), "Delay the second request..." (:149), "If the request is retryable..." (:157). | `// Delay the second request by a percentile of the recorded request latency`<br>`// histogram.` |
| P1-23 | tower/src/timeout/future.rs:38 | #18 | Two labeled phases, "First, try polling the future" (:41) and "Now check the sleep" (:47), and the first phase is a `match` whose only job is an early return. | `// First, try polling the future`<br>`// Now check the sleep` |
| P1-24 | tower/src/buffer/worker.rs:147 | #23 | `Future::poll` is 60 lines: an early return, a `loop`, a two-arm `match` on the message, and inside the `Some` arm a nested three-arm `match` on `poll_ready` with drops, sends and a re-read of `self.failed`. Cognitive complexity well past 15 in the hottest correctness path of `Buffer`. | `match ready!(self.poll_next_msg(cx)) {`<br>`    Some((msg, first)) => {`<br>`        match self.service.poll_ready(cx) {` |
| P1-25 | tower/src/reconnect/mod.rs:84 | #23 | `poll_ready` is a 57-line `loop` over a three-variant state machine, each arm holding its own three-arm `match` with `continue`, `break` and two flavours of early return. The `break` exits to a trailing `Poll::Ready(Ok(()))` that reads as unreachable success but is the error path. | `State::Connecting(ref mut f) => {`<br>`    match Pin::new(f).poll(cx) {` |
| P1-26 | tower/src/balance/p2c/service.rs:215 | #23 | `poll_ready` runs two side-effecting helpers, then a `loop` with a nested `if let` over a three-arm `Result` match and a second exit condition, all against `self.ready_index` taken and put back. | `if let Some(index) = self.ready_index.take() {`<br>`    match self.services.check_ready_index(cx, index) {` |
| P1-27 | tower/src/retry/future.rs:70 | #23 | `poll` nests a `loop` over a projected state enum with a `match` per state and further `match`es on the policy result; 49 lines with 5 branch heads in the retry hot path. | `fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output> {` |
| P1-28 | tower/src/util/mod.rs:77 | #27 | `ServiceExt` is the crate's hottest symbol (re-exported at lib.rs:205, referenced from 20 sites) and it sits in a 1104-line module of which 876 lines are doc comment. Any task touching one combinator ingests the whole file. | `pub trait ServiceExt<Request>: tower_service::Service<Request> {` |
| P1-29 | tower/src/builder/mod.rs:106 | #27 | `ServiceBuilder` is the crate's advertised entry point (39 references) inside an 871-line module, 587 of them doc comment, holding ~30 methods on one type. | `pub struct ServiceBuilder<L> {` |
| P1-30 | tower-layer/src/tuple.rs:1 | #29 | 330 lines and no `//!` header at all; a reader opening the file gets `use crate::Layer;` and then 17 impls with no statement of what the module is for or why it stops at 16. | `use crate::Layer;`<br>`impl<S> Layer<S> for () {` |
| P1-31 | tower/src/balance/p2c/service.rs:1 | #29 | 268 lines implementing the P2C balancer core with no `//!` header; the module doc lives one level up in p2c/mod.rs, so the file that holds the algorithm says nothing about itself. | `use super::super::error;` |
| P1-32 | tower/src/buffer/worker.rs:1 | #29 | 226 lines of the buffer's whole failure protocol with no `//!` header; the one explanation is buried in a comment inside `failed()` at :107. | `use super::{`<br>`    error::{Closed, ServiceError},` |
| P1-33 | tower/src/util/future_service.rs:1 | #29 | 217 lines, no `//!` header, and the module holds two public items (`future_service`, `FutureService`) plus a hidden `State` enum whose `Debug` output is pinned by a test. | `use std::{fmt, future::Future, ...};` |
| P1-34 | tower/src/balance/p2c/service.rs:40 | #32 | `_req: PhantomData<Req>` is dead: `Req` is already named by the `services: ReadyCache<D::Key, D::Service, Req>` field on the same struct, so the marker constrains nothing. It is carried through `from_rng` (:78) and the manual `Debug` (:49) skips it. | `_req: PhantomData<Req>,` |
| P1-35 | tower/src/util/rng.rs:82 | #32 | `HasherRng::new` has zero references anywhere in the workspace (prod, tests, doc examples); its body is `HasherRng::default()`, which every call site uses directly. | `pub fn new() -> Self {`<br>`    HasherRng::default()` |
| P1-36 | tower/src/util/optional/mod.rs:46 | #34 | The inner `match` rebuilds the `Poll` it just destructured: `Poll::Ready(r) => Poll::Ready(r.map_err(Into::into)), Poll::Pending => Poll::Pending` is `inner.poll_ready(cx).map_err(Into::into)`, the form every other middleware in the crate uses (filter/mod.rs:111, latency.rs:60). | `Some(ref mut inner) => match inner.poll_ready(cx) {`<br>`    Poll::Ready(r) => Poll::Ready(r.map_err(Into::into)),`<br>`    Poll::Pending => Poll::Pending,` |
| P1-37 | tower/src/ready_cache/cache.rs:294 | #34 | `assert!(cancel_tx.is_some(), ...)` sits in the `else` arm of `if let Some(cancel_tx) = cancel_tx`, so the condition is statically `false` there. It is a `panic!` wearing an assertion's clothes, and every reader has to re-derive that it always fires. | `} else {`<br>`    assert!(cancel_tx.is_some(),`<br>`        "services that become ready must have a pending cancelation");` |
| P1-38 | tower/src/load/pending_requests.rs:48 | #36 | `#[allow(dead_code)]` on the public `Handle(RefCount)` silences a real signal: the field is never read, only kept alive for `Arc::strong_count`. The suppression hides that the type is a guard, and it would hide a genuinely unused field added later. | `#[allow(dead_code)]`<br>`pub struct Handle(RefCount);` |
| P1-39 | tower/src/util/ready.rs:27 | #36 | `#[allow(missing_docs)]` on `pub const fn new` (:27) and on `Ready::new` (:78) opts two public constructors out of the crate's `#![warn(missing_docs)]`. The same escape appears at oneshot.rs:72, boxed/sync.rs, boxed/unsync.rs, discover/list.rs:30 and on both `Either` variants (either.rs:21,:23): seven public API items whose contract is missing by suppression rather than by oversight. | `#[allow(missing_docs)]`<br>`pub const fn new(service: T) -> Self {` |
| P1-40 | tower/src/discover/list.rs:56 | #36 | Two `#[cfg(test)] #[allow(dead_code)] type` aliases exist only to prove `ServiceList` compiles over `Vec` and `vec::IntoIter`; the `allow` is there because the aliases are unreferenced. A `const _: fn() = ...` or a real test asserting behaviour would carry the same proof without dead items. | `#[cfg(test)]`<br>`#[allow(dead_code)]`<br>`type ListVecTest<T> = ServiceList<Vec<T>>;` |
| P1-41 | tower/src/ready_cache/cache.rs:478 | #38 | `"polled after complete"` is written five times in one `poll` (:445, :447, :460, :473, :474) and the sixth copy at :478 has already drifted to `"polled after compete"`. Exactly the drift a single `const POLLED_AFTER_COMPLETE: &str` prevents. | `let key = this.key.take().expect("polled after compete");` |
| P1-42 | tower/tests/ready_cache/main.rs:198 | #42 | `cancelation_observed` has no assertion: after pushing 130 services it defines a `Ready` wrapper and ends on `Ready(cache).await.unwrap()`. Nothing checks that cancelation was in fact observed, so the test passes on any non-error outcome, including one where every push silently no-ops. | `Ready(cache).await.unwrap();`<br>`}` |
| P1-43 | tower/tests/filter/async_filter.rs:44 | #42 | `rejected_sync`'s whole verdict is `unwrap_err()`. It never checks that the error is the filter's rejection, so any error from any layer, including a panic-adjacent `Closed`, passes the test named "rejected". | `service.call("hello".into()).await.unwrap_err();` |
| P1-44 | tower/src/util/rng.rs:156 | #44 | The `|| range.start == range.end` disjunct can never be true: the test discards every case with `range.start >= range.end` at :145. The assertion is weaker than it reads and the dead clause hides that `n < range.end` is the whole contract. | `TestResult::from_bool(n >= range.start && (n < range.end \|\| range.start == range.end))` |
| P1-45 | tower/tests/buffer/main.rs:10 | #47 | `let_worker_work()` is a 100ms wall-clock `thread::sleep` called from 8 tests in this file; it adds ~0.8s of pure waiting and is flaky by construction on a loaded machine. The buffer worker's progress is observable through `handle`, so the tests can synchronize on the condition. | `fn let_worker_work() {`<br>`    thread::sleep(::std::time::Duration::from_millis(100));` |
| P1-46 | tower/tests/spawn_ready/main.rs:27 | #47 | `time::sleep(100ms)` is used as "let the spawned task run" between `handle.allow(1)` and the readiness assertion. Even under `time::pause()` this is a timer standing in for the condition the test means to wait on. | `handle.allow(1);`<br>`time::sleep(time::Duration::from_millis(100)).await;`<br>`assert_ready_ok!(service.poll_ready());` |
| P1-47 | tower/src/reconnect/future.rs:32 | #48 | Private `fn future(fut) -> Self { Self::Future { fut } }` has exactly one caller (:44) and no other reference; the hop and the name buy nothing over writing the variant inline. | `fn future(fut: F) -> Self {`<br>`    Self::Future { fut }` |
| P1-48 | tower/src/reconnect/future.rs:36 | #48 | Same for `fn error(error) -> Self { Self::Error { error } }`, one caller at :50. | `fn error(error: Option<E>) -> Self {`<br>`    Self::Error { error }` |
| P1-49 | tower/src/hedge/delay.rs:54 | #48 | `State::delaying` is a one-line variant constructor with one caller (:95); its sibling `called` (:58) at least has two. | `fn delaying(delay: tokio::time::Sleep, req: Option<Request>) -> Self {`<br>`    Self::Delaying { delay, req }` |
| P1-50 | tower/src/retry/backoff.rs:78 | #53 | The error contract is headed `# Error`, not `# Errors`, so the section is invisible to rustdoc convention and to clippy's `missing_errors_doc`: the one place a caller of `ExponentialBackoffMaker::new` learns what `InvalidBackoff` means is a heading no tool indexes. | `/// # Error`<br>`///`<br>`/// Returns a config validation error if:` |
| P1-51 | tower/src/hedge/mod.rs:106 | #56 | `new_with_mock_latencies` is public API whose own doc says it is "usedful for integration tests", and its only reference in the workspace is tower/tests/hedge/main.rs. A test fixture is shipped in the public surface of the crate. | `/// A hedge middleware with a prepopulated latency histogram.  This is usedful`<br>`/// for integration tests.`<br>`pub fn new_with_mock_latencies<Request>(` |
| P1-52 | tower/src/balance/p2c/service.rs:83 | #56 | `Balance::len` has no reference anywhere but the `#[cfg(test)] mod test` assertion at p2c/test.rs:29, and its sibling `is_empty` (:88) only p2c/test.rs:48. Both exist to let the balancer's own tests inspect it. | `pub fn len(&self) -> usize {`<br>`    self.services.len()` |
| P1-53 | tower/src/retry/backoff.rs:157 | none | `debug_assert!(jitter_factor > 0.0, "rng returns values between 0.0 and 1.0")` contradicts `Rng::next_f64`'s documented range `[0, 1)` (rng.rs:25): 0.0 is a legal draw, so a debug build can abort on correct RNG output. The message states the wrong contract too. | `debug_assert!(jitter_factor > 0.0,`<br>`    "rng returns values between 0.0 and 1.0");` |
| P1-54 | tower/src/retry/budget/tps_budget.rs:191 | none | `TpsBudget`'s `Debug` prints `debug_struct("Budget")`, a name no type in the crate has since the trait/impl split. Any log line or panic message naming this value points at the wrong type. | `f.debug_struct("Budget")`<br>`    .field("deposit", &self.deposit_amount)` |
| P1-55 | tower/src/retry/budget/tps_budget.rs:69 | none | `TpsBudget::new` has five `assert!`s that abort the process on bad config, and its doc comment has no `# Panics` section: the three range constraints are only discoverable by reading the body. Clippy's `missing_panics_doc` owns this half of the contract. | `assert!(ttl >= Duration::from_secs(1));`<br>`assert!(retry_percent <= 1000.0);` |
| P1-56 | tower/src/hedge/mod.rs:201 | none | `// TODO: Remove when Duration::as_millis() becomes stable.` guards a hand-rolled `millis()` plus two constants. `Duration::as_millis` has been stable since Rust 1.33; the note is stale and the 11 lines it protects are `duration.as_millis()` with an explicit round-up. | `// TODO: Remove when Duration::as_millis() becomes stable.`<br>`const NANOS_PER_MILLI: u32 = 1_000_000;` |
| P1-57 | tower/src/hedge/rotating_histogram.rs:43 | none | `// TODO: replace with delta.duration_div when it becomes stable.` is likewise stale: `Duration::div_duration_f64` stabilized in 1.80, and the surrounding `nanos(delta) / nanos(self.period)` is the reason P1-18's helper exists at all. | `// TODO: replace with delta.duration_div when it becomes stable.`<br>`let rotations = (nanos(delta) / nanos(self.period)) as u32;` |
| P1-58 | tower/src/retry/budget/tps_budget.rs:93 | none | `// AtomicIsize isn't clone, so the slots need to be built in a loop...` justifies a `Vec::with_capacity` plus push loop that `std::iter::repeat_with(\|\| AtomicIsize::new(0)).take(windows).collect()` writes in one line. The comment is longer than the code it defends and its premise no longer forces a loop. | `let mut slots = Vec::with_capacity(windows as usize);`<br>`for _ in 0..windows {`<br>`    slots.push(AtomicIsize::new(0));` |
| P1-59 | tower/src/load/peak_ewma.rs:155 | none | A second `impl<S, C> PeakEwma<S, C>` block exists solely to hold `update_estimate`, with the same generics and the same (empty) bounds as the block at :91. One concept, two homes, 45 lines apart. | `impl<S, C> PeakEwma<S, C> {`<br>`    fn update_estimate(&self) -> f64 {` |
| P1-60 | tower/tests/balance/main.rs:31 | none | The `Load` metric the `stress` test feeds the balancer is `rand::random_range(usize::MIN..=usize::MAX)`, so P2C is choosing between two uniform-random numbers. The 100_000-iteration test exercises the plumbing but can never observe that the balancer prefers a less-loaded endpoint. | `fn load(&self) -> Self::Metric {`<br>`    rand::random_range(usize::MIN..=usize::MAX)` |
| P1-61 | tower/src/buffer/worker.rs:200 | none | `// No more more requests _ever_.` duplicates "more"; the same comment is the only statement of the terminal condition of the whole buffer protocol. | `// No more more requests _ever_.`<br>`self.finish = true;` |
| P1-62 | tower/src/ready_cache/cache.rs:379 | none | The `# Panics` body on `call_ready` is a truncated sentence, "If the specified key does not exist in the ready", and the panic message it maps to says something else entirely ("check_ready was not called"). | `/// If the specified key does not exist in the ready`<br>`pub fn call_ready<Q: Hash + Equivalent<K>>(&mut self, key: &Q, req: Req) -> S::Future {` |

## Phase 2 - audit finding verdicts

Sheet: `corpus-ext/sheets/tower.rs2.wave1.tsv`, 1 row (rs:6 is the only judged
rule that fired). Filled by `sightline-rs2/judge-tmp/fill-tower.py`.

| finding (path:line) | rule | tier | verdict | why |
|---------------------|------|------|---------|-----|
| tower/src/buffer/worker.rs:210 | rs:6 | indexed | fp | The only effect on the callee graph is `Mutex::lock`: the fn writes no field, `ServiceError::clone` is an `Arc` bump and `Closed::new` builds a ZST, so it is idempotent and the accessor name is honest. `blocks` alone flags every `Arc<Mutex<_>>` read accessor in idiomatic Rust; rs:6 needs a mutation or IO effect behind the lock (contrast `RotatingHistogram::read`, which clears the histogram it hands back). |

Totals: 1 judged, 0 real, 1 fp.

### False-positive shapes

| rule | fp class | count | example key |
|------|----------|-------|-------------|
| rs:6 | blocking-read-only-accessor (`blocks` is the sole effect; no state changes behind the lock) | 1 | tower/src/buffer/worker.rs:210:6:dishonest-accessor:tower::buffer::worker::Handle::get_error_on_closed |

## Phase 3 - reconciliation

Audit: 41 findings over 4 rules (#11 x33, #29 x5, #23 x2, #6 x1); #9, #18, #20,
#27, #32, #34, #36, #37, #38, #42, #44, #47, #48, #53, #56, #59 fired nowhere.
The provenance explains much of that: 2292 of 2361 call sites are unresolved and
only 41 call edges landed, so every edge-dependent reading (#6, #32, #48, #56)
was working nearly blind.

| P1 id | rule | class (covered / detector-miss / threshold-miss / inventory-gap) | note |
|-------|------|------------------------------------------------------------------|------|
| P1-1 | #6 | detector-miss | `maybe_rotate` clears and swaps `&mut self` fields; the effect vocabulary that carried worker.rs:210 on `blocks` does not carry own-state mutation, so the one honest #6 site in the repo is silent while a pure read fired. |
| P1-2 | #6 | detector-miss | Same root cause as P1-1. |
| P1-3 | #6 | detector-miss | `Load::load` reaches the same `Mutex::lock` that fired at worker.rs:210 and additionally writes `rtt_ns`, yet it is not flagged: `load` is a noun getter absent from the accessor-name lexicon. |
| P1-4 | #37 | detector-miss | `latency::Record` is non-public with exactly one repo-owned impl, the rule's stated bar, and #37 fired nowhere in the repo. |
| P1-5 | #37 | detector-miss | Same for `delay::Policy`. |
| P1-6 | #37 | detector-miss | Same for `select::Policy`; three sibling instances missed together points at the single-impl arm, not at a threshold. |
| P1-7 | #11 | covered | `boxed_clone.rs:67` / `boxed_clone_sync.rs:27` (`::new` x2) names the same two-module duplication; the anchor is `new` rather than the `CloneService` trait I picked. |
| P1-8 | #11 | covered | `layer_clone_sync.rs:92` (x3 with `layer.rs:60`, `layer_clone.rs:91`). |
| P1-9 | #11 | covered | `map_err.rs:20` (`fmt` x7) plus the x3 group at `map_future.rs:108` / `service_fn.rs:59` / `layer_fn.rs:90` covers all ten copies, split across two groups. |
| P1-10 | #11 | covered | `map_err.rs:74` (`call` x4) and the `fmt` group report the map_* family; `new` / `layer` / the `Layer` impl in the same family stayed silent. |
| P1-11 | #11 | threshold-miss | The 17 tuple `layer` bodies differ by arity, so no two normalize to one digest, and each body is 2 statements. The repeated fact is the *shape*, which a T2 digest over whole bodies cannot see. |
| P1-12 | #11 | covered | `ordered.rs:109` / `unordered.rs:43` (`new` x2) names the module pair; my anchor was `into_inner` / `take_service`. |
| P1-13 | #11 | threshold-miss | The `Drive` impl methods are 1-2 line bodies, under the clone arm's floor, though the sibling pair in the same two files did fire. |
| P1-14 | #11 | threshold-miss | All six duplicated `Filter` / `AsyncFilter` methods have one-line bodies; the duplication that matters is the doc comments plus the whole impl block, not any single body. |
| P1-15 | #11 | threshold-miss | The 11-line poisoned-lock guard is one `let` with a `match` initializer, so it is ~2 statements against a >=5-statement block floor despite being 11 lines of real duplication. |
| P1-16 | #11 | covered | `pending_requests.rs:90` / `peak_ewma.rs:125` (`call` x2). |
| P1-17 | #11 | threshold-miss | Both forwarding impls have one-line `fn` bodies; the duplicated unit is the impl block, which neither arm reads. |
| P1-18 | #11 | threshold-miss | The two `nanos` bodies differ by return type and an `as f64` cast, so the digests part despite identical structure. |
| P1-19 | #11 | threshold-miss | The duplicated block is 4-5 statements, at or just under the block floor. |
| P1-20 | #11 | threshold-miss | `examples/` is test scope and #11 reads prod; the site is real but out of the rule's declared reach. |
| P1-21 | #23 | covered | #23 fired at exactly `call_all/common.rs:92` (complexity 19). I anchored the same function under #18 for its four phase comments; the site is reported, the reading differs. |
| P1-22 | #18 | threshold-miss | The four phases are declarative sentences, not ordinal labels, so they fall under whatever "labeled" means for the rule. |
| P1-23 | #18 | detector-miss | "First, try polling the future" and "Now check the sleep" are ordinal labels on two phases, #18's stated bar met exactly, and the rule fired nowhere in the repo. |
| P1-24 | #23 | threshold-miss | `Worker::poll` nests loop -> match -> match over 60 lines yet scores under 15, while the neighbouring `poll_next` scored 19. The arm-count weighting is what separates them. |
| P1-25 | #23 | threshold-miss | Same shape: a 57-line three-state loop with a nested three-arm match per state, under the cutoff. |
| P1-26 | #23 | threshold-miss | 46 lines, nested loop / if-let / three-arm match, under the cutoff. |
| P1-27 | #23 | threshold-miss | 49 lines, 5 branch heads, under the cutoff. |
| P1-28 | #27 | threshold-miss | 1104 lines holding the crate's hottest symbol and #27 fired nowhere. If the size input counts code lines only, `util/mod.rs` is 193 and duly passes, but the 876 doc-comment lines are exactly the reader tax the rule prices. |
| P1-29 | #27 | threshold-miss | Same shape at 871 lines / 247 code lines. |
| P1-30 | #29 | covered | `tower-layer/src/tuple.rs:1`, verbatim match. |
| P1-31 | #29 | covered | `tower/src/balance/p2c/service.rs:1`. |
| P1-32 | #29 | covered | `tower/src/buffer/worker.rs:1`. |
| P1-33 | #29 | covered | `tower/src/util/future_service.rs:1`. |
| P1-34 | #32 | detector-miss | A redundant `PhantomData` field is not a `pub` item, so it falls outside #32's arm, and rustc's `dead_code` (the declared complement) exempts `PhantomData` by design. Nothing in the pair covers a marker field the struct already constrains. |
| P1-35 | #32 | threshold-miss | `HasherRng::new` is `pub` in a `pub mod` of a library crate, so every entry-point rule must treat it as reachable. The rule is right to be conservative; the site needs a crate-external usage signal the closed world cannot supply. |
| P1-36 | #34 | threshold-miss | The `Poll::Pending => Poll::Pending` arm re-returns what it matched but the `Ready` arm transforms `r`, so the "every arm" bar is not met even though the whole match is a hand-rolled `Poll::map`. |
| P1-37 | #34 | detector-miss | A statically-false `assert!` in the `else` of an `if let Some(x) = x` is dead weight of the same family as commented-out code, and neither #34 arm reaches it. |
| P1-38 | #36 | threshold-miss | One `allow` in 219 lines is under any density bar; the signal here is *what* is silenced (a `pub` type's unread field), not how many. |
| P1-39 | #36 | threshold-miss | 2 allows in 102 lines. Same shape: seven `#[allow(missing_docs)]` scattered one per module never reach a per-module density, yet together they are the crate's whole undocumented public surface. |
| P1-40 | #36 | threshold-miss | 3 allows in 62 lines is the densest prod module in the repo and still silent, which brackets the cutoff usefully. |
| P1-41 | #38 | threshold-miss | Six copies of one panic string, five identical and one drifted, all in-function `expect` arguments inside one module; #38 reads module-level literals across >=3 modules. |
| P1-42 | #42 | threshold-miss | The trailing `.unwrap()` on the awaited future reads as a verdict, so the test clears the bar while asserting nothing about cancelation. |
| P1-43 | #42 | threshold-miss | `unwrap_err()` reads as a verdict; it discriminates Ok from Err but not which error, which is the whole point of a test named `rejected_sync`. |
| P1-44 | #44 | threshold-miss | The dead clause `range.start == range.end` is a sub-expression of a live `from_bool` argument, not a whole tautological assertion, and the verdict is a `TestResult` rather than an assert macro. |
| P1-45 | #47 | detector-miss | A literal `thread::sleep(Duration::from_millis(100))` in a test file, one call hop away inside the helper `let_worker_work()` that 8 tests call. #42 already follows a repo helper for its verdict; #47 stopping at the test-fn boundary is what hides the repo's worst wall-clock wait. |
| P1-46 | #47 | detector-miss | `time::sleep(time::Duration::from_millis(100)).await` sits directly in a `#[tokio::test]` body; an awaited `tokio::time::sleep` is not in the sleep lexicon. |
| P1-47 | #48 | detector-miss | Private one-line `fn future`, single call edge at `reconnect/future.rs:44`, the arm's exact shape. Likely collateral of the 97% unresolved call sites rather than of the arm itself. |
| P1-48 | #48 | detector-miss | Same for `fn error`, single call edge at :50. |
| P1-49 | #48 | detector-miss | Same for `State::delaying`, single call edge at `hedge/delay.rs:95`. |
| P1-50 | #53 | detector-miss | The heading reads `# Error`, so a detector keyed on `# Errors` sees no section and defers to clippy's `missing_errors_doc`, which sees a section and defers back. The near-miss heading falls between the two owners and is the case worth catching. |
| P1-51 | #56 | threshold-miss | `pub` in a library crate, so the closed world must treat it as reachable, even though its own doc says it is for integration tests and its only edge is `tests/hedge/main.rs`. A doc-declared test fixture is the signal the closed world cannot supply. |
| P1-52 | #56 | threshold-miss | Same: `Balance::len` / `is_empty` are `pub` API whose only edges are two assertions in the crate's own `#[cfg(test)] mod test`. |
| P1-53 | none | inventory-gap | `debug_assert!` whose predicate contradicts the documented range of the value it checks. No rule reads a contract stated in a callee's doc against an assertion at the call site. |
| P1-54 | none | inventory-gap | A `Debug` impl naming a type that no longer exists. No rule reads `debug_struct`/`debug_tuple` literals against the enclosing type. |
| P1-55 | none | inventory-gap | Missing `# Panics` for five `assert!`s; #53's rs reading owns `# Errors` only and cedes the panics half to clippy. |
| P1-56 | none | inventory-gap | Stale `TODO` guarding a hand-rolled reimplementation of a stdlib API stable for years. Nothing prices a comment whose premise expired. |
| P1-57 | none | inventory-gap | Same shape as P1-56. |
| P1-58 | none | inventory-gap | A comment longer than the code it defends, defending a premise that no longer forces the shape. |
| P1-59 | none | inventory-gap | Two `impl` blocks for one type with identical generics and bounds, 45 lines apart. One concept, two homes, and no rule reads impl-block fragmentation. |
| P1-60 | none | inventory-gap | A test whose oracle input is uniform noise, so the 100_000-iteration stress run can never observe the property it names. |
| P1-61 | none | inventory-gap | Duplicated word in the one comment that states the buffer's terminal condition. |
| P1-62 | none | inventory-gap | A truncated `# Panics` sentence whose text also disagrees with the panic message it documents. |

Totals: 62 phase-1 sites; 11 covered, 15 detector-miss, 26 threshold-miss,
10 inventory-gap.
