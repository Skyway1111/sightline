# tetro-tui (rs2)

Repo: `../gauntlet-corpus/tetro-tui`. Rust 2024 bin crate, `src/` only (no `tests/`,
`benches/`, `examples/`); 19,745 lines over 58 modules. Two `#[cfg(test)]` modules
(`fmt_helpers.rs:350`, `game_restoration.rs:536`). Judged blind, no audit seen.

## Phase 1 - blind ideal sites

| id | site (path:line) | rule | claim | excerpt (<=3 lines) |
|----|------------------|------|-------|---------------------|
| P1-1 | src/game_renderers/mod.rs:2 | #32 | `mod legacy_buffered` (1081 lines, `LegacyBufRenderer` + an 817-line `render`) is reached by no edge: the `pub use` (line 22), the `MiscGameRenderers::LegacyBuf` arm (line 72), the `from_num` arm (84) and all four dispatch arms are commented out. `#[allow(unused)]` is the only thing keeping rustc quiet. | `#[allow(unused)]` / `mod legacy_buffered;` |
| P1-2 | src/game_renderers/mod.rs:5 | #32 | `mod prototype` (160 lines, `PrototypeRenderer` + a 117-line `render`) is dead by the same mechanism: re-export at line 21 and enum arm at 71 commented out, nothing constructs it. | `#[allow(unused)]` / `mod prototype;` |
| P1-3 | src/terminal_buffers/mod.rs:65 | #32 | `MiscTermBufs` and its 76-line hand-written `TerminalBuffer` forwarding impl (lines 73-149) have zero references anywhere in the crate; `#[allow(unused)]` hides it. Half the module is dead. | `#[allow(unused)]` / `pub enum MiscTermBufs {` |
| P1-4 | src/terminal_buffers/mod.rs:13 | #32 | `SparseDoubleBuffer` (251 lines) is referenced only by its own file and this silenced re-export; only `DenseDoubleBuffer` is ever constructed (`main_buffered.rs:57`). | `#[allow(unused)]` / `pub use sparse_double_buffer::SparseDoubleBuffer;` |
| P1-5 | src/terminal_buffers/mod.rs:15 | #32 | `SparseSingleBuffer` (163 lines) likewise unreferenced outside its own file. | `#[allow(unused)]` / `pub use sparse_single_buffer::SparseSingleBuffer;` |
| P1-6 | src/terminal_buffers/mod.rs:11 | #32 | `DenseSingleBuffer` (165 lines) likewise unreferenced outside its own file. | `#[allow(unused)]` / `pub use dense_single_buffer::DenseSingleBuffer;` |
| P1-7 | src/game_modding/start_board.rs:50 | #32 | `pub fn encode_board` has no caller in the crate (the only occurrence of the name is this definition); `#[allow(dead_code)]` on the line above is an admission, not a use. | `#[allow(dead_code)]` / `pub fn encode_board(board: &Board) -> String {` |
| P1-8 | src/settings/graphics_settings/tile_coloring.rs:131 | #32 | `pub const GRAY` is never read anywhere (`ColorID::GRAY` has zero occurrences); its neighbours BLACK/WHITE are used. | `#[allow(unused)]` / `pub const GRAY: Self = ColorID::N(NamedColor::Gray);` |
| P1-9 | src/settings/graphics_settings/mod.rs:368 | #32 | `fn ordered_colormap` (19 lines) is dead: its single mention in the repo is the commented-out `#[serde(serialize_with = "ordered_colormap")]` at `tile_coloring.rs:15`. | `#[allow(unused)]` / `fn ordered_colormap<S: serde::Serializer>(` |
| P1-10 | src/main.rs:242 | #32 | `GameSaves::get` has no call site; only `get_mut` is called (`new_game.rs:793,899,926,951`). The `&`-returning half of the accessor pair was never needed. | `pub fn get(&self) -> Option<&GameSave<IH>> {` |
| P1-11 | src/game_restoration.rs:357 | #56 | `pub mod simple_bitencoded` (47 lines: struct + `InputHistoryEncoder` impl) is reached only from `run_compressor_test::<simple_bitencoded::SimpleBitencodedInputHistory>()` at line 606, inside `#[cfg(test)]`. The other three encoders are all wired into prod (`savefile_logic.rs:85`, `game_restoration.rs:10`); this one ships nothing. Delete it and its test. | `#[allow(unused)]` / `pub mod simple_bitencoded {` |
| P1-12 | src/game_modding/puzzle.rs:417-482 | #34 | 66 consecutive commented-out lines: six alternate puzzle-stage literals ("T-spin FINALE v1/v2/v2.1/v2.2/v2.3/v3") left inside the `stages` array of `get_stage_data`. Git remembers them. | `// ("T-spin FINALE v2.3", vec![` / `//     b"OOOO  OOOO",` |
| P1-13 | src/main.rs:138-157 | #34 | 20 commented-out lines inside `set_custom_panic_hook`: the whole `#[cfg(debug_assertions)]` crash-file writer, plus another 5-line commented `MoveToColumn` call at 171-174. This is the largest block in the file and the reader has to decide whether it is intent or debris. | `// #[cfg(debug_assertions)]` / `//     use crate::fmt_helpers::generate_timestamp;` |
| P1-14 | src/settings/graphics_settings/tile_coloring.rs:383-398 | #34 | A whole commented-out constructor `pub fn oklch0_palette() -> Self` including its 10-entry colour table, parked between two live constructors. | `// pub fn oklch0_palette() -> Self {` / `//     const COLORS_OKLCH: [(u8, Color); 7 + 3] = [` |
| P1-15 | src/settings/graphics_settings/tui_coloring.rs:399-412 | #34 | A whole commented-out constructor `pub fn debug() -> Self` at the tail of the impl block. | `// pub fn debug() -> Self {` / `//     TuiColoring {` |
| P1-16 | src/game_mode_blueprints.rs:122-137 | #34 | A commented-out `pub fn time_trial() -> GameModePreset` blueprint, referring to a `GameModePreset` type and a `game_mode_blueprints::time_trial()` path that no longer exist: it could not compile if uncommented. | `// pub fn time_trial() -> GameModePreset {// (` / `//     game_mode_blueprints::time_trial(),` |
| P1-17 | src/game_renderers/mod.rs:130-135 | #34 | Six commented-out match arms wedged onto the end of a live arm, so rustfmt has pushed them into a 90-column-indented ragged tail. Same pattern repeats at lines 71-72, 83-84, 94-95, 111-112, 121-122, 154-155. | `MiscGameRenderers::Braille(r) => r.reset_viewport_state(offset, area, ambience), // TetroTUIRenderer::Prototype(r) => {` |
| P1-18 | src/tui_menus/replay_game.rs:180-187 | #34 | An 8-line commented-out alternative event-poll loop, self-labelled "Unused code", inside the hot `'wait` loop. | `// FIXME: Unused code: for performance measurements, this busy-spin can be used.` / `// let event_available = event::poll(Duration::ZERO)?;` |
| P1-19 | src/tui_menus/new_game.rs:1198-1202 | #34 | Four commented-out `game.modifiers.push(...)` lines naming paths (`game_mode_blueprints::game_modifiers::...`, `crate::game_modding::Autorevive`, `crate::tetromino_engine::Modifier`) that do not exist in this crate. | `// FIXME: Unused code: modifier addition.` / `// game.modifiers.push(crate::game_modding::Autorevive::modifier());` |
| P1-20 | src/savefile_logic.rs:18-21 | #34 | Four commented-out serialization backends (serde_json / json5 / toml / serde_yaml) in `to_savefile_string`, mirrored again at lines 28-31 in `from_savefile_str`, and again as four commented-out deps in `Cargo.toml`. The choice is recorded three times as dead alternatives instead of once as a sentence. | `// let string = serde_json::to_string(value)?; // Cannot de-/serialize f64::inf. ...` |
| P1-21 | src/terminal_buffers/dense_double_buffer.rs:32-41 | #34 | A commented-out `fn with_offset_and_area` constructor, still naming the pre-rename type `DenseTerminalDoubleBuffer` and the pre-rename field `TermCell::EMPTY`. The identical dead constructor is copy-pasted into all four buffer modules (`dense_single_buffer.rs:29-37`, `sparse_double_buffer.rs:31-40`, `sparse_single_buffer.rs:30-38`) and into the trait itself (`terminal_buffers/mod.rs:46`). | `// fn with_offset_and_area((x, y): (u16, u16), (w, h): (u16, u16)) -> Self {` |
| P1-22 | src/tui_menus/scores_and_replays.rs:82-87 | #34 | A commented-out six-line `match show_stat` that duplicates, with different label text, the live match immediately above it (lines 76-81). | `// match show_stat {` / `//     Stat::TimeElapsed(_) => format!("time: {}", fmt_duration(p.time_elapsed)),` |
| P1-23 | src/settings/mod.rs:106-159 | #11 | Nine accessors (`tui_coloring`, `tile_coloring`, `tui_symbols`, `tile_symbols`, `hard_drop_effect`, `lock_effect`, `line_clear_effect`, `mini_tetromino_symbols`, `small_tetromino_symbols`) have byte-identical bodies up to two identifiers. Nine copies of one fact. | `&self` / `.tui_coloring_slotmachine` / `.grab(self.graphics().tui_coloring_selected)` |
| P1-24 | src/game_modding/ascent.rs:45 | #11 | `fn try_clone` is copied verbatim (signature and body) into all eleven modifier modules: ascent:45, cheese:86, combo:66, display_finesse:36, placement_practice:37, print_msgs:39, print_recency_stats:29, puzzle:47, revive_top_out:38, start_board:37, survival:80. A blanket impl or a `#[derive]`-style macro is the one home. | `) -> Result<Box<dyn GameModifier<MiscTetGens, MiscPceRots, TileType>>, String> {` / `Ok(Box::new(self.clone()))` |
| P1-25 | src/game_modding/survival.rs:68 | #11 | The same eleven modules each carry `fn id(&self) -> String { Self::MOD_ID.to_owned() }` verbatim (11 occurrences of `Self::MOD_ID.to_owned()` repo-wide), and two of them (`cheese.rs:79`, `survival.rs:73`) also share `fn cfg` verbatim. | `fn id(&self) -> String {` / `Self::MOD_ID.to_owned()` |
| P1-26 | src/tui_menus/mod.rs:132-140 | #11 | The nine-line "clear screen, or repaint the whole terminal in `bg_tui`" block is copy-pasted twelve times: mod.rs:132 and 323, adjust_gameplay:53, adjust_graphics:40, adjust_keybinds:40, advanced_settings:33, game_ended:76, new_game:61, scores_and_replays:39, settings:25, title:35 and 355. | `if self.settings.tui_coloring().bg_tui == Color::Reset {` / `self.term.queue(Clear(ClearType::All))?;` |
| P1-27 | src/tui_menus/mod.rs:285-303 | #11 | The Ctrl+Alt+L / Ctrl+Alt+S savefile handler pair (19 lines, two key-event patterns with the same guard) is copy-pasted eleven times, each opening with the same `// Reload from savefile.` comment: mod.rs:285 and 501, adjust_gameplay:360, adjust_graphics:492, adjust_keybinds:400, advanced_settings:429, game_ended:338, new_game:805, scores_and_replays:442, settings:243, title:325. A shared "handle global keys" helper is the missing seam. | `}) if { modifiers.contains(KeyModifiers::CONTROL.union(KeyModifiers::ALT)) } => {` / `self.temp_data.load_savefile_result = self.savefile_read();` |
| P1-28 | src/tui_menus/mod.rs:226-242 | #11 | The "Special keybinds" legend literal (Ctrl+C / Ctrl+Alt+S / Ctrl+Alt+L with their descriptions, ~17 lines) is copy-pasted thirteen times: mod.rs:227 and 443, adjust_gameplay:290, adjust_graphics:443, adjust_keybinds:254, advanced_settings:340, game_ended:279, new_game:348, play_game:407, replay_game:225, scores_and_replays:306, settings:177, title:253. Changing one keybind description means editing thirteen sites. | `("Ctrl+C", "Quit program (respects save preferences)"),` |
| P1-29 | src/tui_menus/play_game.rs:38-52 | #11 | The 15-line "Our game loop recipe looks like this" block comment is byte-identical to `replay_game.rs:45-59`, and the ~20 lines that follow it (kitty enhancement-flag push, FPS counter init, `reset_viewport_state` + initial `render`) are the same block with one argument changed (`play_game.rs:56-124` vs `replay_game.rs:63-131`). | `/* Our game loop recipe looks like this:` / `* Enter 'update_and_render loop:` |
| P1-30 | src/tui_menus/adjust_graphics.rs:69-115 | #11 | The 47-line "slot label" header block (`"Slot {}/{}: '{}'{}"` plus its two `queue(MoveTo(..)).queue(PrintStyledContent(..))` calls and the `heading_line` rule) is copy-pasted into `adjust_gameplay.rs:86-132` and `adjust_keybinds.rs:69-115`, differing only in which `*_selected` / `*_slotmachine` field it reads. | `"Slot {}/{}: '{}'{}",` / `self.settings.graphics_selected + 1,` |
| P1-31 | src/tui_menus/adjust_graphics.rs:27-34 | #11 | The `if_unmodifiable_clone_and_switch` closure is defined three times with identical bodies up to a field name: adjust_graphics:27, adjust_gameplay:32, adjust_keybinds:25. It belongs on `Settings` beside `clone_slot_if_unmodifiable`. | `let if_unmodifiable_clone_and_switch = \|s: &mut Settings\| {` |
| P1-32 | src/terminal_buffers/dense_single_buffer.rs:28 | #11 | The entire `impl TerminalBuffer for DenseSingleBuffer` is a copy of `dense_double_buffer.rs:31` with `curr_buf` renamed to `buf` and the `prev_buf` diffing removed: `reset_buffer`, `reset_with_ambience`, `offset_and_area`, `write_char`, `write_tile`, `write_str`, `write_str_wrapping` are all T2 clones. The same relation holds between `sparse_single_buffer.rs:29` and `sparse_double_buffer.rs:30`. | `impl TerminalBuffer for DenseSingleBuffer {` |
| P1-33 | src/main.rs:706-728 | #11 | The `Menu::GameOver` and `Menu::GameComplete` screen-wipe blocks are the same eleven statements twice, differing only in `(0..h).rev()` vs `0..h`. One loop with a reversed-or-not iterator is the fix. | `if matches!(menu, Menu::GameOver { .. }) {` / `for y in (0..h).rev() {` |
| P1-34 | src/game_renderers/main_buffered.rs:268 | #23 | `fn render` is 1047 lines in one body: 89 `if`, 41 `else`, 33 `for`, max nesting depth 10, 7 early `return`s. It renders board frame, hold, next, four HUDs, message feed, tiles, shadow piece and three visual effects, and it says so itself in the 20-line comment above it at 248-267 that lists the ten things it does. | `fn render<W: Write>(` |
| P1-35 | src/tui_menus/new_game.rs:43 | #23 | `fn run_menu_new_game` is 972 lines in one body: 81 `if`, 62 `else`, 12 `match`, 50 match arms, max nesting depth 15. | `pub fn run_menu_new_game(&mut self) -> io::Result<MenuUpdate> {` |
| P1-36 | src/tui_menus/replay_game.rs:37 | #23 | `fn run_menu_replay_game` is 902 lines: 61 `if`, 14 `match`, 53 arms, 23 `break`s across three labelled loops, max nesting depth 12. | `pub fn run_menu_replay_game(` |
| P1-37 | src/tui_menus/play_game.rs:30 | #23 | `fn run_menu_play_game` is 836 lines: 41 `if`, 14 `match`, 45 arms, 19 `break`s, 5 `continue`s, max nesting depth 14. | `pub fn run_menu_play_game(` |
| P1-38 | src/tui_menus/adjust_gameplay.rs:31 | #23 | `fn run_menu_adjust_gameplay` is 633 lines with 77 match arms and depth 11: one giant key-event match whose arms each mutate a different settings field. | `pub fn run_menu_adjust_gameplay(&mut self) -> io::Result<MenuUpdate> {` |
| P1-39 | src/tui_menus/adjust_graphics.rs:26 | #23 | `fn run_menu_adjust_graphics` is 785 lines with 56 match arms and depth 10; 42 of its statements are the same `if_unmodifiable_clone_and_switch(&mut self.settings);` line. | `pub fn run_menu_adjust_graphics(&mut self) -> io::Result<MenuUpdate> {` |
| P1-40 | src/game_renderers/legacy_buffered.rs:87 | #23 | `fn render` is 817 lines: 55 `if`, 34 `for`, 9 `match`, depth 10. (Dead per P1-1, so the cheapest fix is deleting the module.) | `fn render<W>(` |
| P1-41 | src/game_renderers/main_buffered.rs:1 | #29 | 1315 lines, the largest module in the crate, with no `//!` header: the first line is `use std::...`. A reader learns nothing about what the module is before line 25. No module in this crate has a `//!` header (0 of 58). | `use std::{` |
| P1-42 | src/tui_menus/new_game.rs:1 | #29 | 1225 lines, no `//!` header. | `use std::{` |
| P1-43 | src/tui_menus/replay_game.rs:1 | #29 | 1036 lines, no `//!` header; the only orientation is a block comment buried at line 45 inside the function. | `use std::{` |
| P1-44 | src/tui_menus/play_game.rs:1 | #29 | 907 lines, no `//!` header. | `use std::{` |
| P1-45 | src/main.rs:1 | #29 | 737 lines and the crate root, no `//!` header: no statement anywhere of what the crate is or how `Application`, `Menu` and the renderers relate. | `mod core_game_engine;` |
| P1-46 | src/settings/graphics_settings/tile_coloring.rs:1 | #29 | 652 lines, no `//!` header; the design discussion that would be the header is instead a 12-line FIXME comment stranded mid-file at lines 110-121. | `use crate::{fmt_helpers::lerp_col, settings::graphics_settings::ColorSerializationType};` |
| P1-47 | src/game_restoration.rs:1 | #29 | 625 lines defining five interchangeable input-history encodings, no `//!` header saying which one ships or why the others exist. | `use crate::core_game_engine::{Button, Game, GameBuilder, InGameTime, Input};` |
| P1-48 | src/game_renderers/main_buffered.rs:60 | #27 | The module is 1315 lines but exports exactly one hot symbol, `MainBufRenderer`, named in `game_renderers/mod.rs:19,68,80,85` and reachable from every menu that draws a game. Any task touching the default renderer ingests 1315 lines to reach a 10-line struct. | `pub struct MainBufRenderer {` |
| P1-49 | src/tui_menus/mod.rs:44 | #27 | `Menu` / `MenuUpdate` are imported by thirteen sibling modules plus `main.rs`, but they live in a 533-line file that also carries two 200-line menu-loop implementations and a 400-character easter-egg constant. Every menu task pays for all of it. | `pub enum Menu {` |
| P1-50 | src/tui_menus/new_game.rs:1055 | #27 | 1225 lines carrying two hot entry points (`run_menu_new_game`, `create_game_menu`); `create_game_menu` is the only constructor of a live `Game` and sits at the far end of the file. | `fn create_game_menu(` |
| P1-51 | src/terminal_buffers/mod.rs:11 | #36 | Four `#[allow(unused)]` in a 149-line module (lines 11, 13, 15, 65), the densest in the crate at 2.7%. Every one of them is hiding genuinely dead code (P1-3 through P1-6) rather than a false positive. | `#[allow(unused)]` |
| P1-52 | src/game_restoration.rs:163 | #36 | Four module-level `#[allow(unused)]` blankets (163, 254, 302, 356) plus one `non_camel_case_types` (12) in a 625-line module. Each blanket covers a whole `pub mod`, so any future dead item inside them is silent too; three of the four modules are in fact live, so the blanket is unearned. | `#[allow(unused)]` / `pub mod deltas_bitencoded_base64_prefixcode {` |
| P1-53 | src/game_modding/mod.rs:7 | #36 | `#[allow(unused)] mod print_recency_stats;` is stale: the module is used at line 71 of this same file (`PrintRecencyStats::MOD_ID`) and re-exported at line 26. The allow suppresses nothing and misleads the reader into thinking the mod is optional. | `#[allow(unused)]` / `mod print_recency_stats;` |
| P1-54 | src/terminal_buffers/sparse_double_buffer.rs:73 | #20 | The closure `\|\| self.curr_buf.get(&idx).unwrap_or(&self.curr_ambience).bg` appears five times in this module (lines 73, 91, 102, 119, 140) as the `unwrap_or_else` fallback of every write method. Name it once (a `fn bg_at(&self, idx) -> Color`). The identical pattern recurs four or five times in each of the other three buffer modules. | `bg: bg.unwrap_or_else(\|\| self.curr_buf.get(&idx).unwrap_or(&self.curr_ambience).bg),` |
| P1-55 | src/tui_menus/mod.rs:223 | #20 | `\|(lhs, rhs)\| (lhs.to_owned(), rhs.to_owned())` is written four times in this module (223, 240, 439, 456) as the tail of every keybinds-legend literal. | `.map(\|(lhs, rhs)\| (lhs.to_owned(), rhs.to_owned()))` |
| P1-56 | src/game_restoration.rs:45 | #18 | `fn restore` narrates three numbered phases in prose ("Step 1: Prepare builder.", "Step 2: Build actual game...", "Step 3: Reenact recorded game inputs." at 45, 47, 82). Step 2 alone is 33 lines with its own error branch; these are three functions spelled as comments. | `// Step 1: Prepare builder.` / `// Step 2: Build actual game by possibly reconstructing mods to finalize builder with.` |
| P1-57 | src/main.rs:522 | #18 | `initialize_terminal_state` narrates five numbered phases (lines 522, 525, 528, 531, 535: enter alternate screen / raw mode / hide cursor / set title / push enhancement flags). | `// 1. Enter alternate screen. This allows us not to trash the terminal's contents...` |
| P1-58 | src/main.rs:555 | #18 | `deinitialize_terminal_state` mirrors it with four bare numeric labels counting down (555, 561, 564, 567) plus a commented-out `// FIXME: 4. Unset title?` at 559. The bare numbers are only readable by cross-referencing the other function, which is the strongest argument that both should be a pair of named step functions. | `// 5.` / `// FIXME: 4. Unset title?` / `// 3.` |
| P1-59 | src/game_modding/cheese.rs:163 | #48 | `fn fmt_cheese_eaten` is private, has exactly one call site (line 152), and its body is one line, `format!("{}", cheese_eaten)`, which is itself `cheese_eaten.to_string()`. The name, signature and hop buy nothing. | `fn fmt_cheese_eaten(cheese_eaten: u32) -> String {` / `format!("{}", cheese_eaten)` |
| P1-60 | src/game_restoration.rs:407 | #37 | `trait QuantizeInGameTime` has exactly one method, exactly one impl (`for InGameTime`, line 411), and two call sites (`timed_input_to_uint` at 440, `play_game.rs:25`). An extension trait with one implementor and one method is a free function wearing an interface. | `pub trait QuantizeInGameTime {` / `fn quantize(&self) -> InGameTime;` |
| P1-61 | src/savefile_logic.rs:93 | #59 | `pub fn savefile_write` deletes the user's savefile from disk on its first branch (`std::fs::remove_file`, line 99) and clears the in-memory scoreboard on its second (line 106), and carries no doc comment at all. The only warning about this lives 700 lines away at `main.rs:126` in the caller. The first screen of the function should say it can delete data. | `pub fn savefile_write(&mut self) -> SavefileResult<()> {` / `std::fs::remove_file(self.temp_data.savefile_path.clone())?;` |
| P1-62 | src/savefile_logic.rs:151 | #59 | `pub fn savefile_read` overwrites `settings`, `scores_and_replays`, `statistics` and `game_saves` wholesale from disk, silently drops every game save that fails to decode (`.filter_map(\|save\| save.try_decode().ok())`, line 177), and has no doc. It is bound to Ctrl+Alt+L in eleven menus, so a mistyped chord discards unsaved state with no notice. | `pub fn savefile_read(&mut self) -> SavefileResult<()> {` |
| P1-63 | src/fmt_helpers.rs:321 | none | The doc says "Returns `None` if ... parameter is not `0 <= p <= 1`" but the guard is `if !(0.0..1.0).contains(&p) { return None; }`, a half-open range: `lerp_col(a, b, 1.0)` returns `None` instead of `b`. The doc and the code disagree at the endpoint. | `/// Returns `None` if one of the colors is not in RGB format or parameter is not `0 <= p <= 1`.` / `if !(0.0..1.0).contains(&p) {` |
| P1-64 | src/fmt_helpers.rs:354-471 | none | Seventeen test functions in `mod tests` are the same three statements with a different literal pair (`let s = ...; let r = incremented_game_mode_derivative(s); assert_eq!(r, ...)`). One table-driven test over seventeen `(input, expected)` rows says the same thing in a tenth of the lines. (Test code, so no prod rule reads it.) | `let s = "a [41]".to_owned();` / `let r = incremented_game_mode_derivative(s);` / `assert_eq!(r, "a [42]");` |
| P1-65 | src/game_restoration.rs:127 | none | The doc comment "Compressed verson of an input history. / Currently done using deltatime and assumption that inputs have millisecond precision at most." is copy-pasted onto five structs (127, 167, 258, 306, 360). On `RawInputHistory` it is simply false: line 5 of the same file calls it "Raw, uncompressed", and `simple_bitencoded` uses no deltatime. The typo "verson" is in all five copies. | `/// Compressed verson of an input history.` |
| P1-66 | src/game_renderers/mod.rs:76 | none | `pub const NUM_VARIANTS: usize = 3; //5;` is hand-synchronised with two `match n` ladders in the same impl (`from_num` at 78, `name_from_num` at 89) and with the enum at 67. Adding a renderer means editing four places and remembering the trailing `//5;`. A slice of `(name, constructor)` pairs would carry the count for free. | `pub const NUM_VARIANTS: usize = 3; //5;` |
| P1-67 | src/main.rs:108 | none | `let _e = app.initialize_terminal_state();` discards the `io::Result` of entering the alternate screen and raw mode, with the FIXME above it asking the question and not answering it. If it failed, the whole run then draws into the user's real scrollback. | `// FIXME: Handle io::Error? If not, why not?` / `let _e = app.initialize_terminal_state();` |
| P1-68 | src/game_modding/mod.rs:52-83 | none | `reconstruct_modded` dispatches on mod id with an eleven-branch `if / else if` chain of `mod_id == X::MOD_ID` comparisons. Every new modifier must be registered here as well as in its own module, and a typo in a `MOD_ID` silently degrades to `unrecognized_mod_ids` at runtime instead of failing to compile. | `if mod_id == Puzzle::MOD_ID {` / `} else if mod_id == Ascent::MOD_ID {` |
| P1-69 | src/game_modding/puzzle.rs:240 | none | `fn get_stage_data` is a 247-line function whose entire body is one array literal of hard-coded puzzle boards plus `stages[idx].clone()`. Data of this size belongs in a file the game reads or an `include_str!`, not in a function body carrying `#[allow(clippy::type_complexity)]` and `#[rustfmt::skip]` to survive. | `#[allow(clippy::type_complexity)]` / `fn get_stage_data(idx: usize) -> (&'static str, Vec<&'static [u8; 10]>, VecDeque<Tetromino>) {` |

## Phase 2 - audit finding verdicts

Sheet: `corpus-ext/sheets/tetro-tui.rs2.wave1.tsv`, 3 rows (rs:48 x2, rs:59 x1),
filled by `sightline-rs2/judge-tmp/fill-tetro-tui.py`. 2 `real`, 1 `fp`.

| finding (path:line) | rule | tier | verdict | why |
|---------------------|------|------|---------|-----|
| src/game_modding/cheese.rs:163 | rs:48 | indexed | real | Body is the single expression `format!("{}", cheese_eaten)`; only call is cheese.rs:152 in `on_lines_clear_post`. The folded form `self.cheese_eaten.to_string()` is shorter and clearer, so the hop and the name buy nothing. (My blind P1-59.) |
| src/game_renderers/legacy_buffered.rs:918 | rs:48 | indexed | real | Body is the single statement `self.prev.clear();`; only call is legacy_buffered.rs:84 in `reset_viewport_state`. Folding gives `self.screen.prev.clear()`. The enclosing module is unreachable dead code (P1-1), so deletion beats folding, but the claim is true and the change is an improvement either way. |
| src/main.rs:66 | rs:59 | indexed | fp | The evidence path is `set_custom_panic_hook -> std::panic::set_hook`, an in-process one-time callback registration: not another machine, another process, shared state, or deleted data, so it should not make an entry point "heavy". The one real cost `main` carries, `app.savefile_write()` deleting the savefile, is already declared in place at main.rs:125-126 one line above the call, so a doc block would be a second home for a stated fact; and `fn main` of a bin has no caller to tell. |

### False-positive shapes

| rule | fp class | count | example key |
|------|----------|-------|-------------|
| rs:59 | non-spend effect in the heavy catalog (`std::panic::set_hook` is in-process registration, not off-machine / other-process / data-deleted) | 1 | src/main.rs:66:59:cost-docstring:tetro_tui::main |

## Phase 3 - reconciliation

69 phase-1 sites against the 201-finding audit (fired: #11 x100, #29 x38,
#23 x30, #34 x18, #20 x6, #27 x4, #48 x2, #18 x2, #59 x1; silent: #6, #9,
#32, #36, #37, #38, #42, #44, #47, #53, #56).
27 covered, 21 detector-miss, 14 threshold-miss, 7 inventory-gap.

Visibility note for the #32 sites: `main.rs` declares `mod game_modding;`,
`mod game_restoration;`, `mod settings;`, `mod terminal_buffers;`,
`mod game_renderers;` - all private. So every dead item I found except P1-10
is `pub`-in-private-module, i.e. rustc's `dead_code` territory rather than the
rs reading's root-reachable bare `pub`; and every one of them carries the
`#[allow(unused)]` / `#[allow(dead_code)]` that disarms rustc. That is the gap:
the two owners split this repo's dead code between them and an `#[allow]`
hands rustc's half to nobody. P1-10 is the single genuinely bare-`pub`,
root-reachable site (`pub fn get` on `pub struct GameSaves`, crate root) and
the rule missed it too.

| P1 id | rule | class (covered / detector-miss / threshold-miss / inventory-gap) | note |
|-------|------|------------------------------------------------------------------|------|
| P1-1 | #32 | detector-miss | `mod legacy_buffered` is private (`game_renderers/mod.rs:3`), so `LegacyBufRenderer` is pub-in-private: rustc's half, disarmed by the `#[allow(unused)]` on line 2. 1081 unreachable lines that the audit only ever reports clones and complexity inside (#29 legacy_buffered.rs:1, #23 :87 cc 199, #48 :918, #11 :943/:952). |
| P1-2 | #32 | detector-miss | Same shape: private `mod prototype`, pub-in-private `PrototypeRenderer`, `#[allow(unused)]` at line 5. The audit instead reports its 160 dead lines as a #11 clone group with `fmt_helpers::fmt_lineclear_name` (prototype.rs:117) and a #23 cc-20. |
| P1-3 | #32 | detector-miss | `MiscTermBufs` is `pub enum` inside private `mod terminal_buffers`: pub-in-private, `#[allow(unused)]` at mod.rs:65. Eight #11 findings fire *inside* this dead enum's forwarding impl (mod.rs:74,82,102,110,118,126,134,142) - the audit prices its duplication and never says it is unreachable. |
| P1-4 | #32 | detector-miss | `SparseDoubleBuffer` pub-in-private, `#[allow(unused)]` at mod.rs:13. Audit reports #29, #23 (cc 32), #20 and nine #11 rows in the file. |
| P1-5 | #32 | detector-miss | `SparseSingleBuffer` pub-in-private, `#[allow(unused)]` at mod.rs:15. |
| P1-6 | #32 | detector-miss | `DenseSingleBuffer` pub-in-private, `#[allow(unused)]` at mod.rs:11. |
| P1-7 | #32 | detector-miss | `StartBoard::encode_board` is `pub fn` on a pub type inside private `mod start_board` (game_modding/mod.rs:11): pub-in-private, `#[allow(dead_code)]` at start_board.rs:49. |
| P1-8 | #32 | detector-miss | `ColorID::GRAY` is a `pub const` on a pub type; the chain is `pub mod tile_coloring` inside private `mod graphics_settings` inside private `mod settings`, so pub-in-private. `#[allow(unused)]` at tile_coloring.rs:130. |
| P1-9 | #32 | detector-miss | `ordered_colormap` is a plain private `fn`: squarely rustc's half, `#[allow(unused)]` at graphics_settings/mod.rs:367. |
| P1-10 | #32 | detector-miss | The one bare-`pub`, root-reachable site: `pub fn get` on `pub struct GameSaves`, both at crate root in main.rs, no `#[allow]` anywhere. This is exactly the rs reading's own territory and no #32 finding exists in the audit at all, so the arm did not run or found no candidate. |
| P1-11 | #56 | detector-miss | `pub mod simple_bitencoded` sits inside private `mod game_restoration`, so it is pub-in-private and the "root-reachable pub item" reading does not own it either, despite every edge to it (`game_restoration.rs:606`) being under `#[cfg(test)]`. Same visibility gap as the #32 rows. |
| P1-12 | #34 | threshold-miss | Rule fired 18x but not in puzzle.rs. The 66-line run strips to a comma-separated sequence of `("name", vec![...], VecDeque::from([...])),` tuple elements - array elements, not items or statements - so the "Rust parses it" filter rejects it. Largest single commented-out block in the repo. |
| P1-13 | #34 | threshold-miss | Rule fired nowhere in main.rs. The 20-line block is split by two genuinely blank lines (141, 148) into runs 138-140, 142-147 and 149-157, and each fragment is brace-unbalanced on its own (`#[cfg(..)] {` with no closer; three trailing `}`) so none parses. The block parses only as a whole. Same shape at 170-174, where the run is headed by the prose line `// Print the actual panic info.`. |
| P1-14 | #34 | covered | `34 src/settings/graphics_settings/tile_coloring.rs:383 (16 commented-out code lines)`. |
| P1-15 | #34 | covered | `34 src/settings/graphics_settings/tui_coloring.rs:399 (14 lines)`; the rule also found two blocks I did not list (tui_coloring.rs:141, :252). |
| P1-16 | #34 | threshold-miss | Not reported. The run's first line is `// pub fn time_trial() -> GameModePreset {// (`, a doubled comment marker that leaves `pub fn time_trial() -> GameModePreset {// (` after one strip; unbalanced and un-parseable. |
| P1-17 | #34 | threshold-miss | Not reported. These are trailing comments hanging off a live code line (`... => r.reset_viewport_state(offset, area, ambience), // TetroTUIRenderer::Prototype(r) => {`), so no run of whole comment lines starts at line 130, and the arms are unbalanced anyway. Six such tails in this one file. |
| P1-18 | #34 | threshold-miss | Not reported. The 8 code lines parse on their own, but the run they belong to is headed by the prose line 179 `// FIXME: Unused code: for performance measurements, this busy-spin can be used.`, which poisons the parse of the whole run. A leading-prose-line trim would recover this one. |
| P1-19 | #34 | threshold-miss | Same shape as P1-18: 4 parseable `game.modifiers.push(...);` lines preceded by the prose line `// FIXME: Unused code: modifier addition.`. |
| P1-20 | #34 | covered | Both halves found: `34 savefile_logic.rs:18` and `34 savefile_logic.rs:28`. |
| P1-21 | #34 | covered | All four copies found: dense_double:32 (10), dense_single:29 (9), sparse_double:31 (10), sparse_single:30 (9). |
| P1-22 | #34 | covered | `34 src/tui_menus/scores_and_replays.rs:82 (6 lines)`. |
| P1-23 | #11 | threshold-miss | The nine `Settings` accessors were not grouped. Each body is a single borrowed method-chain expression, so the whole-`fn` clone arm's minimum-body floor rejects it; nothing in settings/mod.rs is reported. The rule did group same-shaped constructor bodies of comparable size elsewhere (graphics_settings/mod.rs x5, tile_symbols.rs x5, tile_coloring.rs x7), so the cut is size, not shape. |
| P1-24 | #11 | threshold-miss | The eleven identical `try_clone` bodies are one statement each (`Ok(Box::new(self.clone()))`), below the floor; no game_modding finding except the `Ascent::new` / `DisplayFinesse::new` pair (ascent.rs:24, display_finesse.rs:19). |
| P1-25 | #11 | threshold-miss | Same: eleven `fn id` bodies of one statement each. |
| P1-26 | #11 | covered | Found as two groups: `clone-block x3 (7 stmts)` at adjust_gameplay:53 / adjust_graphics:40 / adjust_keybinds:40, and `clone-block x3 (5 stmts)` at advanced_settings:33 / scores_and_replays:39 / settings:25. Six of the twelve copies are still unreported (tui_menus/mod.rs:132 and :323, game_ended:76, new_game:61, title:35 and :355) because the block's tail differs there; the rule sees three fact-copies where a reviewer sees twelve. |
| P1-27 | #11 | detector-miss | The Ctrl+Alt+L / Ctrl+Alt+S handler pair repeats verbatim eleven times and is reported nowhere. Each copy is two `Event::Key(KeyEvent { .. }) if guard => { one statement }` match arms, so it is neither a `fn` body nor a >=5-statement block: the clone arms cannot see repeated *match-arm groups*. That shape is the single largest unpriced duplication in this repo. |
| P1-28 | #11 | detector-miss | The thirteen-way "Special keybinds" legend is a `vec![(String, [(&str,&str); N].into_iter()...collect())]` expression, not a statement block, so neither clone arm reaches it. Repeated large literal *expressions* are the missing arm. |
| P1-29 | #11 | threshold-miss | The rule did pair these two functions, but at a later 5-statement block (play_game:675 / replay_game:581), not at the ~20-line setup preamble (play_game:56-124 / replay_game:63-131) whose statements are few but enormous, nor at the byte-identical 15-line block comment above them. |
| P1-30 | #11 | covered | Collapses into P1-26's `clone-block x3 (7 stmts)` group: the anchor is the bg-clear at adjust_gameplay:53 / adjust_graphics:40 / adjust_keybinds:40 and the seven statements run through the `slot_label` build and its print. One finding for what I logged as two neighbouring sites. |
| P1-31 | #11 | detector-miss | The three `if_unmodifiable_clone_and_switch` closures are reported nowhere: a closure bound by `let` is not a `fn` body, and #20 is per-module so it cannot see the same closure in three modules. Cross-module closure clones fall between #11 and #20. |
| P1-32 | #11 | covered | Nine findings across the pair: whole-`fn` clones at dense_double:79/90/115/132 with dense_single:72/83/108/125, plus the 7-statement `reset_with_offset_and_area` block at :52/:48, and the same set again for the sparse pair. |
| P1-33 | #11 | threshold-miss | The GameOver / GameComplete wipe blocks are two statements each (`let (w, h) = ...;` and a `for`), below the >=5-statement floor; no #11 finding exists in main.rs. |
| P1-34 | #23 | covered | `23 main_buffered.rs:268 cc 338` - the highest score in the audit. |
| P1-35 | #23 | covered | `23 new_game.rs:43 cc 335`. |
| P1-36 | #23 | covered | `23 replay_game.rs:37 cc 241`. |
| P1-37 | #23 | covered | `23 play_game.rs:30 cc 210`. |
| P1-38 | #23 | covered | `23 adjust_gameplay.rs:31 cc 117`. |
| P1-39 | #23 | covered | `23 adjust_graphics.rs:26 cc 57`. |
| P1-40 | #23 | covered | `23 legacy_buffered.rs:87 cc 199`. The rule found 23 more #23 sites I did not list, including three whose enclosing module is dead (legacy_buffered:967, prototype:43). |
| P1-41 | #29 | covered | `29 main_buffered.rs:1 (1315 lines, 6 top-level items)`. |
| P1-42 | #29 | covered | `29 new_game.rs:1 (1225 lines, 0 top-level items)`. |
| P1-43 | #29 | covered | `29 replay_game.rs:1 (1036 lines)`. |
| P1-44 | #29 | covered | `29 play_game.rs:1 (907 lines)`. |
| P1-45 | #29 | covered | `29 main.rs:1 (737 lines, 13 top-level items)`. |
| P1-46 | #29 | covered | `29 tile_coloring.rs:1 (652 lines)`. |
| P1-47 | #29 | covered | `29 game_restoration.rs:1 (625 lines)`. 38 of the crate's 58 modules fired; the 20 that did not are all under the size cutoff, so the rule and I agree on the shape and it simply reports more of it than I listed. |
| P1-48 | #27 | threshold-miss | The largest module in the crate (1315 lines) is not priced. #27 fired only where a symbol clears the hot-reference bar (`Application` 18, `Menu` 26, `GameRestorationData` 10, `ColorID` 9); `MainBufRenderer` has ~5 references, so the biggest file in the repo is invisible to the arm that exists to price big files. |
| P1-49 | #27 | covered | `27 tui_menus/mod.rs:1 - 533 lines holding 3 hot symbols (Menu (26), MenuUpdate (15), heading_line (12))`, the exact reading I gave. |
| P1-50 | #27 | threshold-miss | new_game.rs (1225 lines) is not priced: #29's own message says it has 0 top-level items, because everything in it is an inherent method on `Application` declared in another module. A module can be the second-biggest in a crate and hold no symbol the arm can count. |
| P1-51 | #36 | detector-miss | No #36 finding anywhere. terminal_buffers/mod.rs carries four `#[allow(unused)]` in 149 lines (2.7%), the densest in the crate, and each hides live dead code (P1-3..P1-6). |
| P1-52 | #36 | detector-miss | game_restoration.rs carries five allows, four of them module-level `#[allow(unused)]` blankets over whole `pub mod`s. |
| P1-53 | #36 | detector-miss | The stale `#[allow(unused)]` at game_modding/mod.rs:7 on a module used at line 71 of the same file. A density rule would not catch this one (1 allow in 88 lines); an "allow that suppresses nothing" arm would. |
| P1-54 | #20 | covered | `20 sparse_double_buffer.rs:73 - closure appears 5x`, exact line and count. The rule also found the same shape in the three sibling buffers and two closures I did not list (game_keybinds.rs:93 x5, hard_drop_effect.rs:78 x4). |
| P1-55 | #20 | threshold-miss | `\|(lhs, rhs)\| (lhs.to_owned(), rhs.to_owned())` x4 in tui_menus/mod.rs is not reported although the rule's count bar is 3 and it accepted `\|(k, b)\| ((k, KeyModifiers::NONE), b)` elsewhere; the body must be falling under the nontriviality floor. |
| P1-56 | #18 | covered | `18 game_restoration.rs:45 - narrates 3 labeled phases`. |
| P1-57 | #18 | covered | `18 main.rs:522 - narrates 5 labeled phases`. |
| P1-58 | #18 | detector-miss | `deinitialize_terminal_state` (main.rs:546) is silent although it carries the same five phases as its covered twin, because the labels there are bare digits with no text (`// 5.`, `// 3.`, `// 2.`, `// 1.`) counting *down*. The bare-numeral form is the harder-to-read half of the pair and the one the matcher drops. |
| P1-59 | #48 | covered | `48 cheese.rs:163`, judged `real` in phase 2. |
| P1-60 | #37 | detector-miss | No #37 finding. `QuantizeInGameTime` has one method, one impl and two call sites, but the impl is `for InGameTime`, a type from the `falling-tetromino-engine` dependency, so the "type the repo owns" clause excludes it; and in a bin crate the "non-public trait" clause never bites, since every `pub` is already crate-private. Both clauses misfire for bin crates over foreign types. |
| P1-61 | #59 | detector-miss | `savefile_write` deletes the user's savefile (`std::fs::remove_file`, line 99) and clears the in-memory scoreboard, with no doc; it is not reported, while `main` is reported on a `std::panic::set_hook` edge I judged `fp`. The catalog appears to hold hook registration and not `std::fs::remove_file` / `File::create`: the exact inversion of the rule's own goal wording ("data deleted"). |
| P1-62 | #59 | detector-miss | `savefile_read` overwrites four app-state fields from disk and silently drops undecodable saves, with no doc, and is bound to Ctrl+Alt+L in eleven menus. Same missing catalog entries (`File::open`, `read_to_string`). |
| P1-63 | none | inventory-gap | `lerp_col`'s doc promises `0 <= p <= 1` while the guard is the half-open `(0.0..1.0)`. A doc-vs-code contradiction on a non-`Result` function has no owner: #53 covers only a missing `# Errors` entry. |
| P1-64 | none | inventory-gap | Seventeen copy-paste tests in `fmt_helpers::tests`. #11 does not read `#[cfg(test)]` code by this round's division, and no family-T rule prices test duplication. |
| P1-65 | none | inventory-gap | The same (partly false, typo'd) doc comment on five structs. Duplicated *doc text* has no rule; #38 covers only module-level string literals, and #11 only code. |
| P1-66 | none | inventory-gap | `NUM_VARIANTS = 3` hand-synced with two `match` ladders and an enum. The audit does report the ladders as `11 game_renderers/mod.rs:116 (clone x3)`, but as duplication among the dispatch bodies, not as the count-drift risk. |
| P1-67 | none | inventory-gap | `let _e = app.initialize_terminal_state();` discards an `io::Result` under a FIXME asking whether it should. No rule owns a discarded fallible result (clippy's `let_underscore_must_use` territory). |
| P1-68 | none | inventory-gap | An eleven-branch `if / else if` chain on `mod_id == X::MOD_ID` that must be edited alongside every modifier module. Registration split across two homes has no rule; #23 scores it at 12, under the bar. |
| P1-69 | none | inventory-gap | A 247-line function whose whole body is one data literal, kept compilable by `#[allow(clippy::type_complexity)]` + `#[rustfmt::skip]`. #23 scores it 0 (no branches) and #27 prices modules, not functions; nothing prices data-as-code. |
