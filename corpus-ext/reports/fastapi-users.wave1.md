# fastapi-users — wave 1

Repo: `<GAUNTLET_CORPUS_ROOT>\fastapi-users`, prod tree
`fastapi_users/` (35 modules, 3072 lines) plus root `test_build.py`. Judged cold
against research/SUMMARY.md §3. Line numbers are from the working tree as read.

## Phase 1 — blind ideal sites

| id | site (path:line) | rule | claim | excerpt (<=3 lines) |
|----|------------------|------|-------|---------------------|
| P1-1 | fastapi_users/db/base.py:22 | #1 | The whole user-persistence write boundary is an untyped dict: `create` (:22), `update` (:26), `add_oauth_account` (:35), `update_oauth_account` (:44) all take `dict[str, Any]`, so no caller or adapter implementor can see which keys are legal. `manager._update` (manager.py:684) feeds it a dict it assembles key-by-key from strings. | `async def create(self, create_dict: dict[str, Any]) -> UP:` |
| P1-2 | fastapi_users/authentication/strategy/db/adapter.py:16 | #1 | Same weak boundary repeated on the second adapter Protocol: `create(create_dict: dict[str, Any])` (:16) and `update(access_token: AP, update_dict: dict[str, Any])` (:20). The only producer, `DatabaseStrategy._create_access_token_dict` (db/strategy.py:53), returns exactly `{"token": ..., "user_id": ...}` — a two-field record with no type. | `async def create(self, create_dict: dict[str, Any]) -> AP:` |
| P1-3 | fastapi_users/manager.py:54 | #1 | `parse_id(self, value: Any) -> models.ID` is the documented extension point (both shipped mixins override it at :692 and :706) and its input is `Any`. Callers pass a JWT `sub` claim (manager.py:342), a path string (router/users.py:31) and a redis value (strategy/redis.py:34) — three different real types, none named. | `def parse_id(self, value: Any) -> models.ID:` |
| P1-4 | fastapi_users/manager.py:517 | #1 | `on_after_update` is a documented user-override hook whose `update_dict: dict[str, Any]` is the only description of what changed; the keys it can contain are decided privately in `_update` (:668-683, e.g. `hashed_password` substituted for `password`). | `update_dict: dict[str, Any],` |
| P1-5 | fastapi_users/jwt.py:18 | #1 | `generate_jwt(data: dict, ...)` uses a bare `dict` — no key or value type — on a public helper re-exported through the token flows (manager.py:299, manager.py:379, strategy/jwt.py:67, router/oauth.py:30). | `def generate_jwt(` / `    data: dict,` |
| P1-6 | fastapi_users/openapi.py:3 | #1 | The package's one shared response type is `dict[int \| str, dict[str, Any]]`; every `responses=` literal in the routers (auth.py:23, reset.py:9, transport/bearer.py:33) is checked against nothing. A three-field structure (model/content/description) is being carried as `Any`. | `OpenAPIResponseType = dict[int \| str, dict[str, Any]]` |
| P1-7 | fastapi_users/authentication/authenticator.py:95 | #1 | The generated dependency callables are `(*args: Any, **kwargs: Any)` (:95, :140) and `_authenticate` takes bare `*args`/`**kwargs` (:155, :161) — every authentication input (token, strategy, user_manager, enabled_backends) reaches the auth check through an opaque kwargs bag. | `async def current_user_token_dependency(*args: Any, **kwargs: Any):` |
| P1-8 | fastapi_users/router/users.py:129 | #1 | Three route handlers declare `user=Depends(get_user_or_404)` with no annotation (:129, :177, :219), so `user` is implicitly `Any` even though the dependency is annotated `-> models.UP` at :29. `me` (:46) annotates the same value properly — the omission is inconsistent, not deliberate. | `async def get_user(user=Depends(get_user_or_404)):` |
| P1-9 | fastapi_users/exceptions.py:37 | #1 | `InvalidPasswordException(reason: Any)` — `reason` is rendered straight into HTTP response bodies (router/register.py:68, users.py:103, reset.py:90), so the public error contract is `Any`. | `def __init__(self, reason: Any) -> None:` |
| P1-10 | fastapi_users/schemas.py:11 | #1 | `create_update_dict` (:11) and `create_update_dict_superuser` (:23) have no return annotation and hand an inferred `dict[str, Any]` straight to `user_db.create` — the untyped-dict boundary of P1-1 starts here. | `def create_update_dict(self):` |
| P1-11 | fastapi_users/authentication/strategy/jwt.py:25 | #9 | Mutable default argument: `token_audience: list[str] = ["fastapi-users:auth"]` is stored on the instance (:31) and later placed in the JWT `aud` claim (:66); any caller that mutates `strategy.token_audience` mutates the default shared by every future `JWTStrategy`. | `token_audience: list[str] = ["fastapi-users:auth"],` |
| P1-12 | fastapi_users/jwt.py:34 | #9 | Second mutable default: `algorithms: list[str] = [JWT_ALGORITHM]` on the public `decode_jwt`, passed by reference into `jwt.decode`. | `algorithms: list[str] = [JWT_ALGORITHM],` |
| P1-13 | fastapi_users/router/users.py:56 | #11 | The ~30-line `responses={400: {"model": ErrorModel, "content": ...}}` block for `update_me` (:56-85) is byte-identical to the one for `update_user` (:147-172). The repo already knows the fix — reset.py:9 hoists exactly this shape to a module constant `RESET_PASSWORD_RESPONSES`. | `ErrorCode.UPDATE_USER_EMAIL_ALREADY_EXISTS: {` / `    "summary": "A user with this email already exists.",` |
| P1-14 | fastapi_users/router/users.py:93 | #11 | `update_me` (:93-110) and `update_user` (:180-197) have identical bodies — same try, same two excepts, same two `HTTPException` payloads — differing only in `safe=True` vs `safe=False`. | `user = await user_manager.update(` / `    user_update, user, safe=True, request=request` |
| P1-15 | fastapi_users/router/oauth.py:73 | #11 | `authorize` in `get_oauth_router` (:73-101) and in `get_oauth_associate_router` (:257-288) are the same 25-line function; the only difference is one extra `"sub"` key in `state_data` (:82 vs :269) and the added user dependency. | `csrf_token = generate_csrf_token()` / `state_data: dict[str, str] = {CSRF_TOKEN_KEY: csrf_token}` |
| P1-16 | fastapi_users/router/oauth.py:149 | #11 | The 24-line state-decode + CSRF-compare block (:149-172) is duplicated verbatim at :333-356 — including both `except jwt.*` arms and the three-clause `if not cookie_csrf_token or not state_csrf_token or not compare_digest(...)`. This is the repo's security check, living in two places. | `state_data = decode_jwt(state, state_secret, [STATE_TOKEN_AUDIENCE])` / `except jwt.DecodeError:` |
| P1-17 | fastapi_users/router/oauth.py:57 | #11 | The `redirect_url is not None` / `OAuth2AuthorizeCallback` branch (:57-66) is duplicated at :241-250, and the `responses=` OpenAPI dict at :107-137 is duplicated (minus one example) at :295-321. | `oauth2_authorize_callback = OAuth2AuthorizeCallback(` / `    oauth_client, redirect_url=redirect_url,` |
| P1-18 | fastapi_users/manager.py:64 | #11 | `get` (:64-77), `get_by_email` (:79-92) and `get_by_oauth_account` (:94-108) are the same three-statement body — delegate, `if user is None: raise UserNotExists()`, return — written three times with three near-identical docstrings. | `user = await self.user_db.get(id)` / `if user is None:` / `    raise exceptions.UserNotExists()` |
| P1-19 | fastapi_users/manager.py:321 | #11 | `verify` (:321-347) and `reset_password` (:403-429) share the same four-step token ladder — decode/`except PyJWTError`, claim unpack/`except KeyError`, `parse_id`/`except InvalidID` — with only the exception class and secret swapped. | `except jwt.PyJWTError:` / `    raise exceptions.InvalidVerifyToken()` |
| P1-20 | fastapi_users/authentication/strategy/jwt.py:59 | #11 | The id-parse-and-load tail is copied across all three strategies: jwt.py:59-63, redis.py:33-37, db/strategy.py:37-41 — identical `try: parse_id / get` with identical `except (UserNotExists, InvalidID): return None`. Three copies of the rule that decides whether a token authenticates. | `parsed_id = user_manager.parse_id(user_id)` / `return await user_manager.get(parsed_id)` |
| P1-21 | fastapi_users/authentication/transport/cookie.py:40 | #11 | `_set_login_cookie` (:40-51) and `_set_logout_cookie` (:53-64) are the same eight-argument `response.set_cookie` call, differing only in value (`token` vs `""`) and `max_age`. | `response.set_cookie(` / `    self.cookie_name, token, max_age=self.cookie_max_age,` |
| P1-22 | fastapi_users/authentication/authenticator.py:71 | #11 | `current_user_token` (:62-105) and `current_user` (:107-151) duplicate a 20-line docstring verbatim and a 10-line body; `current_user` differs only by unpacking the tuple and dropping the token, so it could be written over the other. | `user, _ = await self._authenticate(` |
| P1-23 | fastapi_users/models.py:36 | #11 | `UserOAuthProtocol` inherits `UserProtocol[ID]` and then re-declares all six inherited fields (`id`, `email`, `hashed_password`, `is_active`, `is_superuser`, `is_verified`, :36-41) so it can add one new one at :42. Adding a field to `UserProtocol` silently leaves this copy stale. | `class UserOAuthProtocol(UserProtocol[ID], Generic[ID, OAP]):` / `    id: ID` / `    email: str` |
| P1-24 | fastapi_users/manager.py:49 | #11 | The `if x is None: self.y = Default() else: self.y = x  # pragma: no cover` construction is written twice (manager.py:49-52, password.py:20-29). Both spell as an if/else what `x or Default()` says, and both mark the injection branch untested. | `if password_helper is None:` / `    self.password_helper = PasswordHelper()` |
| P1-25 | fastapi_users/fastapi_users.py:54 | #13 | Every one of the seven `FastAPIUsers.get_*_router` methods (:54, :67, :75, :79, :98, :153, :203) is a pure forwarding call to the identically-named module function, adding only `self.get_user_manager`/`self.authenticator`; `get_oauth_router` (:137-151) forwards 13 arguments unchanged and re-documents all of them. The class is a hop, not a layer. | `return get_register_router(` / `    self.get_user_manager, user_schema, user_create_schema` |
| P1-26 | fastapi_users/password.py:31 | #13 | `PasswordHelper.verify_and_update` (:31) and `hash` (:36) are one-line forwards to `self.password_hash`; the class's only content is the default-hasher tuple in `__init__`. | `def hash(self, password: str) -> str:` / `    return self.password_hash.hash(password)` |
| P1-27 | fastapi_users/router/oauth.py:46 | #14 | The six-parameter cookie clump `csrf_token_cookie_{name,path,domain,secure,httponly,samesite}` is spelled out in four signatures (oauth.py:46-51, oauth.py:225-230, fastapi_users.py:107-112, fastapi_users.py:160-165), forwarded verbatim twice, and re-documented twice — a `CookieSettings` value with no name. `CookieTransport.__init__` (transport/cookie.py:13-21) is a fifth spelling of the same concept under the `cookie_*` prefix. | `csrf_token_cookie_name: str = CSRF_TOKEN_COOKIE_NAME,` / `csrf_token_cookie_path: str = "/",` |
| P1-28 | fastapi_users/manager.py:151 | #14 | The OAuth-account clump `oauth_name, access_token, account_id, account_email, expires_at, refresh_token` travels unpacked through `oauth_callback` (:151-156), `oauth_associate_callback` (:235-241) and both call sites (router/oauth.py:186-191, :373-378), then is re-packed into an identical six-key dict twice (:191-198, :260-267). The concept is already named twice over — `models.OAuthAccountProtocol` and `schemas.BaseOAuthAccount` — and neither is used to carry it. | `oauth_name: str,` / `access_token: str,` / `account_id: str,` |
| P1-29 | fastapi_users/manager.py:204 | #18 | `oauth_callback` narrates its three branches with phase comments — `# Associate account` (:204), `# Create account` (:210), `# Update oauth` (:221) — inside one 82-line method with nested try/except/else. Each label is a function that was not extracted. | `# Associate account` / `# Create account` / `# Update oauth` |
| P1-30 | fastapi_users/authentication/authenticator.py:170 | #24 | Authentication parameters are addressed by runtime-constructed names: `_get_dependency_signature` builds `Parameter(name=name_to_variable_name(backend.name))` and `name_to_strategy_variable_name(...)` (:216, :221) via regex-scrubbed backend names, and `_authenticate` reads them back out with `kwargs[name_to_variable_name(backend.name)]` (:170-173). No grep connects the declaration to the use, and the failure mode (colliding scrubbed names) is caught only as a `ValueError` at :236. | `token = kwargs[name_to_variable_name(backend.name)]` / `strategy = kwargs[name_to_strategy_variable_name(backend.name)]` |
| P1-31 | fastapi_users/db/__init__.py:15 | #26 | The public export list is assembled by nine `__all__.append(...)` calls inside two `try/except ImportError` blocks (:15-19, :31-34); a reader must execute the module to learn what `fastapi_users.db` exports. | `__all__.append("SQLAlchemyBaseUserTable")` / `__all__.append("SQLAlchemyBaseUserTableUUID")` |
| P1-32 | fastapi_users/router/__init__.py:23 | #26 | Same pattern, smaller: `__all__` is declared literally at :8-15 and then mutated at :23-24 inside a `try`. Two mechanisms for one list in one file. | `__all__.append("get_oauth_router")` / `__all__.append("get_oauth_associate_router")` |
| P1-33 | fastapi_users/authentication/strategy/__init__.py:26 | #26 | The literal `__all__` promises `"RedisStrategy"` (:26) but the name is only bound inside `try: ... except ImportError: pass` (:13-16) — with redis absent, the declaration names a symbol that does not exist and `from ... import *` raises AttributeError. Identical defect at authentication/__init__.py:22. | `except ImportError:  # pragma: no cover` / `    pass` / `__all__ = [... "RedisStrategy"]` |
| P1-34 | fastapi_users/manager.py:1 | #29 | 34 of the 35 modules in the package have no module docstring — including the four heaviest entry points (manager.py, 715 lines; router/oauth.py, 384; authentication/authenticator.py, 237; router/users.py, 225). Only `fastapi_users/__init__.py` has one. A reader opening manager.py gets an import block and must infer the module's job from its class. | `import uuid` / `from typing import Any, Generic` |
| P1-35 | fastapi_users/router/oauth.py:26 | #6 | `generate_state_token` is named as a pure producer but mutates its caller's dict in place (`data["aud"] = STATE_TOKEN_AUDIENCE`, :29) before encoding. `generate_jwt` (jwt.py:23) copies before writing `exp`; this one does not. Both call sites (:83, :270) happen to pass a throwaway literal, so the effect is invisible until someone reuses the dict. | `def generate_state_token(data: dict[str, str], ...) -> str:` / `    data["aud"] = STATE_TOKEN_AUDIENCE` |
| P1-36 | fastapi_users/authentication/authenticator.py:168 | #19 | `for backend in self.backends: if backend in enabled_backends:` — `enabled_backends` is typed `Sequence`, so membership is a linear scan per backend on every authenticated request (O(n·m) over the same backend objects). A set of names, or intersecting once before the loop, is the same code. | `for backend in self.backends:` / `    if backend in enabled_backends:` |
| P1-37 | fastapi_users/authentication/authenticator.py:179 | #23 | The accept/reject decision is expressed as a mutated `status_code` plus reassignments of `user` to `None`: 401 is set, overwritten with 403 when a user exists, then possibly reset to 401 (:183), and `verified and not user.is_verified or superuser and not user.is_superuser` (:186) relies on `and`/`or` precedence with no parentheses. Four booleans and two outputs braided into one 13-line block. | `elif (` / `    verified and not user.is_verified or superuser and not user.is_superuser` / `):` |
| P1-38 | fastapi_users/router/auth.py:71 | #12 | `{**{k: v}, **backend.transport...}` unpacks a dict literal into a dict literal — the inner `**{}` is pure ceremony; `{status.HTTP_401_UNAUTHORIZED: {...}, **backend.transport.get_openapi_logout_responses_success()}` is the same value. The sibling `login_responses` at :23-42 already writes it that way. | `logout_responses: OpenAPIResponseType = {` / `    **{status.HTTP_401_UNAUTHORIZED: {"description": ...}},` |
| P1-39 | fastapi_users/manager.py:436 | none | Likely bug: `reset_password` computes `updated_user` at :434 and returns it, but fires the hook with the *stale* `user` — `await self.on_after_reset_password(user, request)`. The parallel method `verify` (:354) passes `verified_user`. A user-supplied hook therefore sees the pre-reset object. | `updated_user = await self._update(user, {"password": password})` / `await self.on_after_reset_password(user, request)` |
| P1-40 | fastapi_users/exceptions.py:37 | none | `InvalidPasswordException.__init__` sets `self.reason` and never calls `super().__init__(reason)`, so `str(exc)` is empty and the reason is lost from tracebacks and logs — the only place it survives is the hand-built HTTP payloads. | `def __init__(self, reason: Any) -> None:` / `    self.reason = reason` |
| P1-41 | fastapi_users/router/common.py:10 | none | `ErrorCodeReasonModel` is declared and never referenced anywhere in the repo (only occurrence is its own definition). Meanwhile `ErrorModel.detail` at :7 is typed `str \| dict[str, str]` — the loose spelling of exactly the model sitting unused three lines below, and the actual payloads (register.py:66-69) are `{"code": ..., "reason": ...}`. | `class ErrorCodeReasonModel(BaseModel):` / `    code: str` / `    reason: str` |
| P1-42 | fastapi_users/fastapi_users.py:160 | none | API asymmetry: `get_oauth_router` marks the six cookie options keyword-only with `*` (:106) but `get_oauth_associate_router` (:160-165) does not, so the same six options are positionally passable on one method and not the other — and the underlying `get_oauth_associate_router` in oauth.py:224 *is* keyword-only. | `requires_verification: bool = False,` / `csrf_token_cookie_name: str = CSRF_TOKEN_COOKIE_NAME,` |
| P1-43 | fastapi_users/authentication/authenticator.py:76 | none | The `optional` parameter doc contains a duplicated, self-contradicting pair of lines — "Otherwise, throw `401 Unauthorized`. Defaults to `False`." immediately followed by "Otherwise, an exception is raised. Defaults to `False`." — copied into both docstrings (:76-77 and :121-122), so the error propagates twice. | `Otherwise, throw \`401 Unauthorized\`. Defaults to \`False\`.` / `Otherwise, an exception is raised. Defaults to \`False\`.` |
| P1-44 | fastapi_users/authentication/authenticator.py:204 | none | The `try:` at :204 wraps 31 lines — list construction, the backend loop with `Depends(...)` calls, and the `Signature(...)` call — but the `except ValueError` at :236 reports every failure as `DuplicateBackendNamesError()`. Only `Signature(parameters)` raises for duplicate names; any other ValueError is silently relabelled, and the original is discarded (no `from e`). | `except ValueError:` / `    raise DuplicateBackendNamesError()` |
| P1-45 | fastapi_users/manager.py:222 | none | The OAuth-account update loop scans every account with no `break` after the match at :227, so a user with duplicate matching accounts issues repeated DB updates; and when nothing matches the method silently returns the unchanged user, indistinguishable from a successful update. | `for existing_oauth_account in user.oauth_accounts:` / `    if (existing_oauth_account.account_id == account_id` |
| P1-46 | fastapi_users/db/base.py:10 | none | Two sibling adapter contracts, two mechanisms: `BaseUserDatabase` is a plain `Generic` class whose eight methods `raise NotImplementedError()`, while the exactly analogous `AccessTokenDatabase` (strategy/db/adapter.py:7) is a `Protocol` with `...` bodies. An implementor of both must learn two extension idioms in one library. | `class BaseUserDatabase(Generic[UP, ID]):` / `    async def get(self, id: ID) -> UP \| None:` / `        raise NotImplementedError()` |
| P1-47 | fastapi_users/router/verify.py:9 | none | `get_verify_router` is the only one of the six router factories with neither a return annotation nor a docstring; its five siblings (auth.py:16, register.py:12, reset.py:37, users.py:15, oauth.py:52) all declare `-> APIRouter` and a one-line summary. | `def get_verify_router(` / `    get_user_manager: UserManagerDependency[models.UP, models.ID],` / `    user_schema: type[schemas.U],` |
| P1-48 | test_build.py:6 | none | The packaging smoke test uses a bare `except:` — it catches `KeyboardInterrupt`/`SystemExit` too, and swallows the actual import error it exists to diagnose, exiting 1 with no message. | `except:` / `    sys.exit(1)` |

48 sites: #1 ×10, #9 ×2, #11 ×12, #13 ×2, #14 ×2, #18 ×1, #24 ×1, #26 ×3, #29 ×1,
#6 ×1, #19 ×1, #23 ×1, #12 ×1, none ×10.

Deliberately not listed: `schemas.py:77 oauth_accounts: list[...] = []` (pydantic
deep-copies field defaults, so the mutable-default shape is not a real defect here);
the ten `on_after_*` no-op hook methods in manager.py (identical bodies, but they are
a documented override surface, not a clone to merge); `ErrorCode` enum members whose
values repeat their names (verbose, but explicit values are the safer choice for a
wire contract).

## Phase 2 — audit finding verdicts

Audit: 385 findings, 194 heuristic + 191 indexed, 0 proved (oracle under-fired —
target deps unresolved, per coordinator; oracle rule #2 and honesty/WP rules #4-6
did not run). Grouped where a rule fires many near-identical times; real/fp splits
broken out so every finding line is accounted for. The count in each row's last
sentence sums to the rule total.

| finding (path:line) | rule | tier | verdict | why |
|---------------------|------|------|---------|-----|
| fastapi_users/db/base.py:22 (+11: adapter.py:16,20; db/base.py:26,35,44; jwt.py:18,35; manager.py:54,517,692,706) | 1 | heuristic | real | Every one is a genuine Any/bare-dict on a public boundary — the untyped user/token persistence dicts, parse_id(value: Any) and its two overrides, generate_jwt(data: dict), decode_jwt returning dict[...,Any]. 12/12 real. |
| fastapi_users/db/base.py:18; fastapi_users/manager.py:152 | 8 | indexed | real | account_id: str and access_token: str are genuine recurring primitives across the OAuth signatures — legitimate NewType candidates; the "21 signatures" count is inflated by test mocks but the prod concept is real. 2/2 real. |
| fastapi_users/authentication/strategy/jwt.py:25; fastapi_users/jwt.py:34 | 9 | heuristic | real | Two genuine mutable defaults (token_audience=[...] stored on the instance; algorithms=[...] passed into jwt.decode). 2/2 real. |
| fastapi_users/router/users.py:87,174 (prod x2) + 118 test/example/doc clones (repr: tests/test_fastapi_users.py:156; tests/test_router_users.py; docs/src/db_sqlalchemy.py:1 == examples/sqlalchemy/app/db.py:1) | 11 | indexed | real | The update_me/update_user prod clone is real (my P1-14). The 118 non-prod are genuine AST-normalized structural clones: repetitive endpoint tests (parametrization candidates) and doc-snippet/example duplication that genuinely drifts. 120/120 real. |
| fastapi_users/password.py:31,36,39 | 13 | indexed | real | verify_and_update/hash/generate are pure one-line forwards (to self.password_hash.* / secrets.token_urlsafe) adding nothing. 3 real. |
| tests/conftest.py:506 | 13 | indexed | fp | MockTransport.get_logout_response does `return Response()` — constructs a fresh response and changes behavior (parent raises); a mock stub, not a forwarding middle-man. |
| fastapi_users/authentication/authenticator.py:62; fastapi_users/fastapi_users.py:98; fastapi_users/router/auth.py:11 | 14 | indexed | real | Genuine clumps that each name one concept: the 4 auth-requirement flags (active/optional/superuser/verified), the 6 csrf_token_cookie_* options (my P1-27), the router-factory dep trio. 3 real. |
| fastapi_users/manager.py:440; fastapi_users/router/oauth.py:323 (+33 test clumps, repr: tests/test_router_oauth.py:146) | 14 | indexed | fp | (request,user,user_*) co-occur because request is ambient context threaded everywhere — not a cohesive type. The 33 test clumps are pytest fixtures that travel together by injection; bundling them fights the framework. 35 fp. |
| fastapi_users/authentication/strategy/db/strategy.py:21,43 (+15 prod: jwt.py:43, redis.py:23, manager.py:440,636, router/auth.py:49 x3 & :83, oauth.py:139 x3 & :323, reset.py:46, users.py:26, verify.py:20) + tests/conftest.py:515 | 15 | heuristic/indexed | real | Genuine demand>use: write_token(user) reads only .id; strategies take the whole user_manager for .get/.parse_id; handlers take a full Request for .cookies. Framework/Protocol contracts make several unactionable, but each instance is real. 18 real. |
| tests/test_manager.py; tests/test_router_oauth.py:168; tests/test_router_users.py (64 test-function fixture params, repr: tests/test_authentication_strategy_jwt.py:78) | 15 | heuristic | fp | Test functions receive pytest fixtures by name and use a few attributes — idiomatic injection, not a narrowable demanded parameter. 64 fp. |
| fastapi_users/manager.py:18 (hash, get_by_email); tests/test_authentication_strategy_db.py:26; tests/test_authentication_strategy_redis.py:9 | 21 | heuristic | fp | None is a distributed invariant: self.password_helper.hash(...) and self.get_by_email(...) are ordinary reuse of already-encapsulated methods; self.store[k] is trivial dict indexing in a 10-line mock. Encapsulation adds nothing. 4 fp. |
| fastapi_users/fastapi_users.py:54,67,75,79,98,153,203 | 22 | heuristic | real | The seven get_*_router facade methods use only injected collaborators and forward to identically-named module functions that already take them — genuine free functions hiding in a namespace class. 7 real. |
| fastapi_users/manager.py:64 (+27 prod: strategy/transport/backend/manager domain methods, JWT encode_key/decode_key) + 8 test mocks (repr: tests/test_authentication_strategy_db.py:32) | 22 | heuristic | fp | Over-fires: these read instance data (self.user_db, self.secret, self.store) to do real stateful work — that is encapsulation of representation, not a free function. A property returning self.secret is the opposite of the defect. 36 fp. |
| tests/conftest.py:139,347,417,437; tests/test_authentication_strategy_db.py:53 | 24 | heuristic | real | All are setattr(obj, field, value) with a runtime-determined name (loop var / param) — genuinely ungreppable dynamic attribute writes that blind whole-program analysis, exactly the rule's soundness concern. 5 real. |
| fastapi_users/authentication/strategy/jwt.py:65 | 25 | indexed | real | write_token delegates to generate_jwt; the names share no stem, so tracing the call by grep breaks. Low-value but a genuine instance. |
| fastapi_users/models.py:30,45; fastapi_users/schemas.py:7 | 26 | heuristic | fp | OAP/UOAP/SCHEMA are plain TypeVar(...) declarations — literal idioms with no "members to know". Misread as computed declarations; the real dynamic-__all__ sites (db/__init__.py, router/__init__.py) were not flagged. 3 fp. |
| fastapi_users/manager.py:18 (+9 manager symbols) + tests/conftest.py:65 (+8 conftest symbols) | 27 | indexed | real | Both manager.py (715 L) and conftest.py (571 L) are genuinely large, high-fan-in modules where reading one symbol loads the whole file — accurate purchase-price instances. 19 real. |
| fastapi_users/manager.py:1; router/oauth.py:1; authenticator.py:1; router/users.py:1; fastapi_users.py:1; oauth.py:139 & :323 (heavy entries); verify.py:9 (heavy entry) + 6 big test modules (conftest, test_manager, test_router_oauth/users/auth, test_fastapi_users) | 29 | heuristic | real | Large modules (223-961 L) and genuinely heavy entry points (59-72 L) with no top-loading/cost docstring — a reader opening them gets an import block. 14 real. |
| fastapi_users/router/common.py:1 (30 L); jwt.py:1 (41 L); exceptions.py:1 (38 L); models.py:1 (45 L); 11 test modules <200 L; all examples/docs snippets (14-61 L); oauth.py:257 (31 L entry) | 29 | heuristic | fp | Violates "never punish small files": a 14-61 line module (and a 31-line function) fits on the first screen — it is its own top-loading; a docstring adds nothing. 36 fp. |

**Phase 2 totals: 385 judged — 206 real, 179 fp.**

## Phase 3 — reconciliation

Every phase-1 site classified against the audit. 13 covered, 35 misses
(19 detector-miss, 6 threshold-miss, 10 inventory-gap).

| P1 id | rule | class | note |
|-------|------|-------|------|
| P1-1 | #1 | covered | rule 1 fired db/base.py:22 |
| P1-2 | #1 | covered | rule 1 fired adapter.py:16,20 |
| P1-3 | #1 | covered | rule 1 fired manager.py:54 (+ overrides :692,:706) |
| P1-4 | #1 | covered | rule 1 fired manager.py:517 |
| P1-5 | #1 | covered | rule 1 fired jwt.py:18 |
| P1-6 | #1 | detector-miss | Any lives in the OpenAPIResponseType alias RHS; rule 1 scans signature params, not type-alias definitions |
| P1-7 | #1 | detector-miss | opaque *args:Any,**kwargs:Any on the generated dependency callables not flagged — rule 1 didn't reach authenticator.py |
| P1-8 | #1 | detector-miss | user=Depends(...) with no annotation = implicit Any; detector keys on explicit Any/dict tokens |
| P1-9 | #1 | detector-miss | reason: Any on InvalidPasswordException.__init__ is a public boundary param but was not flagged |
| P1-10 | #1 | detector-miss | missing return annotation -> inferred dict[str,Any]; detector needs an explicit token |
| P1-11 | #9 | covered | rule 9 fired jwt.py:25 |
| P1-12 | #9 | covered | rule 9 fired jwt.py:34 |
| P1-13 | #11 | covered | rule 11 caught the enclosing update_me/update_user clone (users.py:87,174) |
| P1-14 | #11 | covered | same rule-11 clone group |
| P1-15 | #11 | detector-miss | the two authorize functions (oauth.py:73,257) are a ~25-line function clone; rule 11 never fired on oauth.py |
| P1-16 | #11 | threshold-miss | CSRF-decode block is a sub-function block clone inside two larger, differing functions — under the function-level clone granularity |
| P1-17 | #11 | threshold-miss | redirect-branch / responses-dict sub-function block dup, same granularity gap |
| P1-18 | #11 | detector-miss | the 3 near-identical getters (manager.py:64/79/94) not caught as an x3 clone (rule 22 flagged them instead) |
| P1-19 | #11 | threshold-miss | verify/reset_password token ladder is a sub-function block pattern |
| P1-20 | #11 | threshold-miss | 5-line parse_id/get tail across 3 strategy files — below function-clone size (rule 15 fired on the methods) |
| P1-21 | #11 | detector-miss | _set_login_cookie/_set_logout_cookie x2 clone not caught (rule 22 flagged them) |
| P1-22 | #11 | detector-miss | current_user_token/current_user dup (incl. verbatim docstring) not caught |
| P1-23 | #11 | detector-miss | class-body field re-declaration is not a function/AST-clone target |
| P1-24 | #11 | threshold-miss | 4-line if-x-is-None/else idiom dup — below clone size |
| P1-25 | #13 | covered | site caught by rule 22 at fastapi_users.py:54-203 (free-function facade); #13 itself under-fired |
| P1-26 | #13 | covered | rule 13 fired password.py:31,36 |
| P1-27 | #14 | covered | rule 14 reported the csrf-cookie clump at fastapi_users.py:98 |
| P1-28 | #14 | threshold-miss | the 6-field OAuth-account clump appears in only 2 prod signatures — under the >=3-signature cutoff (rule 14 fired elsewhere in manager) |
| P1-29 | #18 | detector-miss | rule 18 produced 0 findings; the Associate/Create/Update phase comments not caught |
| P1-30 | #24 | detector-miss | dynamic name via kwargs[name_to_variable_name(...)] is a dict subscript, outside rule 24's getattr/setattr detector |
| P1-31 | #26 | detector-miss | dynamic __all__.append in db/__init__.py not caught; rule 26 fired only on TypeVars (fp) |
| P1-32 | #26 | detector-miss | same, router/__init__.py |
| P1-33 | #26 | detector-miss | same, strategy/__init__.py __all__ naming maybe-unbound RedisStrategy |
| P1-34 | #29 | covered | rule 29 fired manager.py:1 |
| P1-35 | #6 | detector-miss | honesty/WP rules did not run (deps unresolved); also #6 targets get_* accessors, not generate_* |
| P1-36 | #19 | detector-miss | rule 19 produced 0 findings |
| P1-37 | #23 | detector-miss | rule 23 produced 0 findings |
| P1-38 | #12 | detector-miss | rule 12 produced 0 findings; redundant **{} unpack not in the idiom catalog |
| P1-39 | none | inventory-gap | stale-user hook logic bug — no rule covers correctness |
| P1-40 | none | inventory-gap | missing super().__init__ — no rule covers it |
| P1-41 | none | inventory-gap | dead ErrorCodeReasonModel — no dead-code rule in the inventory |
| P1-42 | none | inventory-gap | keyword-only API asymmetry — uncovered |
| P1-43 | none | inventory-gap | contradictory duplicated docstring lines — doc rules cover integrity/presence, not self-contradiction |
| P1-44 | none | inventory-gap | over-broad try relabeling any ValueError — uncovered |
| P1-45 | none | inventory-gap | missing break + silent no-match return — uncovered |
| P1-46 | none | inventory-gap | Generic-vs-Protocol adapter inconsistency — uncovered |
| P1-47 | none | inventory-gap | missing return annotation + summary vs siblings (rule 29 fired here for the cost-doc aspect only) |
| P1-48 | none | inventory-gap | bare except: in the build smoke test — uncovered |

**Phase 3 totals: 48 sites — 13 covered, 19 detector-miss, 6 threshold-miss, 10 inventory-gap.**
