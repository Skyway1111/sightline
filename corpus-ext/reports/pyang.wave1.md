# pyang — wave 1

Repo: `<GAUNTLET_CORPUS_ROOT>\pyang`
Prod tree judged: `pyang/**` (package, plugins, translators, transforms,
scripts). `pyang/yacc.py` is a verbatim vendored copy of PLY (dabeaz
copyright header, lines 1-28) and is excluded as vendored code. `test/`,
`doc/`, `tools/`, `xslt/`, `modules/` not judged.

## Phase 1 — blind ideal sites

| id | site (path:line) | rule | claim | excerpt (<=3 lines) |
|----|------------------|------|-------|---------------------|
| P1-1 | pyang/plugin.py:10 | #9 | `plugins` is a module-level list mutated cross-module: appended by `register_plugin` from every plugin module (`uml.py:30`, `lint.py:17`, `tree.py:17`, ...) and iterated by `scripts/pyang_tool.py:44,221,286,335,426,483`. Registration order (thus option/format precedence) is import-order-dependent global state. | `plugins = []` |
| P1-2 | pyang/plugin.py:66 | #24 | `__import__(modname)` where `modname` is built from a directory listing (`fname[:-3]`). No plugin module name is greppable; this also blinds whole-program reachability for every plugin entry point. | `pluginmod = __import__(modname)` |
| P1-3 | pyang/plugin.py:70 | #34 | Debug spew in an exception handler: dumps the whole module `__dict__` to stdout before re-raising. A handler that prints module internals is leftover debugging, not an error strategy. | `print(pluginmod.__dict__)` |
| P1-4 | pyang/error.py:63 | #9 | `error_codes` is a module-level dict mutated from at least 4 other modules via `add_error_code` (`plugins/check_update.py:54-116` alone adds ~20) and *rewritten in place* by `scripts/pyang_tool.py:307` (`error.error_codes[w] = (4, wstr)`). Error severity is global mutable state patched at runtime. | `error_codes = \` |
| P1-5 | pyang/error.py:568 | #41 | `err_add` linearly scans every previously recorded error on each add, so recording n errors is O(n^2). This is on the validation hot path — `err_add` is called from ~400 sites in `statements.py`/`types.py`/`grammar.py` during the per-statement walk, and a lint run over a large module set produces thousands of errors. A dedupe key set is the fix. | `for p, t, a in errors:` |
| P1-6 | pyang/error.py:553 | #32 | `fmt` is unpacked and never used. Same shape at `error.py:560` where `level` is unpacked and never used. | `(level, fmt) = error_codes[tag]` |
| P1-7 | pyang/util.py:139 | #9 | `files_read` is a module-level dict used as an unbounded set, written by `report_file_read` and never cleared. It silently changes the output of every verbose run in the same process (`"READ"` vs `"read"`), and is not reset by `Context.internal_reset`. | `files_read = {}` |
| P1-8 | pyang/util.py:39 | #12 | Hand-rolled `str.partition`: `find(':')` + two slices. `identifier.partition(':')` (or `split(':', 1)`) is the vocabulary. | `idx = identifier.find(":")` |
| P1-9 | pyang/util.py:39, pyang/types.py:253, pyang/scripts/pyang_tool.py:572 | #11 | Three copies of the same "split on first separator, handle the no-separator case" body — `util.split_identifier`, `IdentityrefTypeSpec.str_to_val`, `parse_features_string`. `types.py:253` even uses `find(":") == -1` then `split(':', 1)`, i.e. it reimplements the helper that already exists one import away. | `if string.find(":") == -1:` |
| P1-10 | pyang/util.py:55 | #12 | `guess_format` hand-loops characters to find the first non-space; `text.lstrip()` + a `startswith('<')` test says the same thing in one line. | `for char in text:` |
| P1-11 | pyang/context.py:43 | #11 | `Context.__init__:43-47` and `Context.internal_reset:53-58` are a byte-identical revs-loading loop (only the line wrapping differs). `internal_reset` should call the shared loader, not re-copy it. | `for mod, rev, handle in self.repository.get_modules_and_revisions(self):` |
| P1-12 | pyang/context.py:149 | #33 | `get_module` has three exits with two different shapes: an explicit `return None` on the `revision is None and not found` branch, and an *implicit* `None` fall-through when `revision is not None` but `(modulename, revision)` is not in `self.modules`. The `else: return None` is dead-ish decoration while the real miss path is silent. | `if (modulename,revision) in self.modules:` |
| P1-13 | pyang/context.py:180 | #34 | `except repository.ReadError as ex: i += 1; continue` — the exception is bound and thrown away, and the read failure is invisible to the caller. Same swallow at `context.py:337` (`except self.repository.ReadError as ex: return None`), where `ex` is again bound and unused. | `except repository.ReadError as ex:` |
| P1-14 | pyang/context.py:273 | #34 | A 12-line commented-out block (`# if modulename != module.arg:` through `# return None`) sits between the last statement and the `return module`. Git has this; the reader pays for it on every visit to the module-search path. | `# if modulename != module.arg:` |
| P1-15 | pyang/context.py:255 | none | `self.yin_module_map` is written here but is never initialized in `Context.__init__` (lines 18-41). It is created ad hoc in three unrelated places: `scripts/pyang_tool.py:385`, and a `hasattr` guard at `yin_parser.py:276-277`. Reaching `search_module`'s parsed-yintext branch through the library API (not the CLI) raises `AttributeError`. No rule covers "attribute the class never declares". | `self.yin_module_map[module.arg] = []` |
| P1-16 | pyang/context.py:342 | #12 | `for k in self.modules: m = self.modules[k]` — iterating keys and re-indexing instead of `.values()`. Repeated at `context.py:352` and `context.py:363-364`, and again in `scripts/pyang_tool.py:290-291` and `:304-305` (`for tag in error_codes: (level, fmt) = error_codes[tag]`). | `for k in self.modules:` |
| P1-17 | pyang/repository.py:92 | #34 | Two nested bare `except:` clauses (lines 92 and 96), the inner one `pass`. Any failure — including `KeyboardInterrupt` — inside the pip-location probe is silently discarded. | `except:` |
| P1-18 | pyang/repository.py:56 | none | `while use_env:` with `use_env = False` as the first statement is a one-iteration loop used purely as a `goto`-target for the four `break`s at lines 70, 76, 84 and the fallthrough. A reader must simulate the loop to learn it never repeats. No inventory rule covers control-flow-as-goto. | `while use_env:` |
| P1-19 | pyang/repository.py:114 | #32 | `_setup(self, ctx)` never reads `ctx`; it is threaded in only because `get_modules_and_revisions(self, ctx)` (line 138) also ignores it. The parameter is dead through both frames. | `def _setup(self, ctx):` |
| P1-20 | pyang/repository.py:145 | #12 | Manual `fd = None` / `try` / `finally: if fd is not None: fd.close()` reimplements `with io.open(...) as fd:`. The hand-rolled version also leaves the `in_format`/`guess_format` tail outside the guard for no reason. | `fd = None` |
| P1-21 | pyang/repository.py:15 | #33 | `Repository.get_modules_and_revisions` and `get_module_from_handle` (line 23) document a returned tuple ("Returns a tuple (`modulename`, `revision`, `handle`)") but have docstring-only bodies that return `None`. An abstract base that returns `None` instead of raising `NotImplementedError` turns a missing override into a `TypeError` far from the cause. | `def get_modules_and_revisions(self, ctx):` |
| P1-22 | pyang/repository.py:112 | #32 | `_add_directory` returns `True`/`False` (110, 112), and all six call sites (54, 61, 65, 69, 75, 99) discard it. The return contract is dead. | `return True` |
| P1-23 | pyang/repository.py:83 | none | `pkgutil.find_loader` was deprecated in 3.12 and removed in 3.14; `setup.py`/`README` do not cap the interpreter. On a modern interpreter this raises `AttributeError` inside `FileRepository.__init__`, the constructor every entry point runs. No inventory rule covers removed-stdlib-API use. | `if not pkgutil.find_loader('pip'):` |
| P1-24 | pyang/scripts/pyang_tool.py:22 | #18 | `run()` is a single 547-line function whose phases are narrated in prose instead of named: `# check for --plugindir` (29), `# Parse hello if present` (232), `# add standard search path` (249), `# make a map of features to support, per module` (271), `# patch the error spec ...` (302), `# apply deviations` (405), `# verify the given features` (440), `# transform modules` (464). Eight labelled phases is eight function boundaries spelled as comments. | `def run():` |
| P1-25 | pyang/scripts/pyang_tool.py:8 | #32 | `import codecs` (line 8) and `from pyang import statements` (line 18) are both unreferenced anywhere in the 583-line module. | `import codecs` |
| P1-26 | pyang/scripts/pyang_tool.py:29 | none | `--plugindir` is parsed twice from two unrelated pieces of code: a hand-rolled `sys.argv` scan at lines 30-39 (needed because plugins must load before `optparse` is built) and the real `optparse` option at lines 174-176 whose `dest="plugindir"` value is then never read. One flag, two parsers, one of them dead. | `if arg.startswith('--plugindir'):` |
| P1-27 | pyang/scripts/pyang_tool.py:468 | #41 | `ctx_validate_and_prune()` sits inside `for module in modules:` (line 470), so a transform that returns falsy re-validates the *entire* context once per module — O(modules x full-validation) where one pass suffices. Validation is the dominant cost of a pyang run; the sibling calls at 469 and 472 are correctly per-module, which is what makes the indentation of 473 look accidental. | `ctx_validate_and_prune()` |
| P1-28 | pyang/scripts/pyang_tool.py:563 | none | `-O/--overwrite` is documented as "Overwrite the output file, if it already exists", but both branches overwrite: `os.rename` (not-overwrite branch) replaces an existing destination, and `shutil.copyfile` (overwrite branch) does too. The flag changes the mechanism, never the documented behaviour. No inventory rule covers a CLI flag whose help text does not match its effect. | `if not o.overwrite_output_file:` |
| P1-29 | pyang/statements.py:1 | #29 | The 3557-line core module — every rule, phase and the `Statement` class itself — opens with `import copy` and has no module docstring. It is the single most-imported hand-written module in the repo; nothing on the first screen says what a validation phase is or where the entry point (`validate_module`, line 359) lives. | `import copy` |
| P1-30 | pyang/statements.py:1 | #27 | `statements.py` is 3557 lines and holds the hot symbols the whole codebase reaches for — `Statement`, `validate_module`, `search_typedef`, `mk_path_str`, `iterate_stmt`, `add_validation_fun` — so any agent touching a plugin, translator or parser ingests a 3557-line container to reach a 6-line helper (`get_description`, line 3552). The declarative tables (lines 122-345), the `Statement` class family (2993-3378) and the path/xpath helpers (3380-3557) share no state with each other. | `(file is 3557 lines)` |
| P1-31 | pyang/statements.py:178 | #13 | Every one of ~60 entries in `_validation_map` wraps its target in a lambda of the *identical* signature: `lambda ctx, s: v_init_module(ctx, s)`. The lambda adds nothing — the bare function object is the value. It also costs a Python frame per statement per phase on the hot validation walk and hides the callee from grep-by-reference. | `('init', 'module'):lambda ctx, s: v_init_module(ctx, s),` |
| P1-32 | pyang/statements.py:240 | #34 | Five lines of commented-out dict entries and their rationale sit inside the live `_validation_map` literal (`#    ('reference_3', 'must'):...`, `#    ('reference_3', 'when'):...`). | `#    ('reference_3', 'must'):lambda ctx, s:v_reference_must(ctx, s),` |
| P1-33 | pyang/statements.py:271 | #12 | `_keyword_with_children` is a dict whose every value is `True` and whose only use is membership (`keyword in _keyword_with_children`, line 71/285). Same dict-as-set shape at `_v_i_children` (line 254), `_v_i_children_keywords` (263), `_singleton_keywords` (325), `_deviate_delete_singleton_keywords` (335). A `set`/`frozenset` says it directly. | `_keyword_with_children = {` |
| P1-34 | pyang/statements.py:122 | #9 | Nine module-level mutables (`_validation_phases`, `_validation_map`, `_validation_variables`, `_v_i_children`, `_v_i_children_keywords`, `data_keywords`, `_keywords_with_no_explicit_config`, `_copy_uses_keywords`, `_refinements`, `_valid_deviations`) are mutated cross-module by the `add_*` API at lines 13-101 — `plugins/lint.py:91-116`, `plugins/ieee.py:47-53`, `plugins/smi.py`, `plugins/metadata.py` all write them at `setup_ctx` time. Validation behaviour therefore depends on which plugins were imported, with no way to scope or undo it. | `_validation_phases = [` |
| P1-35 | pyang/statements.py:82 | #13 | `add_xpath_function` is a body of exactly one forwarding call to `xpath.add_extra_xpath_function` with the same three arguments in the same order. | `xpath.add_extra_xpath_function(name, input_params, output_param)` |
| P1-36 | pyang/statements.py:17 | #12 | `add_validation_phase` keeps a manual `idx = 0 ... idx = idx + 1` cursor over `_validation_phases`; `enumerate` (or `list.index`) is the vocabulary. Identical hand-rolled cursor at `grammar.py:789` (`spec_del_kwd`) and `xpath.py:194` (`i = 0 ... i = i + 1` over `args`). | `idx = 0` |
| P1-37 | pyang/statements.py:372 | #11 | Inside `iterate`, the block `f = _validation_map[key]; res = f(ctx, stmt); if res == 'stop': raise Abort` appears three times verbatim (lines 372-375, 380-383, 387-390) with only the key expression differing. | `res = f(ctx, stmt)` |
| P1-38 | pyang/statements.py:391 | none | `if res == 'continue': pass else: <30 lines>` — an empty `if` arm whose only purpose is to avoid writing `!=`. The reader has to hold an inverted condition across a 30-line block. No inventory rule covers an empty guard arm. | `if res == 'continue':` |
| P1-39 | pyang/statements.py:3033 | none | `Statement.__slots__` ends with `'__dict__'`, which defeats the entire declaration: instances still get a dict, so neither the memory saving nor the typo protection survives, and the adjacent comment ("Fun to see in one place just how many *possible* attributes a Statement can have!") presents the list as an inventory it is not. `self.stmt_parent` (line 3046) is proof — it is assigned in `__init__` and absent from every `__slots__` in the hierarchy. No inventory rule covers a neutered `__slots__`. | `'__dict__',` |
| P1-40 | pyang/statements.py:3078 | #24 | `internal_reset` walks `getattr(cls, '__slots__', ())` and `delattr`s every name starting with `i_`. Attribute identity is a runtime string prefix, so no `i_*` field's lifetime is greppable and nothing static can tell which fields survive a reset. | `for s in getattr(cls, '__slots__', ()):` |
| P1-41 | pyang/statements.py:3141 | #32 | `Statement.pprint` is a public method with no caller anywhere in `pyang/` or `test/` — its only invocations are its own two recursive calls (3150, 3159). Same for `print_tree` (line 3380), zero callers. Both are stdout-printing debug helpers shipped in a library class. | `def pprint(self, indent='', f=None):` |
| P1-42 | pyang/statements.py:3461 | #25 | `get_xpath` delegates its entire body to `mk_path_str` with renamed keywords and nothing else. The two names share no token stem (`get`/`xpath` vs `mk`/`path`/`str`), so the call chain `get_xpath -> mk_path_str -> mk_path_list` cannot be followed by grepping any one concept. | `return mk_path_str(` |
| P1-43 | pyang/statements.py:3462 | #39 | `get_xpath`'s docstring is 30 lines (3462-3491) of format examples for a 6-line forwarding body. The prose outweighs the code by 5x, and every example is a restatement of what `mk_path_str`'s own docstring (3421-3441) already says. | `"""Gets the XPath path of the data node \`stmt\`.` |
| P1-44 | pyang/statements.py:3499 | #39 | `# Return type value if exists` restates `return getattr(type_obj, 'arg', None)` on the very next line. Identical restatement at `statements.py:3556` (`# Return description value if exists`) and `plugins/threegpp.py:307` (`# remove error from ctx` above `ctx.errors.remove(ctx_error)`). | `# Return type value if exists` |
| P1-45 | pyang/statements.py:3550 | #12 | `return True if <expr> else False` is `return bool(<expr>)` written the long way. Same shape at `syntax.py:205-208` (`if v >= 1 and v <= 18: return True else: return False`, which also declines `1 <= v <= 18`). | `return True if getattr(stmt, 'i_type_spec', None) else False` |
| P1-46 | pyang/statements.py:3519 | #2 | `if type_obj:` truth-tests a `Statement`, which defines neither `__bool__` nor `__len__`, so the test can only ever mean `is not None` — every other test of the same value in the file spells it `is not None`. Same at `statements.py:3542` (`elif type_obj and ...`) and `transforms/edit.py:155,165` (`if not imprev`, `if imprev:`). | `if type_obj:` |
| P1-47 | pyang/grammar.py:564 | #9 | `extension_modules` is a module-level list appended cross-module by `register_extension_module` and read on the grammar hot path (`_chk_stmts:606`). Whether a statement is grammar-checked depends on global registration order. | `extension_modules = []` |
| P1-48 | pyang/grammar.py:574 | #13 | `chk_module_statements`'s body is one forwarding call to `chk_statement` with the module-level constant `top_stmts` substituted. It prices a hop and a second docstring for a defaulted argument. | `return chk_statement(ctx, module_stmt, top_stmts, canonical)` |
| P1-49 | pyang/grammar.py:806 | #19 | `if bs not in res` and `if (kw, s) not in res` (line 809) are list-membership scans inside `flatten_spec`'s nested loops, so flattening a spec of n entries is O(n^2). `res` is order-sensitive, so the fix is a companion `set`, not a different container. | `if bs not in res:` |
| P1-50 | pyang/grammar.py:842 | #19 | `[stmt for stmt in stmts if stmt not in res]` scans `res` once per statement — quadratic in the substatement count, and it runs for every statement in the tree under `--canonical`. | `res.extend([stmt for stmt in stmts if stmt not in res])` |
| P1-51 | pyang/grammar.py:721 | #39 | `# occurane == '*'` sits above the `else:` arm and restates the condition the `elif` chain already established (the two preceding arms tested `'1'/'?'` and `'+'`), misspelling it in the process. | `# occurane == '*'` |
| P1-52 | pyang/syntax.py:3 | #32 | `import os` (line 3) and `import sys` (line 6) are unreferenced — neither `os.` nor `sys.` appears anywhere in the 379-line module. | `import os` |
| P1-53 | pyang/syntax.py:139 | #20 | `arg_type_map` repeats one lambda body 19 times: `lambda s: re_X.search(s) is not None`, varying only the regex object (lines 139-166). The predicate is one named helper (`def _matches(rx): return lambda s: rx.search(s) is not None`) applied 19 times. Five more entries (`"date"`, `"enum-arg"`, `"fraction-digits-arg"`, `"if-feature-expr"`) are pure forwarding lambdas (`lambda s: chk_date_arg(s)`) that could name the function. | `"identifier": lambda s: re_identifier.search(s) is not None,` |
| P1-54 | pyang/syntax.py:372 | none | `yin_map`'s docstring describes the pair components in the wrong order: it says the first component "specifies whether the argument is stored in a subelement" and the second "is the name of the attribute or subelement", but the data is `('name', False)` — name first, boolean second. Every reader who trusts the docstring reads the table backwards. No inventory rule covers a docstring that contradicts the literal beside it. | `The values are pairs whose first component specifies whether the` |
| P1-55 | pyang/types.py:154 | #34 | Commented-out code plus its own history narration: `# slen = len(s.rstrip('0')) # ignore trailing zeroes` followed by `# No, do not ignore trailing zeroes!` — a rejected implementation and the argument against it, both kept in the live decimal parser. | `#            slen = len(s.rstrip('0')) # ignore trailing zeroes` |
| P1-56 | pyang/types.py:223 | #34 | Bare `except:` around `base64.b64decode`, and the handler records an error but falls off the end returning `None` while the success path returns `bytes` — the failure is both over-caught and silently reshaped. | `except:` |
| P1-57 | pyang/types.py:294 | #19 | `if val not in visited` is a list-membership scan inside the recursive identity walk, and `visited` grows with every base identity crossed. Identity derivation over a deep `identityref` hierarchy is quadratic by construction; `visited` is used only for membership, so it wants a `set`. | `if val not in visited:` |
| P1-58 | pyang/types.py:10, pyang/error.py:35, pyang/statements.py:109 | #11 | Three separate `class Abort(Exception)` declarations, one per module, for the same "abort this walk" concept. `statements.py` and `types.py` both `from .error import err_add`, so both already see `error.Abort` and shadow it. A reader tracing an `Abort` must first work out which one. | `class Abort(Exception):` |
| P1-59 | pyang/xpath.py:95 | #14 | `(ctx, mod, pos, initial, node)` travels as an unnamed 5-tuple through `chk_xpath_expr` (95), `chk_xpath_function` (157) and `chk_xpath_path` (219), threaded verbatim 22 times in a 351-line module. That group is the XPath checking context and wants a name. | `def chk_xpath_expr(ctx, mod, pos, initial, node, q, t):` |
| P1-60 | pyang/xpath.py:107 | #11 | The `'comp'`, `'arith'` and `'bool'` arms (107-108, 110-111, 113-114) have byte-identical two-line bodies. Three copies of one behaviour that a single `q[0] in ('comp', 'arith', 'bool')` arm would state once. | `elif q[0] == 'comp':` |
| P1-61 | pyang/xpath.py:157 | #33 | `chk_xpath_function` returns `None` on two error paths (163, 171) and a `(signature[1], tgt)` tuple on the success paths (216, 222) — but its only caller, `chk_xpath_expr:117`, discards the result entirely. The tuple contract exists for nobody. | `return None` |
| P1-62 | pyang/xpath.py:44, :52 | #38 | `'deref': (['node-set'], 'node-set')` is declared identically in `yang_1_1_xpath_functions` (44) and `extra_xpath_functions` (52). `chk_xpath_function` then has to test both dicts (161, 165, 167) for the same fact; a signature change updates one and not the other. | `'deref': (['node-set'], 'node-set'), # pyang extension for 1.0` |
| P1-63 | pyang/plugin.py:19 | #35 | `from .translators import yang,yin,dsdl` is a function-local import inside `plugin.init` that hides a real cycle: all three translators import `pyang.plugin` at module level (`translators/yang.py:5`, `yin.py:9`, `dsdl.py:50`). The lazy import makes the cycle load-order-dependent rather than absent. | `from .translators import yang,yin,dsdl` |
| P1-64 | pyang/yang_parser.py:367 | #32 | `# FIXME: tmp debug` heads two module-level functions, `ppkeywd` (368) and `pp` (374), that nothing in `pyang/` or `test/` calls — `pp` calls itself and `ppkeywd`, and that is the whole reference graph. Debug scaffolding shipped in the parser. | `# FIXME: tmp debug` |
| P1-65 | pyang/hello.py:3 | #38 | `NC_NS_URI` is defined at line 3 and the same literal is immediately re-typed as the first key of `CAPABILITIES` (line 5). One URI, two homes, two lines apart. | `NC_NS_URI ="urn:ietf:params:xml:ns:netconf:base:1.0"` |
| P1-66 | pyang/hello.py:79 | #12 | `if m is None or m in res.keys()` — `.keys()` is redundant on a dict membership test; `m in res` is the idiom and reads as the intent. | `if m is None or m in res.keys():` |
| P1-67 | pyang/hello.py:92 | #12 | `set(dict(self.yang_modules()).keys())` round-trips `dict_items -> dict -> keys -> set` to get module names that `yang_modules` already had as dict keys before it returned `res.items()`. Three conversions for a projection. | `advertised_modules = set(dict(self.yang_modules()).keys())` |
| P1-68 | pyang/hello.py:106 | #12 | Builds a full list comprehension over every capability and takes `[0]` — `next(c for c in ... )` states "the first match" and stops there; the current form also raises a bare `IndexError` with no context when the module is not advertised. | `mcap = [ c for c in self.capabilities` |
| P1-69 | pyang/translators/dsdl.py:354 | #37 | `debug=0` is a knob no prod caller ever turns: the single call site (`dsdl.py:106`) passes `debug=0` explicitly, there is no `--dsdl-debug` option among the four the plugin registers (lines 62-84), and the only consumer is the dead branch `if self.debug > 0:` at line 919. The parameter, the instance field (367), its docstring entry (157) and the branch are all flexibility nobody exercises. | `record_defs=False, lax_yang_version=False, debug=0):` |
| P1-70 | pyang/translators/dsdl.py:98 | #11 | The emit-precheck block `for epos, etag, eargs in ctx.errors: if error.is_error(error.err_level(etag)): raise error.EmitError("<name> needs a valid module")` is copied verbatim into 7 modules — `dsdl.py:98`, `plugins/jsonxsl.py:71`, `plugins/jtox.py:43`, `plugins/sample-xml-skeleton.py:90`, `plugins/uml.py:137`, `plugins/check_update.py:214`, `plugins/depend.py:58` — differing only in the message string; `plugins/sid.py:102` is an eighth, hand-varied copy (`for _, etag, _`). `epos` and `eargs` are bound and unused in every copy. | `for epos, etag, eargs in ctx.errors:` |
| P1-71 | pyang/transforms/edit.py:111 | #24 | The option name is assembled at runtime — `('%s-%s' % (plugin_name, key)).replace('-', '_')` — then fed to `getattr(ctx.opts, dest, None)`. No `--edit-*` option's `dest` (`edit_yang_version`, `edit_namespace`, `edit_organization`, ...) is findable by grep from the code that reads it, and the `default=None` swallows every typo. | `dest = ('%s-%s' % (plugin_name, key)).replace('-', '_')` |
| P1-72 | pyang/transforms/edit.py:341 | #34 | `del stmt` deletes a local name in a frame that is about to be discarded. It has no effect on the object, the caller, or anything else — it reads as an intent to free memory and does nothing. | `del stmt` |
| P1-73 | pyang/transforms/edit.py:338 | #12 | `if stmt in parent.substmts: idx = parent.substmts.index(stmt); del parent.substmts[idx]` scans the list twice to do what `parent.substmts.remove(stmt)` does once. | `if stmt in parent.substmts:` |
| P1-74 | pyang/transforms/edit.py:163 | #32 | `delete_import_date(ctx, stmt)` never reads `ctx`, and the author says so in a `# noinspection PyUnusedLocal` pragma directly above (line 162). Same pragma-and-dead-parameter at `edit.py:170` (`set_meta_details`), which additionally unpacks an unused `ignore` at line 171, and at `edit.py:20` (`check_date`, dead `option`/`opt`). Three suppressed dead parameters in a 341-line module. | `# noinspection PyUnusedLocal` |
| P1-75 | pyang/plugins/threegpp.py:291 | #34 | A string literal placed *after* `if not ctx.opts.threegpp: return` is not a docstring — it is a no-op expression statement that never even evaluates on the early-return path. The method's actual purpose is documented in a statement Python discards. | `"""Remove some lint errors that 3GPP considers acceptable"""` |
| P1-76 | pyang/plugins/threegpp.py:297 | #12 | A ten-way `or` chain of `ctx_error[2][1] == '<keyword>'` (lines 297-305) is a set membership test written out longhand: `ctx_error[2][1] in {...}`. | `and (ctx_error[2][1] == 'enum'` |
| P1-77 | pyang/plugins/threegpp.py:307 | #19 | `ctx.errors.remove(ctx_error)` inside `for ctx_error in ctx.errors[:]` scans the live list from the front for every removal — O(n^2) over the error list, which for a lint run over a 3GPP module set is exactly the case where many errors are removed. A filtering rebuild is one pass. | `ctx.errors.remove(ctx_error)` |
| P1-78 | pyang/plugins/check_update.py:509 | #11 | `chk_must` (509) and `chk_when` (528) are the same function with `'must'` substituted for `'when'`: same double `search`, same remove-the-common-ones loop, same empty-list dispatch. Two copies of one algorithm parameterised by a keyword string. | `def chk_must(old, new, ctx):` |
| P1-79 | pyang/plugins/check_update.py:516 | #19 | `newmust.remove(newm)` / `oldmust.remove(oldm)` inside the loop at 513 make the common-must elimination quadratic in the number of `must` statements. Same shape at 535-536 (`chk_when`) and 663 (`oldunique.remove(o)`). | `newmust.remove(newm)` |
| P1-80 | pyang/plugins/jstree.py:439 | #32 | `element_htmnl_info = ""` is assigned and never read — the only occurrence of the (misspelled) name in the file. | `element_htmnl_info = ""` |
| P1-81 | pyang/plugins/jstree.py:531 | #32 | Inside `get_nontypedefstring`, `found` is initialised `False` and set `True` in seven branches (535, 541, 547, 552, 562, 566, 571) but never read. A whole tracked flag with no consumer. | `found  = False` |
| P1-82 | pyang/plugins/jstree.py:582 | #32 | `type_namespace = None` (582) and `i_type_name = None` (583) are assigned in `typestring` and never referenced again in the function. | `type_namespace = None` |
| P1-83 | pyang/plugins/jstree.py:594 | #33 | `typestring` returns a string on every other path but a bare `return` (implicit `None`) when the prefix does not resolve, and its caller concatenates the result. The bare return is the one path that hands back a non-string. | `return` |
| P1-84 | pyang/plugins/jstree.py:503 | #33 | `get_status_str` returns the string `'current'` on one branch and the `status` **Statement object** on the other. A `*_str` function that returns a statement makes every call site guess. | `return status` |
| P1-85 | pyang/plugins/jstree.py:284 | none | `from pyang.plugins.html import force_link` (also at line 442) imports a module that does not exist in the tree — `pyang/plugins/` has no `html.py`. It is reachable whenever an out-of-tree plugin sets `ctx.html_plugin_user`, so the coupling is real but unverifiable from this repo, and the import is duplicated in two functions rather than hoisted. No inventory rule covers an import of a module absent from the tree. | `from pyang.plugins.html import force_link` |
| P1-86 | pyang/plugins/tree.py:494 | #33 | `get_status_str` returns `'+'`, `'x'`, `'o'` for three known status values and falls off the end returning `None` for anything else — and the value comes from parsed YANG, so a status the grammar rejected still reaches here. The caller concatenates the result into the tree line. | `elif status.arg == 'obsolete':` |
| P1-87 | pyang/plugins/tree.py:519 | #33 | `get_leafref_path` mixes exit shapes inside one three-branch decision: an explicit `return None` when `t is None`, an implicit `None` when `t` is not a leafref, and a value otherwise. The two None paths are written differently for no reason. | `if t.arg == 'leafref':` |
| P1-88 | pyang/plugins/mef.py:19 | #11 | `add_opts` is copied across the derived lint plugins — `mef.py:19`, `bbf.py:20`, `ieee.py:22`, plus `lint.py`-derived `ietf.py` and `threegpp.py` — with only the flag name and the "<X> rules." help string differing, and each is paired with an identically-shaped `setup_ctx` guard (`if not ctx.opts.<flag>: return; self._setup_ctx(ctx)`). Five copies of one registration shape that `LintPlugin` could own. | `optparse.make_option("--mef",` |
| P1-89 | pyang/plugins/ieee.py:19 | #13 | `IEEEPlugin.__init__` does nothing but call `lint.LintPlugin.__init__(self)` — unlike its siblings (`mef.py:15-17`, `bbf.py:15-18`) it sets no prefixes, so the override adds nothing at all. | `lint.LintPlugin.__init__(self)` |
| P1-90 | pyang/plugins/lint.py:103 | #13 | The same forwarding-lambda shape as `_validation_map`: `lambda ctx, s: v_chk_default(ctx, s)` at 103-110, repeated for `v_chk_required_substmt`, `v_chk_recommended_substmt`, `v_chk_hyphenated_names` (115) and the namespace/prefix checks. The bare function is the value. | `lambda ctx, s: v_chk_default(ctx, s))` |
| P1-91 | pyang/plugins/uml.py:466 | #34 | The heaviest commented-out-code concentration in the repo: `uml.py` carries dead code at 466-467, 513, 552, 639-640, 670, 824, 834, 844-845, 853, 880-881, 886 — whole call sites and variable assignments left in place across the emitter. | `# for s in stmt.substmts:` |
| P1-92 | pyang/plugins/uml.py:394 | #39 | `# FIXME: previous code skipped first char, possibly in error` narrates the file's edit history rather than the current behaviour, and is duplicated verbatim at line 999. Git holds the previous code; the comment tells a reader nothing about what the line in front of them does. | `# FIXME: previous code skipped first char, possibly in error` |
| P1-93 | pyang/plugins/uml.py:954, pyang/plugins/jstree.py:527 | #11 | `UMLPlugin.typestring` and `jstree.typestring` render a YANG type to a display string with the same branch skeleton (`enumeration` -> comma-joined `substmts`, `leafref` -> `search_one('path')`, `union` -> joined `search('type')`, then range/length/pattern suffixes), independently drifted. `plugins/tree.py:527` (`get_typename`) and `plugins/omni.py:276` (`get_typename`) are a third and fourth partial copy of the same concept. | `def typestring(self, node):` |
| P1-94 | pyang/statements.py:2932 | none | `# FIXME: what is this??` is a comment recording that the author did not understand the code beneath it. It transfers confusion, not knowledge, and has outlived whatever session produced it. No inventory rule covers a comment that documents ignorance. | `# FIXME: what is this??` |
| P1-95 | pyang/statements.py:3495 | #28 | `get_type`'s docstring names a symbol that does not exist: "TODO: Add get_prefixed_type method to get prefixed types." There is no `get_prefixed_type` anywhere in the repo, so a reader (or agent) following the docstring's pointer finds nothing. | `TODO: Add get_prefixed_type method to get prefixed types.` |
| P1-96 | pyang/plugins/flatten.py:317 | #24 | `getattr(module, "i_children", [])` (317), `getattr(module, "i_not_supported", [])` (323) and `getattr(child, "i_this_not_supported", False)` (355) reach for `i_*` fields by string name with a silent default, so a renamed or never-set field degrades to "empty" instead of failing. The same string-keyed reach appears ~25 times in `statements.py` (e.g. 368, 1177, 2025, 2520). | `for child in getattr(module, "i_children", [])` |
| P1-97 | pyang/util.py:7 | #12 | `attrsearch` is a hand-rolled `next((x for x in in_list if getattr(x, attr) == tag), None)`; `keysearch` (15) and `dictsearch` (23) are the same linear-find loop keyed differently. Three functions, one idiom, none of them naming what they search for. | `for x in in_list:` |
| P1-98 | pyang/util.py:9 | #24 | `getattr(x, attr)` where `attr` is a caller-supplied string makes every field `attrsearch` touches invisible to grep — the call sites pass `'arg'`, `'keyword'` etc. as data. | `if getattr(x, attr) == tag:` |
| P1-99 | pyang/error.py:574 | #8 | Error severity is a bare `int` whose meaning lives only in a comment block (lines 58-62) and is re-decoded by three separate ad-hoc predicates: `is_error(level): level < 4`, `allow_warning(level): level > 2`, `is_warning(level): not is_error(level)`, plus open-coded `(4, wstr)` writes at `scripts/pyang_tool.py:307`. Four sites re-validate the same four-value domain that wants to be a named type. | `def is_warning(level):` |
| P1-100 | pyang/plugins/jsonxsl.py:107 | #19 | `yam.i_children.remove(ch)` inside the loop at 100 re-scans the child list from the front for every rpc/notification removed — quadratic in the child count. It also mutates the shared validated tree that every other emit plugin reads, from inside an emitter. | `yam.i_children.remove(ch)` |

## Phase 2 — audit finding verdicts

1378 findings judged: **654 real / 724 fp**. Large homogeneous families are
grouped by distinct shape with an instance count; every group names the
spot-checks actually read in the repo. Group counts sum to 1378.

Two systematic discriminators were applied by script over the repo AST and are
named where used:

- **PLY reflection.** `pyang/xpath_parser.py` and `pyang/xpath_lexer.py` are PLY
  grammar/lexer modules. `p_*` rule functions, `tokens`, and the `input`/`token`
  lexer protocol are bound by `yacc.yacc()` through `get_caller_module_dict`
  reflection, so "never referenced" is false for them.
- **Contract-forced signatures.** `_validation_map` /
  `statements.add_validation_fun` callbacks are all `f(ctx, stmt)`, and
  `check_update.chk_type_func` entries are all `(old, new, oldts, newts, ctx)`.
  A param unused inside one table member is required by the table.

| finding (path:line) | rule | tier | verdict | why |
|---------------------|------|------|---------|-----|
| plugins/sid.py:1130 (x2: param `sid_cont`, return) | #1 | heuristic | real | The repo's only annotated signature, and both sides are a bare `dict` on a public `@staticmethod`; the body then demands `assignment-range`/`entry-point`/`size` keys the type never names. |
| plugins/sid.py:365, :372, syntax.py:277 (x3) | #2 | heuristic | fp | pyright infers `self.range` / `operators[-1]` as `None` from the initializer alone; all three are assigned real values at runtime (`ctx.opts.sid_range`, `push_operator`). The no-overlap claim is an inference-scope artifact, not a redundant check. |
| statements.py:3204 | #3 | heuristic | real | `if len(deletes) > 0:` wraps `for d in deletes:` — a loop over an empty list runs zero times, so the guard is pure defensive noise. |
| 8 findings: statements.py:2910, :3117, :31, context.py:122, translators/yang.py:318, plugins/lint.py:301, types.py:460, test/test_feature/feature.py:9 | #4 | indexed | fp | The closed-world premise fails: pyang loads plugins out-of-tree (`pyang.plugin` entry_points, `PYANG_PLUGINPATH`), so `Context.add_parsed_module` and `Statement.copy` have callers the index cannot see. Traced three: `_sequence`'s `one is None` is reached on every first registration (`add_validation_fun` passes `_validation_map.get(...)`); `need_new_line`'s `max_line_len is not None` is reached because `ctx.max_line_len` defaults to `None`; `not_hyphenated`'s caller tests `arg_type`, which does not establish `stmt.arg is not None`. |
| repository.py:106 (`directory: str`, 6 call sites) | #5 | indexed | real | Six prod call sites all pass `str`; the lift is a genuine unencoded invariant in a fully unannotated repo. |
| context.py:287 (x2: `modulename: dict[str, bool]`, `revision: None`), plugins/flatten.py:345, :467 (x2) | #5 | indexed | fp | `read_module`'s proposals are positionally mis-mapped — the sole caller (`yin_parser.py:255`) passes `modname` plus `extra={...}`, and the dict landed on `modulename` while `revision` is never passed at all. The flatten pair are one-call-site lifts (`parent_flag: None` is not a type), the over-narrow-lift failure mode the design flags as its main FP engine. |
| 16 findings: repository.py:143, :138, context.py:149, :287, yang_parser.py:159, :85, :116, statements.py:2602, :1538, :2755, :1313, uml.py:1184, types.py:287, sid.py:947, :972, transforms/edit.py:251 | #6 | indexed | real | Verified the worst: `FileRepository.get_module_from_handle` opens a file *and* writes the `util.files_read` global; `Context.get_module` reaches `_ensure_revs`, which parses modules off disk and rewrites the `revs` list in place; `get_modules_and_revisions` scans the whole search path on first call; `statements.find_target_node` reports errors via `err_add` while "finding"; `sid.list_all_items`/`list_deleted_items` print to stdout; `edit.get_arg_value` reads a file for `@`-specs; the three `YangTokenizer.get_*` consume the buffer. |
| types.py:280, sample-xml-skeleton.py:173, :225, dsdl.py:1081, dsdl.py:1318 (x5) | #6 | indexed | fp | `is_derived_from` mutates only the `[]` it creates itself. `SampleXMLSkeletonPlugin.list`/`list_comment` and `dsdl.list_stmt` are handlers named after the YANG `list` keyword, not accessors. `string_type.get_data` is a nested closure with no arguments to mutate. |
| plugins/sid.py:810 | #7 | heuristic | real | `# Call only after validate_dep_revisions()` above `build_dependencies` — a precondition asserted nowhere, exactly the narrated protocol the rule targets. |
| error.py:63 | #9 | indexed | real | `error_codes` is rewritten in place at `scripts/pyang_tool.py:307` and grown by `add_error_code` from four plugins. (The finding undercounts the writers at "1 foreign module" — the indirect writers through `add_error_code` are missed — but the site is real.) |
| **#11 function clones — 16 groups / 35 instances**: bbf/ieee/mef/threegpp `add_opts` (x4); check_update `chk_rpc`/`chk_notification`/`chk_structure` (x3), `chk_container`/`chk_choice` (x2), `chk_leaf`/`chk_leaf_list` (x2); omni `print_rpc`/`print_notification` (x2); lint `v_chk_required_substmt`/`v_chk_recommended_substmt` (x2); statements `v_unused_typedef`/`v_unused_grouping` (x2); util `keysearch`/`dictsearch` (x2); types `IntTypeSpec.validate`/`Decimal64TypeSpec.validate` (x2), `StringTypeSpec.validate`/`BinaryTypeSpec.validate` (x2), `StringTypeSpec.__init__`/`BinaryTypeSpec.__init__` (x2); yang `need_quote`/`need_single_quote` (x2); threegpp `v_chk_yang_version_present`/`v_chk_module_reference_present` (x2); uml `emit_feature`/`emit_deviation` (x2); ieee `v_chk_module_name`/threegpp `v_chk_yang_version` (x2); test `composed.v_chk_module`/`v_chk_submodule` (x2) | #11 | indexed | real | Read all 16 group heads. Each pair/triple is the same body with one keyword or dict name substituted — `StringTypeSpec.__init__` and `BinaryTypeSpec.__init__` both hard-code `18446744073709551615`, and the four lint-derivative `add_opts` differ only in the flag name and the "<X> rules." string. |
| **#11 function clones — 3 groups / 20 instances**, all `xpath_parser.py` `p_*` (x15 @:65, x3 @:49, x2 @:58) | #11 | indexed | fp | PLY grammar actions. The body (`p[0] = p[1]`) is framework boilerplate; the content of each rule is its docstring grammar production. Deduplicating them would delete the grammar. |
| **#11 block clones — 56 groups / 142 instances**: types.py `parse_keypath`/`parse_key_predicate`/`parse_descendant` family (x5,x4,x3,x3,x2,x2,x2,x2,x2); check_update `chk_children`/`chk_leaf`/`chk_list`/`chk_module` (x5,x2,x2), `chk_must`/`chk_when` (x2), `chk_stmt`/`chk_stmt_definitions` (x2); uml `action_params`/`emit_action` (x4,x4,x4,x2,x2) and `attribs`/`typestring` (x4,x3,x2); statements `iterate` (x3), `v_expand_1_children` (x2,x2), `v_type_feature`/`v_type_identity` (x2), `v_key`/`v_unique` (x2), `v_default`/`v_type_leaf` (x2); context `__init__`/`internal_reset` (x2), `read_module`/`search_module` (x2); omni `fullpath` / uml `full_path`,`make_plantuml_keyword` / yang `escape_str` (x3,x3,x2); dsdl `case_stmt`/`choice_stmt`/`container_stmt` (x3); types `LengthTypeSpec`/`RangeTypeSpec` `__init__` and `inner_validate` (x2,x2,x2); plus 26 further groups | #11 | indexed | real | Spot-read 12: `Context.__init__:43-47` vs `internal_reset:53-58` byte-identical; `chk_must`/`chk_when` identical modulo `'must'`/`'when'`; `parse_key_predicate` repeats `s = s[N:] # skip 'x'` + `skip_space(s)` eight times where one `expect()` would do; `omni.fullpath` and `uml.make_plantuml_keyword` both hand-roll the same identifier sanitiser; `statements.iterate` repeats `res = f(ctx, stmt); if res == 'stop': raise Abort` three times; `jstree.action_params` / `uml.emit_action` share a 6-statement rpc-parameter walk. |
| **#11 block clones — 30 groups / 84 instances**, members entirely inside `add_opts` / `pyang_plugin_init` / `setup_ctx` / `_setup_ctx` / `emit_uml_header` / `emit_module_header` / `emit_css` / `emit_js` / `emit_bodystart` / `emit_tree` / `emit_yin` / `print_help` / `yang2html.run` | #11 | indexed | fp | These are declaration and emission tables: runs of `optparse.make_option(...)`, `error.add_error_code(...)`, `statements.add_validation_fun(...)`, and `fd.write("""...""")`. Collapsing them would replace a literal, greppable declaration with code that builds it — the opposite of what #26 asks for. |
| plugins/omni.py:113, statements.py:1002, check_update.py:817, hello.py:84, statements.py:3550 (x5) | #12 | heuristic | real | All five verified: `[n for n in newts.idbases]` is `list(...)`; `m in res.keys()` is `m in res`; `return True if getattr(...) else False` is `bool(...)`. |
| statements.py:2219, :2222 (to `v_xpath`); check_update.py:710, :713 (to `chk_i_children`), :731 (to `chk_range`), :609 (to `int`); identifiers.py:36, name.py:30 (plugin `emit` to module fn); structure.py:58; statements.py:82; transforms/edit.py:106 (x11) | #13 | indexed | real | Each prices a hop for nothing. `v_reference_must`/`v_reference_when` exist only to be dispatch-table values — the table could hold `v_xpath`. `_min_elements_val` is `int(arg)` wearing a comment about a default it does not apply. `structure.v_expand_2_augment_sx` forwards one call under a 16-line docstring copied from the callee. |
| grammar.py:567, plugin.py:74, statements.py:63, :73, :76, :79 (to `list.append`); types.py:383, :414, :546, :590, :669, :683, :764, :831, :849 (TypeSpec to `self.base.*`); yin_parser.py:29 (x16) | #13 | indexed | fp | The `add_*`/`register_*` one-liners are the documented plugin API over module state — naming the operation is the depth. The nine `TypeSpec` forwarders are Decorator-pattern delegation: `RangeTypeSpec` wraps a base spec and must pass through what it does not restrict. |
| **#14 — 20 findings**, one per distinct clump: check_update `(ctx,new,old)`x35, `(ctx,new,newts,old,oldts)`x12, `(ctx,newmod,olds)`x8; plugin `(ctx,fd,modules)`x28; dsdl `(p_elem,pset,stmt)`x23; types `(errors,module,pos)`x17 and `(errors,errstr,pos,val)`x15; jstree `(ctx,fd,s)`x9; sample-xml `(module,node,path)`x8; uml `(fd,node,parent)`x6; omni `(ctx,fd,root)`x6; schemanode `(interleave,name,parent)`x5; tree `(fd,llen,path)`x4; yang `(fd,indent,indentstep)`x4 and `(fd,indent,keywordstr,line_len,max_line_len)`x3; statements `(children,identifier,modulename)`x4, `(ctx,no_error_report,stmt)`x3, `(arg,keyword,parent,pos,top)`x3; xpath `(ctx,initial,mod,node,pos)`x3; test yangxml `(fd,ind,stmt)`x6 | #14 | indexed | real | Textbook Fowler clumps. The xpath one is threaded verbatim 22 times through three signatures; the dsdl one through 23; `(ctx, fd, modules)` is the emit contract of 28 plugin methods and has no name. |
| **#14 — 25 findings**, nested subsets/supersets of a clump already reported: jstree x9 further variants, types x6, omni x5, sample-xml x1, check_update x1, tree x1, yang x1, test yangxml x2 | #14 | indexed | fp | Same clump re-counted at every subset size — `(ctx,fd,path)`, `(fd,module,path)`, `(ctx,fd,modules,path)`, `(fd,module,path,prefix)` are all slices of jstree's one `print_node(s, module, fd, prefix, path, ctx, level)` signature. One concept, one finding. |
| **#15 — all 114 findings** (check_update x48, statements x15, dsdl x9, types x6, identifiers x6, jsonxsl/uml/threegpp/yangxml x3 each, ...) | #15 | indexed+heuristic | fp | Every flagged param is one of the domain's two universal types: `ctx` (24) or a `Statement`/`SchemaNode` AST node (90, under the names `stmt`, `new`, `old`, `node`, `newmod`, `s`, `module`, `newp`, `refinement`, `type_`, `oldmod`, `mod`, `dstmt`, `action`, `default`, `p_elem`). Verified the families: every `identifiers.add_*` and `threegpp.v_chk_*` is registered through `statements.add_validation_fun`, so `(ctx, s)` is contract-forced; every `check_update.chk_*` is a `chk_type_func` table member. And `.arg`/`.pos` are not two incidental attributes of a Statement — they are its identity. Narrowing here fragments the convention without demanding less. |
| capability.py:65, lint.py:281, sid.py:900 (x3) | #16 | heuristic | real | `emit_capability` builds a capability URI across 33 pure statements and ends in a single `fd.write(s + '\n')`; splitting out `capability_uri(ctx, m) -> str` makes it testable, which today it is not. |
| dsdl.py:706, uml.py:1102, sample-xml-skeleton.py:183, jsonxsl.py:140, :117 (x5) | #16 | heuristic | fp | No pure head exists: these build XML/UML by mutation from the first statement. `process_annotation`'s "pure" prefix is `tmpl = self.xsl_template(...)`, which creates a SubElement in the output tree. |
| plugin.py:20, context.py:28, json2xml.py:415, sid.py:300, yin_parser.py:94, yang2html.py:38, syntax.py:238 (x7) | #17 | heuristic | real | Each neck falls on a real seam in a long function — `plugin.init`'s at the boundary between built-in plugin init and the plugin-directory scan. |
| check_update.py:74, lint.py:136, structure.py:30, identifiers.py:27 (x4) | #17 | heuristic | fp | Necks inside registration runs. `structure.pyang_plugin_init` is 13 lines of independent `register_*`/`add_*` calls: every line is a "neck" and none is a split point. |
| yin_parser.py:243 | #18 | heuristic | real | `look_ahead` numbers its phases (`# 1. find our own namespace URI`, ...) inside one method. |
| uml.py:537; statements.py:2121 (x2), :2140 (x2), :20; plugin.py:59; grammar.py:809; jsonxsl.py:190; util.py:153; tree.py:301 (x11) | #19 | heuristic | real | All name a list used for membership or removal inside a loop over another collection. Two sites are double-reported (2121 and 2140 each appear once for `v_reference_list` and once for its nested `v_unique`), which is redundancy, not error. `util.search_data_node`'s `skip` list is only four elements but is scanned per child on every data-node walk. |
| sid.py:625 (x3); statements.py:179, :186, :191, :212, :216, :231, :234, :249 (x9) | #20 | heuristic | real | Literally duplicated lambda bodies: `lambda item: item['sid']` three times in sid; the `_validation_map` forwarders repeat identical text for keywords that share a handler — and the right fix is not to name them but to delete them and store the function. |
| sid.py:292 (`self.content['item']`, 8 methods); context.py:12 (x4: `mod not in self.revs`, `self.revs[mod]`, `(modulename, revision) in self.modules`, `self.modules[modulename, revision]`); sid.py:292 (`statement.keyword in self.leaf_keywords`); yang_parser.py:12 (`len(self.buf)`); uml.py:155 (`fd.write(self.full_path(node) + '\n')`) (x8) | #21 | heuristic | real | Raw index/membership expressions on instance state, repeated across methods — `self.content['item']` in eight `SidFile` methods is the rule's `CaseInsensitiveMap` shape exactly. |
| 18 findings: dsdl.py:144 (`handle_substmts`, `process_patches`, `apply_augments`, `lookup_expand` x2, `propagate_occur`, `add_prefix`); uml.py:155 (`emit_child_stmt`, `make_plantuml_keyword`); jsonxsl.py:56 (x2); schemanode.py:19 (x2); sid.py:292 (`merge_item`); sample-xml-skeleton.py:45; context.py:12 (`_get_latest_rev(...)`); yang_parser.py:12 (`readline()`) | #21 | heuristic | fp | These are *calls to an already-extracted helper*. A method used by nine siblings is encapsulation working, not a distributed invariant. |
| **#22 — 40 findings** where the method touches no instance data at all: jsonxsl `type_param`/`process_children`/`process_module`/`process_rpc`/`process_notification`/`process_annotation`; json2xml `translate_obj`/`handle_anyxml`/`handle_leaf`; jtox `process_children`/`base_type`; dsdl `handle_substmts`/`choice_stmt`/`case_stmt`/`must_stmt`/`when_stmt`/`enum_stmt`/`unique_stmt`/`string_type`/`binary_type`/`leafref_type`/`choice_type`/`type_with_ranges`/`add_patch`/`process_patches`; uml `emit_action`/`emit_feature`/`emit_deviation`/`emit_notif`/`emit_must`/`emit_must_leaf`/`emit_module_class`/`emit_augmented_children`/`full_path`/`augment2identifier`/`lollipop_node`/`last_component`/`first_component`; sample-xml `leaf_list`; schemanode `list` | #22 | heuristic | real | Classified by AST: for these 40 the only `self.` names are method calls, never fields. `jsonxsl.type_param` reaches solely through `self.get_types` and `self.xsl_withparam`; five of the uml members are additionally dead. Meyers' candidate set precisely. |
| **#22 — 113 findings** where the method reads or writes instance fields | #22 | heuristic | fp | The detector treats plain attribute reads as "the public interface", and pyang has essentially no `_`-private attribute convention, so core methods qualify. Verified: `error.Position.label` reads `self.ref`/`self.line`/`self.uses_pos`; `Context.add_parsed_module` writes `self.modules`; `yin_parser.find_child` reads `self.children`; `sid.build_dependencies` mutates `self.content`; `repository._add_directory` appends to `self.dirs`; `hello.Capability.parse_pars` fills `self.parameters`; `Statement.search`/`search_one` read `self.substmts`; `sid.collect_module_items` reads six keyword sets off `self`. A method that manipulates its object's data is not a free function. |
| **#23 — 128 findings with cognitive complexity >= 17** | #23 | heuristic | real | Spot-read 12: `uml.emit_child_stmt` (315), `statements.v_reference_deviate` (238), `pyang_tool.run` (231), `xpath.chk_xpath_path` (229) and `chk_xpath_expr` (200), `statements.validate_leafref_path` (169), `v_type_type` (154), `jstree.get_nontypedefstring` (39), `sid.get_path` (22, nested `while`s under an eight-clause condition annotated with case (A)/(B) comments), `grammar.add_to_stmts_rules` (17, triple-nested insertion sort turning on `if i == len(rules0)` after the loop), `check_update.chk_ordered_by` (17), `context.validate` (17). Weakest member of the band is `tree.get_flags_str` (22), a flat return-dispatch the metric over-scores. |
| **#23 — 21 findings with complexity 15-16** | #23 | heuristic | fp | Named spot-checks, all short and flat: `name.emit_name` (15, 16 linear lines), `restconf.v_yang_data` (15), `statements.iterate_i_children` (15, a tidy 17-line walker), `context._ensure_revs` (15), `types.parse_dot_dot` (15), `json2xml.handle_anyxml` (15), `statements.v_reference_action` (15), `yang.emit_path_arg` (15), `jtox.base_type` (15), `statements.set_is_key` (15). The threshold sits one notch below the reviewer bar; `threegpp.post_validate_ctx` (16) and `statements.mk_path_str` (16) are the band's arguable members. |
| plugin.py:66, statements.py:3081, :3082, util.py:10, transforms/edit.py:113 (x5) | #24 | heuristic | real | All five hide a name from grep: `__import__(fname[:-3])`; `getattr(cls,'__slots__')` + `delattr` keyed on an `i_` prefix; `getattr(x, attr)` with a caller-supplied field name; and `getattr(ctx.opts, ('%s-%s' % (plugin_name,key)).replace('-','_'))` — no `--edit-*` option's `dest` is findable from the code that reads it. |
| schemanode.py:192, check_update.py:168, uml.py:1106, :1154, :1164, statements.py:3187, :3461, transforms/edit.py:106, dsdl.py:1254, yang_parser.py:155 (x10) | #25 | indexed | real | `get_xpath`->`mk_path_str`, `transform`->`edit_tree`, `skip_tok`->`set_buf`, `last_component`->`make_plantuml_keyword`: the concept is renamed mid-delegation, so the chain cannot be followed by grepping one word. |
| error.py:20, plugins/threegpp.py:215 (x2) | #25 | indexed | fp | `Position.__str__` is a dunder — it cannot share a stem with anything. `v_chk_not_allowed_statements`->`error.err_add` is a call to the repo-wide error API, not a delegation hop. |
| statements.py:3094, :3086, :37, :3134, :2986; error.py:565, :545, :551, :35, :63, :577, :50; grammar.py:108; dsdl.py:132 (x14, fan-in >= 7) | #27 | indexed | real | `err_add` (fan-in 351) forces every reader into a 581-line module that is 480 lines of error-message table; `Statement.search_one` (fan-in 208) and `search` (92) sit in the 3557-line `statements.py` alongside the validation phases, the Statement class family, and the path helpers, which share no state with each other. |
| 22 findings with fan-in 3-6 (statements.py x11, grammar.py x6, types.py x3, error.py x2) | #27 | indexed | fp | "Hot symbol" is the premise. `is_warning` (3), `Eof` (4), `data_def_stmts` (3), `add_stmt` (4) are not hot; three readers do not make a purchase-price problem. (`Statement` itself is measured at fan-in 3, which understates it — the metric counts import edges, not use.) |
| statements.py:1, pyang_tool.py:1, error.py:1, json2xml.py:1, yin_parser.py:1, xpath.py:1, omni.py:1, schemanode.py:1, yang2html.py:1, util.py:1 (x10) | #29 | heuristic | real | All ten genuinely open with an import and no docstring. `statements.py` is 3557 lines and 120 top-level defs with nothing on the first screen saying what a validation phase is. The detector correctly skipped `dsdl.py`, whose docstring follows a licence comment. |
| 30 findings on entry points >= 100 lines or named `run`/`main`/`emit*`: `pyang_tool.run` (547), `statements.v_type_type` (212), `v_reference_deviate` (187), `v_expand_1_uses` (168), `yang2html.run` (162), `tree.emit_tree` (153), `flatten.add_opts` (153), `jstree.print_node` (140), `check_update.add_opts` (140), ... | #29 | heuristic | real | A 547-line CLI entry point and a 212-line validation pass both need a first paragraph saying what they cost. |
| 64 findings on 30-99-line internal functions (`statements.v_type_grouping` 31, `check_update.chk_identity` 30, `yin_parser.start_element` 30, `ietf.setup_ctx` 31, `tree.print_path` 31, ...) | #29 | heuristic | fp | These are not entry points, and requiring a docstring on every 30-line internal function is the doc-*presence* scoring the rule's own grounding (the factorial null result) rules out. |
| tree.py:494, :519; jstree.py:527, :503; omni.py:146, :276; xpath.py:219; statements.py:777, :1425, :1710, :1069, :1014, :1116, :1301; yang_parser.py:159; types.py:549, :386; translators/yang.py:390, :364; dsdl.py:822; context.py:149; check_update.py:805; test yangxml.py:112 (x22) | #33 | heuristic | real | Confirmed by AST that each has a value return plus a silent exit — a bare `return` or a reachable fall-off-the-end. `tree.get_status_str` returns `'+'`/`'x'`/`'o'` and `None` for any other status; `jstree.get_status_str` returns a string on one branch and a `Statement` on the other; `jstree.typestring` returns a string everywhere except one bare `return`; `Context.get_module` has three exit shapes, one of them silent. |
| 53 findings whose functions return a value plus an *explicit* `None` (`statements.search_one`, `search_child`, `search_typedef`, `util.attrsearch`/`keysearch`/`dictsearch`, `types.*.str_to_val`, `yin_parser.find_child`, `context.add_module`, ...) | #33 | heuristic | fp | The message says "bare returns"; the AST shows zero bare returns in all 53 — they are `Optional[T]` lookups, the canonical Python contract for "the thing or nothing". `attrsearch` is `for x in in_list: ... return x` / `return None`. Nothing here breaks a contract. |
| omni.py:280; context.py:273, :278, :282; uml.py:843, :639; repository.py:96; yin_parser.py:130 (x8) | #34 | heuristic | real | Six genuine commented-out code runs (omni's is 12 lines of a dead type renderer) and two bare `except:` blocks whose handlers only `pass`. |
| plugin.py:19 (x3: dsdl, yang, yin) | #35 | indexed | real | The function-local `from .translators import yang,yin,dsdl` hides a real cycle — all three translators import `pyang.plugin` at module level. |
| statements.py:3094, :3086 (`children`); transforms/edit.py:313 (`index`); util.py:8 (`attr`); schemanode.py:106 (`parent`) (x5) | #37 | indexed | real | Verified by grep: no call site anywhere passes `children=` to `Statement.search`/`search_one` across 263 and 122 calls, and `attrsearch` receives the literal `'arg'` at all three. Flexibility nobody exercises. |
| context.py:236, :314; threegpp.py:306; metadata.py:17; restconf.py:23; smi.py:41; structure.py:23; uml.py:892 (x8) | #39 | heuristic | real | `# get the latest revision` above `_get_latest_rev(...)`; `# remove error from ctx` above `ctx.errors.remove(ctx_error)`; four copies of `# Register the plugin` above `plugin.register_plugin(...)`. uml.py:892 is a commented-out line — restatement in its purest form. |
| error.py:56; syntax.py:215, :217 (x3) | #39 | heuristic | fp | `### error codes` is a section banner introducing the genuinely informative severity legend beneath it. The syntax.py lines are the RFC 7950 `if-feature-expr` ABNF, which the hand-written recursive-descent parser below does not restate. |
| uml.py:565, :568, :1014; context.py:66; repository.py:27; sid.py:3; statements.py:110, :2984 (x8) | #39 | heuristic | fp | None of the eight narrates edit history; the arm is matching the words "used to" and "previously". `"""used to abort an iteration"""` is a docstring; `# matches previously found key statement` describes runtime state. The arm also missed the two real cases in the repo (`types.py:155 "No, do not ignore trailing zeroes!"`, `uml.py:394/:999 "previous code skipped first char"`). |
| statements.py:3461; structure.py:58; omni.py:276; statements.py:3547, :3493, :1905 (x6) | #39 | heuristic | real | `get_xpath` carries 23 prose lines over a 7-line forwarding body that restates `mk_path_str`'s own docstring; `v_expand_2_augment_sx` carries a 16-line algorithm description copied from the callee onto one forwarding line; `omni.get_typename`'s "prose" is 8 lines of commented-out code. |
| 32 findings on documented public/extension API and abstract hooks: plugin.py x10 (`add_output_format`, `add_transform`, `emit`, `setup_fmt`, `setup_xform`, `add_opts`, `setup_ctx`, `post_validate`, `post_validate_ctx`, `register_plugin`); `error.add_error_code`; `grammar.register_extension_module`/`chk_module_statements`; `statements.add_validation_fun`/`add_validation_var`/`set_phase_i_children`/`get_description`; `syntax.add_arg_type`; `lint.LintPlugin.__init__`; jsonxsl x5; dsdl x5; schemanode x2; `hello.registered_capabilities`; `check_update._max_elements_val`; test `create_context` | #39 | heuristic | fp | For an abstract plugin base whose bodies are `return`, the docstring *is* the contract — that is where prose is supposed to outweigh code. `LintPlugin.__init__`'s comments tell a subclass author what to override, which the four empty-list assignments cannot. |
| **#32 — 95 findings, referenced after all**: xpath_parser.py 72 `p_*` functions + `tokens` (:355); xpath_lexer.py `XPathLexer.input` (:24) and `token` (:32); statements.py:53, :59, :70, :76, :79, :82, :85, :93 (the `### Functions that plugins can use` block); statements.py:515, :520, :523, :1052 and smi.py:113 (`ctx` in `_validation_map` callbacks); check_update.py:797 (x2), :815 (x2), :832 (`chk_type_func` members that use their other params); transforms/edit.py:20 (`option` in an optparse TYPE_CHECKER); util.py:3 (`int_types`) | #32 | indexed | fp | `p_*`, `tokens`, `input` and `token` are bound by `yacc.yacc()` reflection. The `statements.py` block sits under a header declaring it the plugin API, and pyang's plugins load out-of-tree. The callback `ctx` params are forced by `f(ctx, stmt)`. And `int_types` *is* referenced — as `util.int_types` from `types.py:203` and `sid.py:540`; the qualified reference was missed. |
| **#32 — 86 findings, genuinely dead**: omni.py x31 (incl. `print_omni_header`, whose whole body computes a `name` it discards, and `print_association`, which uses 2 of 7 params); check_update.py x23 (the five params each of the `return`-only stubs `chk_string`, `chk_binary`, `chk_instance_identifier`, `chk_dummy`, plus `chk_range`'s `oldts`/`newts` shadowed by locally re-derived `ots`/`nts`, plus `import sys`); jstree.py x5 (`ctx` in five emitters with already-differing signatures); uml.py x5 (`lollipop_node`, `augment2identifier`, `last_component`, `next_tolast_component`, `first_component` — verified: `lollipop_node`'s and `first_component`'s only call sites are commented out); setup.py x3; dsdl.py x2 (`empty_type`, `mapped_type`); pyang_tool.py x2; syntax.py x2; statements.py x3 (`re_and_or`, `iterate_i_children`, `get_type`); edit.py x2; `util.dictsearch`; `context.del_module` (only mention is inside a comment); `sid.sid_used`; `structure.py import pyang`; jsonxsl/sample-xml `import sys`; `__main__.py import re`; `xpath_parser.pparse` | #32 | indexed | real | Grepped every named symbol across `pyang/` and `test/`: zero references outside the definition (or, for `del_module`/`lollipop_node`/`first_component`, only inside commented-out code). |
| plugins/sid.py:1055 | #40 | heuristic | fp | `number_of_unassigned_yang_items` has "number" as its head noun; returning an `int` is exactly what the name promises. The trailing plural token was matched, not the name's shape. |
| uml.py:495, :422, :857, :630, :734 (x5) | #30 | heuristic | real | `self._ctx.opts.uml_inline` and friends reach three deep through a stored context — and `uml_emitter.__init__` already copies most options into `self.ctx_*` fields, so the reach is inconsistent with the class's own convention. |
| statements.py:2741, :1804, :2630, :2229 (x4) | #30 | heuristic | fp | `stmt.parent.parent.i_children` and `path.parent.parent.keyword` are parent-chain walks on a tree. Demeter does not apply to the node type whose job is to expose its neighbours. |

### Verdict totals by rule

| rule | n | real | fp | rule | n | real | fp |
|---|---|---|---|---|---|---|---|
| #1 | 2 | 2 | 0 | #21 | 26 | 8 | 18 |
| #2 | 3 | 0 | 3 | #22 | 153 | 40 | 113 |
| #3 | 1 | 1 | 0 | #23 | 149 | 128 | 21 |
| #4 | 8 | 0 | 8 | #24 | 5 | 5 | 0 |
| #5 | 5 | 1 | 4 | #25 | 12 | 10 | 2 |
| #6 | 21 | 16 | 5 | #27 | 36 | 14 | 22 |
| #7 | 1 | 1 | 0 | #29 | 104 | 40 | 64 |
| #9 | 1 | 1 | 0 | #30 | 9 | 5 | 4 |
| #11 | 281 | 177 | 104 | #32 | 181 | 86 | 95 |
| #12 | 5 | 5 | 0 | #33 | 75 | 22 | 53 |
| #13 | 27 | 11 | 16 | #34 | 8 | 8 | 0 |
| #14 | 45 | 20 | 25 | #35 | 3 | 3 | 0 |
| #15 | 114 | 0 | 114 | #37 | 5 | 5 | 0 |
| #16 | 8 | 3 | 5 | #39 | 57 | 14 | 43 |
| #17 | 11 | 7 | 4 | #40 | 1 | 0 | 1 |
| #18 | 1 | 1 | 0 | | | | |
| #19 | 11 | 11 | 0 | **total** | **1378** | **654** | **724** |
| #20 | 9 | 9 | 0 | | | | |

## Phase 3 — reconciliation

Of 100 phase-1 sites: **33 covered, 67 misses** — 44 detector-miss, 13
threshold-miss, 10 inventory-gap. The 10 inventory-gaps are exactly the 10 rows
I mapped `none` in phase 1, so the inventory prediction held with no leakage in
either direction.

| P1 id | rule | class (covered / detector-miss / threshold-miss / inventory-gap) | note |
|-------|------|------------------------------------------------------------------|------|
| P1-1 | #9 | detector-miss | `plugin.plugins` is appended by `register_plugin` from ~25 plugin modules and read from `pyang_tool` at six sites; #9 fired only on `error.error_codes`. Cross-module writers reached through a setter function are invisible to it. |
| P1-2 | #24 | covered | #24 @ plugin.py:66. |
| P1-3 | #34 | threshold-miss | The handler prints then re-raises a *different* exception, so it misses the "pass/print only" shape that fired at repository.py:96 and yin_parser.py:130. |
| P1-4 | #9 | covered | #9 @ error.py:63 (undercounts the writers, names the site). |
| P1-5 | #41 | detector-miss | Family P produced zero findings for this repo. `err_add`'s O(n^2) dedupe scan is on the validation walk, the one path pyang spends its time in. |
| P1-6 | #32 | detector-miss | `(level, fmt) = error_codes[tag]` with one half unused, twice. #32 covers symbols/params/imports; a dead unpacked local is the same liveness question and is not asked. |
| P1-7 | #9 | detector-miss | `util.files_read` is a module-level dict, never cleared, that changes verbose output. Missed for the same reason as P1-1 — the writer is a function, not a direct assignment from another module. |
| P1-8 | #12 | detector-miss | Catalog gap: no `str.partition` / `split(sep, 1)` entry. |
| P1-9 | #11 | detector-miss | The cross-module trio (`util.split_identifier`, `types.py:253`, `pyang_tool.parse_features_string`) was not grouped; the two #11 findings near util.py:39 are the unrelated `keysearch`/`dictsearch` pair. |
| P1-10 | #12 | detector-miss | Catalog gap: no `lstrip` / leading-whitespace-scan entry. |
| P1-11 | #11 | covered | #11 @ context.py:44, :55 — group named as `Context.__init__`, `Context.internal_reset`. |
| P1-12 | #33 | covered | #33 @ context.py:149. |
| P1-13 | #34 | threshold-miss | `except ReadError as ex: i += 1; continue` swallows silently, but the handler is not `pass`/`print`, so it falls outside the arm that fired twice elsewhere. |
| P1-14 | #34 | covered | #34 @ context.py:273, :278, :282 (three runs of the one block). |
| P1-15 | none | inventory-gap | `Context.yin_module_map` is written at context.py:255 and initialised nowhere in `__init__` — created ad hoc by `pyang_tool.py:385` and a `hasattr` guard in `yin_parser`. No rule covers an attribute the class never declares. |
| P1-16 | #12 | detector-miss | `for k in d: v = d[k]` is not in the catalog, though the sibling keys-membership entry is; the same shape recurs five times across context.py and pyang_tool.py. |
| P1-17 | #34 | covered | #34 @ repository.py:96 (the inner of the two nested bare excepts). |
| P1-18 | none | inventory-gap | `while use_env:` as a one-shot goto target. No rule covers control-flow-as-goto. |
| P1-19 | #32 | detector-miss | `_setup(self, ctx)` never reads `ctx`; #32 reported 68 unused params but not this one, presumably because the caller `get_modules_and_revisions(self, ctx)` is an ABC override and the exemption propagated to the private callee. |
| P1-20 | #12 | detector-miss | Catalog gap: no "manual open/finally instead of `with`" entry. |
| P1-21 | #33 | threshold-miss | The two abstract methods have docstring-only bodies, so there are no returns to "mix"; the rule needs a value return to fire. |
| P1-22 | #32 | detector-miss | `_add_directory`'s `True`/`False` is discarded at all six call sites. Dead *return values* are outside what the liveness pass asks about. |
| P1-23 | none | inventory-gap | `pkgutil.find_loader` was removed in Python 3.14 and is called from `FileRepository.__init__`. No rule covers removed-stdlib-API use. |
| P1-24 | #18 | detector-miss | `run()` narrates eight labelled phases (`# check for --plugindir`, `# Parse hello if present`, `# apply deviations`, ...). #18 fired once, on `yin_parser.look_ahead`, which uses explicit `# 1.`/`# 2.` numbering — the arm appears to require literal step numbers, and the 547-line function that most needs the finding does not use them. |
| P1-25 | #32 | covered | #32 @ pyang_tool.py:8, :18. |
| P1-26 | none | inventory-gap | `--plugindir` parsed twice, once by hand from `sys.argv` and once by optparse (whose `dest` is then never read). No rule covers one flag with two parsers. |
| P1-27 | #41 | detector-miss | `ctx_validate_and_prune()` inside `for module in modules:` re-validates the whole context once per module. Family P silent. |
| P1-28 | none | inventory-gap | `-O/--overwrite` changes the mechanism, never the documented behaviour. No rule covers help text that does not match effect. |
| P1-29 | #29 | covered | #29 @ statements.py:1. |
| P1-30 | #27 | covered | #27 fires 16 times inside statements.py naming exactly the hot-symbol-in-a-huge-container claim (`search_one` fan-in 208, `search` 92). |
| P1-31 | #13 | threshold-miss | #13 does not consider lambdas, so the ~60 forwarding entries in `_validation_map` are invisible to it; #20 caught the 15 that happen to be *duplicated* (statements.py:179, :186, :191, :212, :216, :231, :234, :249) and none of the rest. The rule fired nearly; the unique-target forwarders fall under the duplication cutoff. |
| P1-32 | #34 | detector-miss | Five commented-out `_validation_map` entries inside the live dict literal. #34's commented-code arm fired six times elsewhere but not on comment lines inside a collection literal. |
| P1-33 | #12 | detector-miss | Catalog gap: no dict-as-set entry, though the shape recurs five times in statements.py alone. |
| P1-34 | #9 | detector-miss | Nine module-level mutables mutated cross-module through the `add_*` API at statements.py:13-101, written by lint/ieee/smi/metadata at `setup_ctx` time. Same setter-indirection blind spot as P1-1 and P1-7. |
| P1-35 | #13 | covered | #13 @ statements.py:82. |
| P1-36 | #12 | detector-miss | Hand-rolled `idx = 0 ... idx = idx + 1` cursor; #19 fires on the same function for the linear op, #12 has no `enumerate` entry. |
| P1-37 | #11 | covered | #11 @ statements.py:374, :382, :389 — the three `res = f(ctx, stmt)` blocks inside `iterate`. |
| P1-38 | none | inventory-gap | `if res == 'continue': pass else: <30 lines>`. No rule covers an empty guard arm. |
| P1-39 | none | inventory-gap | `__slots__` ending in `'__dict__'`, with `self.stmt_parent` assigned but absent from every slot list. No rule covers a neutered `__slots__`. |
| P1-40 | #24 | covered | #24 @ statements.py:3081, :3082. |
| P1-41 | #32 | detector-miss | `Statement.pprint` and `print_tree` have no caller but themselves. Self-recursion is counted as a reference, so a recursive dead function stays invisible — the same miss as P1-64. |
| P1-42 | #25 | covered | #25 @ statements.py:3461. |
| P1-43 | #39 | covered | #39 @ statements.py:3461 (prose-outweighs, 23 vs 7). |
| P1-44 | #39 | covered | #39 @ statements.py:3493 lands on `get_type`, the function whose `# Return type value if exists` I named — though it arrives via the prose-ratio arm, not the restatement arm. |
| P1-45 | #12 | covered | #12 @ statements.py:3550 (bool-ternary). |
| P1-46 | #2 | detector-miss | `if type_obj:` truth-tests a `Statement` with no `__bool__`/`__len__`, so it can only mean `is not None`. The oracle has the class in hand; the three #2 findings it did produce are all false, so the arm is firing on inference artifacts and missing the provable case. |
| P1-47 | #9 | detector-miss | `grammar.extension_modules`, appended by `register_extension_module` from the structure/ieee plugins and read on the grammar hot path. |
| P1-48 | #13 | detector-miss | `chk_module_statements` forwards one call to `chk_statement` with the constant `top_stmts` substituted; #13 fired on the sibling `register_extension_module` seven lines above but not here — a substituted constant appears to defeat the forward test. |
| P1-49 | #19 | covered | #19 @ grammar.py:806 and :809. |
| P1-50 | #19 | threshold-miss | `[stmt for stmt in stmts if stmt not in res]` is the same quadratic shape in a comprehension rather than a `for` body. |
| P1-51 | #39 | detector-miss | `# occurane == '*'` restates the condition the elif chain established. Not matched. |
| P1-52 | #32 | covered | #32 @ syntax.py:3, :6. |
| P1-53 | #20 | threshold-miss | The 19 `lambda s: re_X.search(s) is not None` bodies differ by one identifier, so exact-body matching sees 19 unique lambdas rather than one shape repeated 19 times. |
| P1-54 | none | inventory-gap | `yin_map`'s docstring describes the pair components in the wrong order. No rule covers a docstring contradicting the literal beside it. |
| P1-55 | #34 | detector-miss | `# slen = len(s.rstrip('0')) # ignore trailing zeroes` plus `# No, do not ignore trailing zeroes!`. A single commented-out line falls under the run length the arm needs — the six runs it did find are all >= 3 lines. |
| P1-56 | #34 | threshold-miss | Bare `except:` around `b64decode`, but the handler calls `err_add`, so it is not "pass/print only". |
| P1-57 | #19 | detector-miss | `if val not in visited` inside the recursive identity walk. The membership target is a parameter rather than a local, which the other ten findings all are. |
| P1-58 | #11 | threshold-miss | Three one-statement `class Abort(Exception)` declarations — below the clone size floor (the smallest block group found is 3 statements). |
| P1-59 | #14 | covered | #14 @ xpath.py:95, `(ctx, initial, mod, node, pos)` x3. |
| P1-60 | #11 | threshold-miss | The `'comp'`/`'arith'`/`'bool'` arms are two statements each — under the 3-statement floor. |
| P1-61 | #33 | covered | #33 @ xpath.py:157. |
| P1-62 | #38 | threshold-miss | As predicted in phase 1: `'deref'` is duplicated within one module, and #38 needs the same literal at module level in >= 3 modules. #38 fired zero times repo-wide. |
| P1-63 | #35 | covered | #35 @ plugin.py:19 x3 (dsdl, yang, yin). |
| P1-64 | #32 | detector-miss | `pp`/`ppkeywd` under `# FIXME: tmp debug` reference only each other. Mutual recursion counts as liveness — same miss as P1-41. |
| P1-65 | #38 | threshold-miss | `NC_NS_URI` restated two lines later as a `CAPABILITIES` key: intra-module, under the >= 3-module cutoff. |
| P1-66 | #12 | covered | #12 @ hello.py:84 (keys-membership). |
| P1-67 | #12 | detector-miss | `set(dict(self.yang_modules()).keys())` — a three-step round trip back to keys the callee already had. Catalog gap. |
| P1-68 | #12 | detector-miss | `[c for c in ...][0]` instead of `next(...)`. Catalog gap. |
| P1-69 | #37 | detector-miss | `debug=0` is passed explicitly at its one call site, has no CLI option, and gates a dead branch at dsdl.py:919. #37's monomorphic-param arm caught `attrsearch`'s `attr='arg'` but not this. |
| P1-70 | #11 | threshold-miss | The emit-precheck block is copied into eight modules but is a 3-node nested `for`/`if`/`raise`, which the block-clone floor counts as under-size. |
| P1-71 | #24 | covered | #24 @ transforms/edit.py:113. |
| P1-72 | #34 | detector-miss | `del stmt` on a local in a frame about to be discarded — a no-op wearing the look of a free. |
| P1-73 | #12 | detector-miss | `if x in lst: i = lst.index(x); del lst[i]` instead of `lst.remove(x)`. Catalog gap. |
| P1-74 | #32 | covered | #32 @ transforms/edit.py:163, :170. (The audit also flagged `check_date`'s `option` at :20, which I judge fp — optparse fixes that signature; my phase-1 row over-reached in naming it.) |
| P1-75 | #34 | detector-miss | A string literal placed *after* an early `return`, so it is a no-op expression, not a docstring. Not in the arm's shapes. |
| P1-76 | #12 | detector-miss | A ten-way `==` or-chain that is a set membership written longhand. Catalog gap. |
| P1-77 | #19 | detector-miss | `ctx.errors.remove(ctx_error)` inside `for ctx_error in ctx.errors[:]`. The iteration target is a slice copy of the mutated list, which appears to break the "same collection" test. |
| P1-78 | #11 | covered | #11 @ check_update.py:509, :510 (`chk_must`/`chk_when`). |
| P1-79 | #19 | detector-miss | `newmust.remove(newm)` / `oldmust.remove(oldm)` inside the loop at :513. Same `.remove`-in-loop shape as P1-77 and P1-100, none of the three found. |
| P1-80 | #32 | detector-miss | `element_htmnl_info = ""` assigned and never read. Dead locals are outside the pass. |
| P1-81 | #32 | detector-miss | `found` set `True` in seven branches and never read. Dead local. |
| P1-82 | #32 | detector-miss | `type_namespace` / `i_type_name` assigned and never read. Dead locals. |
| P1-83 | #33 | covered | #33 @ jstree.py:527 — the finding names `typestring`, whose one bare `return` at :594 is my site. |
| P1-84 | #33 | covered | #33 @ jstree.py:503. |
| P1-85 | none | inventory-gap | `from pyang.plugins.html import force_link` imports a module absent from the tree, twice. No rule covers it. |
| P1-86 | #33 | covered | #33 @ tree.py:494. |
| P1-87 | #33 | covered | #33 @ tree.py:519. |
| P1-88 | #11 | covered | #11 @ mef.py:19 — group `BBFPlugin.add_opts`, `IEEEPlugin.add_opts`, `MEFPlugin.add_opts` +1. |
| P1-89 | #13 | detector-miss | `IEEEPlugin.__init__` does nothing but call the base `__init__`. A super-only override is not in #13's forward test. |
| P1-90 | #13 | detector-miss | Same lambda blind spot as P1-31: lint.py:103-116's `lambda ctx, s: v_chk_default(ctx, s)` forwarders are invisible to #13, and unlike statements.py's they are not duplicated, so #20 does not see them either. |
| P1-91 | #34 | covered | #34 @ uml.py:639 and :843 — two of the runs in the concentration I named. |
| P1-92 | #39 | detector-miss | `# FIXME: previous code skipped first char, possibly in error`, duplicated at :394 and :999, is the repo's clearest history narration and the history arm did not match it — while matching eight comments that narrate no history at all. |
| P1-93 | #11 | covered | #11 x5 @ uml.py:954 (`attribs`/`typestring` groups). |
| P1-94 | none | inventory-gap | `# FIXME: what is this??` — a comment recording the author's own incomprehension. No rule covers it. |
| P1-95 | #28 | detector-miss | `get_type`'s docstring names `get_prefixed_type`, which exists nowhere in the repo. #28 fired zero times on this repo, so no docstring symbol was resolved at all. |
| P1-96 | #24 | threshold-miss | `getattr(module, "i_children", [])` uses a literal name with a silent default, so it is under the "constructed name" bar the five #24 findings all clear. My phase-1 mapping was the loose one here. |
| P1-97 | #12 | detector-miss | The `attrsearch`/`keysearch`/`dictsearch` linear-find trio. #11 grouped two of them as clones and #37 caught `attr`'s monomorphism, but the `next(...)` idiom claim was not made — catalog gap. |
| P1-98 | #24 | covered | #24 @ util.py:10. |
| P1-99 | #8 | detector-miss | Error severity as a bare int decoded by `level < 4`, `level > 2`, `not is_error(level)` and an open-coded `(4, wstr)` write. #8 fired zero times repo-wide. |
| P1-100 | #19 | detector-miss | `yam.i_children.remove(ch)` inside the loop at :100. Third instance of the unfound `.remove`-in-loop shape (with P1-77, P1-79). |

### Miss classes, grouped

| cluster | P1 ids | class |
|---|---|---|
| #12 catalog gaps (partition, lstrip-scan, `with`, dict-as-set, enumerate, keys-iteration, dict round-trip, `[..][0]`, `in`+`.index`, or-chain-as-set, linear-find) | 8, 10, 16, 20, 33, 36, 67, 68, 73, 76, 97 | detector-miss x11 |
| #9 misses every module global whose writers go through a setter function | 1, 7, 34, 47 | detector-miss x4 |
| #32 does not ask about dead locals, dead return values, or self/mutually recursive dead functions | 6, 22, 41, 64, 80, 81, 82 | detector-miss x7 |
| #19 does not fire on `.remove` inside a loop | 77, 79, 100 | detector-miss x3 |
| #13 does not consider lambda forwarders or super-only overrides | 31 (threshold), 48, 89, 90 | detector-miss x3 + threshold x1 |
| #11 size floor (< 3 statements) | 58, 60, 70 | threshold-miss x3 |
| #38 cross-module cutoff vs intra-module duplication | 62, 65 | threshold-miss x2 |
| rules that fired zero times repo-wide (#8, #28, #38, #41) | 5, 27, 62, 65, 95, 99 | 4 detector-miss + 2 threshold-miss |
| my `none` mappings, all confirmed uncovered | 15, 18, 23, 26, 28, 38, 39, 54, 85, 94 | inventory-gap x10 |
