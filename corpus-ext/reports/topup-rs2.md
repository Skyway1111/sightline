| repo | rule | fp class | count | example key |
|---|---|---|---|---|
| doxx | rs:59 | in-process thread hop (`spawn_blocking`) counted as an off-machine spend, on a `fn main` that has no caller to inform | 1 | `src/main.rs:111:59:cost-docstring:doxx::main::main` |
| turmoil | rs:9 | `RefCell::borrow_mut()` on a `thread_local!` counted as a write to the static (8 of the 10 only borrow to reach into the value; 2 rebind the slot, under an RAII guard) | 1 | `crates/turmoil-net/src/lib.rs:48:9:local-writers:turmoil_net::CURRENT` |
| turmoil | rs:48 | one named half of a pair or family whose sibling stays, so folding makes the surviving call sites asymmetric | 3 | `crates/turmoil/src/host.rs:454:48:fold:turmoil::host::Tcp::is_port_assigned` |
| turmoil | rs:48 | rustdoc intra-doc link (``[`abort_connection`]``) not counted as another reference | 1 | `crates/turmoil-net/src/kernel/tcp.rs:724:48:fold:turmoil_net::kernel::tcp::abort_connection` |
| turmoil | rs:48 | canonical constructor (`new`) carrying a default or a type invariant that folding moves into the caller | 2 | `crates/turmoil-net/src/shim/tokio/net/tcp/stream.rs:235:48:fold:turmoil_net::shim::tokio::net::tcp::stream::FdGuard::new` |
| turmoil | rs:48 | folding reaches through the type's encapsulation: a private tuple field, or a memory ordering the type owns | 2 | `crates/turmoil/src/net/tcp/stream.rs:521:48:fold:turmoil::net::tcp::stream::FlowControl::has_credits` |
| turmoil | rs:48 | inlined body is materially harder to read at the call site (needs parens inside a compound condition, repeats the receiver, buries a non-obvious case) | 1 | `crates/turmoil-net/src/kernel/udp.rs:219:48:fold:turmoil_net::kernel::udp::is_ipv4_broadcast` |
| turmoil | rs:53 | `# Errors` section already covers every error the body returns in prose; the rule wants the literal error type name spelled in the section | 1 | `crates/turmoil-fs/src/shim/std/fs/mod.rs:1737:53:raise-contract:undeclared:turmoil_fs::shim::std::fs::remove_dir_all:Error` |
| salvo | rs:6 | `async fn` returning `Result` named `get_*`: `.await?` at every call site is the effect contract, so nothing is buried behind a getter name | 4 | `crates/jwt-auth/src/oidc.rs:257:6:dishonest-accessor:salvo_jwt_auth::oidc::OidcDecoder::get_config` |
| salvo | rs:48 | one named half of a pair or family whose sibling stays, so folding makes the surviving call sites asymmetric | 3 | `crates/serve-static/src/dir.rs:731:48:fold:salvo_serve_static::dir::list_json` |
| salvo | rs:48 | inlined body is materially harder to read at the call site (a nested `matches!` in an `if`, a `format!` two levels deep in a call chain) | 2 | `crates/oapi-macros/src/parameter/derive.rs:257:48:fold:salvo_oapi_macros::parameter::derive::ToParameters::is_named_struct` |
| salvo | rs:48 | folding reaches through the type's encapsulation: the helper is one of the readers that own the type's `Mutex` | 1 | `crates/cache/src/lib.rs:407:48:fold:salvo_cache::Flight::entry` |
| salvo | rs:48 | the helper's body is already duplicated inline elsewhere in the file, so the fix runs the other way: absorb the copy | 1 | `crates/flash/src/cookie_store.rs:140:48:fold:salvo_flash::cookie_store::CookieStore::secure_for_request` |
| salvo | rs:44 | a hand-written `PartialEq` makes the self-comparison fallible, so the assertion is a law under test rather than a tautology | 1 | `crates/oapi/src/openapi/schema/number.rs:223:44:tautology:salvo_oapi::openapi::schema::number::tests::test_eq_is_reflexive_for_nan:223` |
