# indextree - judge report (Rust)

Repo: `../gauntlet-corpus/indextree` (workspace: `indextree` 4.9.1 + `indextree-macros` 0.1.3).
Prod tree read: `indextree/src/*.rs`, `indextree-macros/src/lib.rs`.
Test tree read: `indextree/tests/`, `indextree/examples/`, `indextree-macros/tests/`,
`#[cfg(test)]` module in `id.rs`, bare `#[test]` fns at the tail of `arena.rs`.

## Phase 1 - blind ideal sites

| id | site (path:line) | rule | claim | excerpt (<=3 lines) |
|----|------------------|------|-------|---------------------|
| P1-1 | indextree/src/id.rs:1 | #27 | 2213 lines / 77 KB holding 51 `pub fn` - the entire `NodeId` surface (accessors, 7 iterator constructors, append/prepend/insert/remove/detach families, `subtree_eq`, pretty-print). Any task touching one method pays for all of them. | `//! Node identifier and tree manipulation methods.` |
| P1-2 | indextree/src/arena.rs:1 | #27 | 1008 lines / 32 KB mixing three concerns: the public `Arena` API (25 `pub fn`), the free-list internals (`free_node`, `pop_front_free_node`), and the 110-line `validate` consistency checker. `validate` alone is a natural separate module. | `//! Arena structure and node storage.` |
| P1-3 | indextree-macros/src/lib.rs:1 | #29 | 321-line crate root with no `//!` module header at all (every module in the `indextree` crate has one). First screen is `use proc_macro2::TokenStream;`, so nothing states what the parse/stack/coalesce pipeline is. | `use proc_macro2::TokenStream;` |
| P1-4 | indextree/src/arena.rs:646 | #23 | `validate` is ~110 lines with two top-level loops, a nested child-chain walk with its own step counter, a free-list walk with its own counter, a closure, and 20 `return false` exits. Cognitive complexity is far over 15; a failure tells the caller nothing about which invariant broke (`bool`, not an enum). | `pub fn validate(&self) -> bool {` |
| P1-5 | indextree/src/debug_pretty_print.rs:181 | #23 | `IndentWriter::write_str`: a `while !s.is_empty()` loop wrapping a `match`, three nested `if`s on `line_state`, an `if let`, and an if/else expression, all mutating `self.line_state` and `self.pending_ws_only_indent_level`. Complexity around 16. | `fn write_str(&mut self, mut s: &str) -> fmt::Result {` |
| P1-6 | indextree/src/id.rs:909 | #11 | Five byte-identical guard wrappers modulo the delegate name: `checked_detach` (909), `checked_remove` (1563), `checked_remove_subtree` (1687), `checked_detach_children` (1781), `checked_remove_children` (1867). Same three-statement body each time. | `if self.is_removed(arena) { return Err(NodeError::Removed); }` |
| P1-7 | indextree/src/id.rs:1021 | #11 | Four byte-identical panicking wrappers modulo the delegate name: `append` (1021), `prepend` (1212), `insert_after` (1309), `insert_before` (1449). The panic message literal `"Preconditions not met: invalid argument"` is copied four times with it. | `self.checked_append(new_child, arena).expect("Preconditions not met: invalid argument");` |
| P1-8 | indextree/src/id.rs:1052 | #11 | `checked_append` (1052) and `checked_prepend` (1243) are T2 clones: same five statements, differing only in the two error variants and the last two `insert_with_neighbors` arguments (`last_child, None` vs `None, first_child`). | `if self.ancestors(arena).any(\|ancestor\| new_child == ancestor) {` |
| P1-9 | indextree/src/id.rs:1382 | #11 | `checked_insert_after` (1382) and `checked_insert_before` (1522) are T2 clones: same six statements, differing only in the error variant, the destructured field, and the neighbour argument order. | `let (next_sibling, parent) = { let current = &arena[self]; (current.next_sibling, current.parent) };` |
| P1-10 | indextree/src/id.rs:1740 | #11 | `remove_subtree` (1740) and `remove_children` (1919) share a 12-statement free-walk block verbatim (read 3 links, `free_node`, `first_child.or(next_sibling).or_else(ancestor walk)`). The only difference is the `if a == self { return None; }` guard present at 1933 and absent at 1751, which is the kind of divergence duplication produces. | `cursor = first_child.or(next_sibling).or_else(\|\| {` |
| P1-11 | indextree/src/debug_pretty_print.rs:247 | #11 | `Display::fmt` (247) and `Debug::fmt` (278) are 25-line T2 clones differing only in the four format specs (`{data}`/`{data:#}` vs `{data:?}`/`{data:#?}`), including both section comments. A single generic helper taking a formatting closure removes one copy. | `while let Some(id) = prepare_next_node_printing(&mut writer, &mut traverser)? {` |
| P1-12 | indextree/src/traverse.rs:87 | #11 | The identical `size_hint` body appears five times: macro arms at 87 and 125, then `Descendants` (273), `Traverse` (552), `ReverseTraverse` (601). Same `is_some()` -> `(1, None)` else `(0, Some(0))` shape each time. | `if self.0.node.is_some() { (1, None) } else { (0, Some(0)) }` |
| P1-13 | indextree/src/node.rs:59 | #11 | Four-member clone family: `get` (59) / `get_mut` (103) and `try_get` (79) / `try_get_mut` (126). All four are the same `if let NodeData::Data(..) = self.data` shape; `get`/`get_mut` even repeat the panic literal `"attempted to access a freed node"`. `get` could be `try_get().expect(..)`. | `if let NodeData::Data(ref data) = self.data { data } else { panic!("attempted to access a freed node") }` |
| P1-14 | indextree/src/node.rs:452 | #11 | `Display for Node` repeats one if/else `write!` block five times over `parent`, `previous_sibling`, `next_sibling`, `first_child`, `last_child` (452-476). 25 lines that a slice of `(&str, Option<NodeId>)` pairs plus one loop replaces. | `if let Some(parent) = self.parent { write!(f, "parent: {parent}; ")?; } else { write!(f, "no parent; ")?; }` |
| P1-15 | indextree/src/arena.rs:911 | #11 | `Default::default` (911) duplicates `Arena::new`'s struct literal verbatim; `with_capacity` (68) is the third copy of the same three-field initialiser. `Default` should be `Self::new()`. Three homes for "what an empty arena is". | `Self { nodes: Vec::new(), first_free_slot: None, last_free_slot: None }` |
| P1-16 | indextree/src/relations.rs:176 | #11 | `insert_last_unchecked` (176) and `insert_first_unchecked` (205) are T2 clones: same four statements, differing only in `last_child`/`first_child` and the argument slot. Even the two-line `.expect(..)` message is duplicated with one word changed (`append` vs `prepend`), so the two panic strings will drift. | `DetachedSiblingsRange::new(new, new).transplant(arena, Some(parent), previous_sibling, None)` |
| P1-17 | indextree/src/traverse.rs:59 | #36 | Four `#[allow(clippy::redundant_closure_call)]` in one module (59, 74, 102, 130), all suppressing the same lint that the `new_iterator!` design creates by passing closures through `$new`/`$next` and immediately calling them. The module carries every `#[allow]` in the crate except one; the lint is being silenced rather than the macro fixed. | `#[allow(clippy::redundant_closure_call)]` |
| P1-18 | indextree/tests/remove.rs:6 | #42 | `toplevel_with_no_child` has no verdict of any kind: it creates one node and calls `remove`. It passes for any behaviour short of a panic, and in particular would not notice a corrupted free list, a wrong stamp, or a wrong `is_removed`. | `let n1 = arena.new_node("1"); n1.remove(&mut arena); }` |
| P1-19 | indextree/tests/lib.rs:1098 | #44 | `assert_eq!(NodeError::AppendSelf, NodeError::AppendSelf)` compares a call-free constant expression with itself: it cannot fail for any implementation of `PartialEq` that is reflexive, which the derive always is. The following `assert_ne!` of two distinct unit variants (1099) is the same non-test. | `assert_eq!(NodeError::AppendSelf, NodeError::AppendSelf);` |
| P1-20 | indextree/src/id.rs:1111 | none | The doc block on `append_new_node_unchecked` is copy-pasted from `checked_append`: its `# Examples` calls `checked_append`, never the documented function, so the example demonstrates the wrong API. The function itself is a one-line delegate to `insert_last_unchecked` called from exactly one place (`append_value`, 1106), while the symmetric `prepend_value` (1168) calls `insert_first_unchecked` directly. | `assert!(n1.checked_append(n1, &mut arena).is_err());` |
| P1-21 | indextree/src/id.rs:92 | none | `NodeStamp::reuse(&mut self) -> Self` returns a value no caller uses: the only call site, `node.rs:177`, is `self.stamp.reuse();`. The return is unexercised surface that hides that the method mutates. | `pub(crate) fn reuse(&mut self) -> Self {` |
| P1-22 | indextree-macros/src/lib.rs:117 | none | `ActionStream::count` is written (initialised to 1 at 263, incremented at 258) and never read: `ToTokens` at 123 emits only `stream`. The field and both writes are dead. | `struct ActionStream { count: usize, kind: ActionKind, stream: TokenStream }` |
| P1-23 | indextree-macros/src/lib.rs:298 | none | `tree!` expands to `unsafe { ManuallyDrop::take(..) }` at the call site (298 and 306), so the macro cannot be used inside a crate that declares `#![forbid(unsafe_code)]` - which is exactly what `indextree/src/lib.rs:66` does. Neither the macro docs (128-211) nor the README mention this constraint. | `unsafe { ::core::mem::ManuallyDrop::take(&mut self.0) }` |
| P1-24 | indextree/tests/lib.rs:334 | none | Line 334 repeats line 333 verbatim (`n2.checked_append(n5, arena)`), so the second call re-appends a node that is already `n2`'s last child. Copy-paste artefact: the expected node list at 348 shows six values for seven appends, and no assertion distinguishes the duplicate from a missing `n7`. | `assert!(n2.checked_append(n5, arena).is_ok());` |
| P1-25 | indextree-macros/tests/stress_test.rs:1 | none | `mod regular_usage;` pulls the whole sibling integration-test file into this binary, so its four `#[test]` fns (`no_nesting`, `mild_nesting`, `integer_values`, `single_leaf_node`) are compiled and executed a second time. The shared helper `compare_nodes` belongs in `tests/common/mod.rs`. | `mod regular_usage;` |
| P1-26 | indextree/src/arena.rs:228 | none | The deprecated `count()` carries a 25-line doc comment duplicated verbatim from `len()` (233-254), down to the `[iter()]` and `[Node::is_removed()]` link definitions. Two homes for one explanation, on a method whose whole point is that it is going away. | `#[deprecated(since = "4.9.0", note = "use len() instead")]` |
| P1-27 | indextree/src/traverse.rs:115 | none | `(Some(head), None) \| (Some(head), Some(_))` is just `(Some(head), _)`, and `(None, Some(_)) \| (None, None)` at 121 is `(None, _)`; the same pair is written again at 140 and 146 in the `next_back` arm. Four hand-expanded match alternations that the compiler already collapses. | `(Some(head), None) \| (Some(head), Some(_)) => {` |

Rules in the inventory with no site found on this repo: **#18** (no numbered or labelled multi-phase narration inside any single function; the two comments in `debug_pretty_print.rs:252/263` are a two-step preamble, not phases), **#20** (no non-trivial closure body repeated three times; the `new_iterator!` closures are single field projections), **#34** (every non-doc comment run of three or more lines is prose or an ASCII tree diagram, none is disabled Rust), **#37** (the only `trait` declarations are `__ToNodeId` / `__NodeIdToNodeId` inside `quote!` at `indextree-macros/src/lib.rs:286/290`; each has one impl, but the two-trait split *is* the autoref-specialization mechanism and removing either breaks it, so this is not an improvement site), **#38** (no module-level string literal shared by three or more modules; the crate declares no string consts at all), **#47** (no sleep of any kind anywhere in the workspace).

## Phase 2 - audit finding verdicts

Audit: `../sightline-rs1/audits/indextree.json`; sheet:
`corpus-ext/sheets/indextree.rs1.wave1.tsv` (verbatim `why` text lives there).
25 findings, all judged: **21 real, 4 fp**. By rule: rs:11 20 rows (18 real, 2 fp),
rs:20 2 rows (0 real, 2 fp), rs:23 2 rows (2 real), rs:29 1 row (1 real).

| finding (path:line) | rule | tier | verdict | why |
|---------------------|------|------|---------|-----|
| indextree/src/debug_pretty_print.rs:31 | rs:11 | indexed | real | real: the same 4-case (is_last_item, is_first_line) table is spelled three times here (as_str:31, as_str_leading:42, as_str_trailing_spaces:53) and as_str is exactly leading++trailing in all four cases, so an indent-style change must be repeated in three places with nothing checking it |
| indextree/src/debug_pretty_print.rs:42 | rs:11 | indexed | real | real: the same 4-case (is_last_item, is_first_line) table is spelled three times here (as_str:31, as_str_leading:42, as_str_trailing_spaces:53) and as_str is exactly leading++trailing in all four cases, so an indent-style change must be repeated in three places with nothing checking it |
| indextree/src/id.rs:909 | rs:11 | indexed | real | real: five byte-identical guard wrappers modulo the delegate name (is_removed -> Err(Removed), call, Ok); the removed-check fact has five homes and the next checked_* method copies it a sixth time |
| indextree/src/id.rs:1563 | rs:11 | indexed | real | real: five byte-identical guard wrappers modulo the delegate name (is_removed -> Err(Removed), call, Ok); the removed-check fact has five homes and the next checked_* method copies it a sixth time |
| indextree/src/id.rs:1687 | rs:11 | indexed | real | real: five byte-identical guard wrappers modulo the delegate name (is_removed -> Err(Removed), call, Ok); the removed-check fact has five homes and the next checked_* method copies it a sixth time |
| indextree/src/id.rs:1745 | rs:11 | indexed | real | real: the 5-statement free-walk block is copied verbatim between remove_subtree and remove_children and has already drifted - the `if a == self { return None; }` guard exists at 1933 and not at 1751 |
| indextree/src/id.rs:1781 | rs:11 | indexed | real | real: five byte-identical guard wrappers modulo the delegate name (is_removed -> Err(Removed), call, Ok); the removed-check fact has five homes and the next checked_* method copies it a sixth time |
| indextree/src/id.rs:1867 | rs:11 | indexed | real | real: five byte-identical guard wrappers modulo the delegate name (is_removed -> Err(Removed), call, Ok); the removed-check fact has five homes and the next checked_* method copies it a sixth time |
| indextree/src/id.rs:1925 | rs:11 | indexed | real | real: the 5-statement free-walk block is copied verbatim between remove_subtree and remove_children and has already drifted - the `if a == self { return None; }` guard exists at 1933 and not at 1751 |
| indextree/src/traverse.rs:273 | rs:11 | indexed | real | real: the same is_some -> (1, None) else (0, Some(0)) body appears five times in this module (273, 552, 601 plus the macro arms at 87 and 125); one shared helper replaces all five |
| indextree/src/traverse.rs:383 | rs:11 | indexed | fp | fp: mirror pair - two documented pub methods on NodeEdge whose bodies differ at every mirrored axis (first_child/last_child, next/previous_sibling, Start/End); merging needs a direction parameter threaded through a public API and makes both call sites worse, so the only fact actually duplicated is the 3-line comment at 393/496 |
| indextree/src/traverse.rs:486 | rs:11 | indexed | fp | fp: mirror pair - two documented pub methods on NodeEdge whose bodies differ at every mirrored axis (first_child/last_child, next/previous_sibling, Start/End); merging needs a direction parameter threaded through a public API and makes both call sites worse, so the only fact actually duplicated is the 3-line comment at 393/496 |
| indextree/src/traverse.rs:518 | rs:11 | indexed | real | real: Traverse and ReverseTraverse are the same struct with the same three fields and their new/next_of_next/next/size_hint are all clone groups; weakest member alone (one struct literal), but the module's own new_iterator! macro at :51 is the established remedy and was not applied to this pair |
| indextree/src/traverse.rs:527 | rs:11 | indexed | real | real: mirrored bodies of a fully duplicated private type pair, not a public API mirror - collapsing Traverse/ReverseTraverse behind the module's own new_iterator! macro removes this copy with no direction parameter at any call site |
| indextree/src/traverse.rs:546 | rs:11 | indexed | real | real: the two Iterator::next bodies are byte-identical (take, next_of_next, Some), the direction difference having already been factored into next_of_next; nothing distinguishes the two impls |
| indextree/src/traverse.rs:552 | rs:11 | indexed | real | real: the same is_some -> (1, None) else (0, Some(0)) body appears five times in this module (273, 552, 601 plus the macro arms at 87 and 125); one shared helper replaces all five |
| indextree/src/traverse.rs:575 | rs:11 | indexed | real | real: Traverse and ReverseTraverse are the same struct with the same three fields and their new/next_of_next/next/size_hint are all clone groups; weakest member alone (one struct literal), but the module's own new_iterator! macro at :51 is the established remedy and was not applied to this pair |
| indextree/src/traverse.rs:584 | rs:11 | indexed | real | real: mirrored bodies of a fully duplicated private type pair, not a public API mirror - collapsing Traverse/ReverseTraverse behind the module's own new_iterator! macro removes this copy with no direction parameter at any call site |
| indextree/src/traverse.rs:595 | rs:11 | indexed | real | real: the two Iterator::next bodies are byte-identical (take, next_of_next, Some), the direction difference having already been factored into next_of_next; nothing distinguishes the two impls |
| indextree/src/traverse.rs:601 | rs:11 | indexed | real | real: the same is_some -> (1, None) else (0, Some(0)) body appears five times in this module (273, 552, 601 plus the macro arms at 87 and 125); one shared helper replaces all five |
| indextree/src/arena.rs:673 | rs:20 | heuristic | fp | fp: trivial projection - the body is a single method call on the parameter (\|n\| n.index0()), all four uses sit inside the one function validate, and the fix is the method path NodeId::index0 rather than a named predicate; there is no logic here that can drift |
| indextree/tests/lib.rs:299 | rs:20 | heuristic | fp | fp: scope leak - #20 is a prod rule but this fired inside the integration test crate indextree/tests/lib.rs (symbol indextree::tests::lib::iter_mut); the body is also a trivial projection, a deref of one method call, spread over six independent tests that would not share a helper anyway |
| indextree/src/arena.rs:646 | rs:23 | heuristic | real | real: ~110 lines with two top-level loops, a nested child-chain walk and a free-list walk each carrying its own step counter, and 20 `return false` exits; the caller learns only true/false and cannot tell which invariant broke |
| indextree/src/debug_pretty_print.rs:181 | rs:23 | heuristic | real | real: a three-state line-writing machine (BeforeIndent/PartialIndent/Content) plus a pending-indent counter driven inside a byte-slice while loop; the indent-completion half splits out cleanly |
| indextree-macros/src/lib.rs:1 | rs:29 | heuristic | real | real: 321-line crate root with no //! header at all while every module in the sibling indextree crate has one; the first screen is a use block, so nothing states the parse -> stack -> action-coalesce -> quote pipeline |

### False-positive shapes

| rule | fp class | count | example key |
|------|----------|-------|-------------|
| rs:11 | mirror pair: two documented `pub` methods whose bodies differ at every mirrored axis (`first_child`/`last_child`, `next_sibling`/`previous_sibling`, `Start`/`End`); a merge needs a direction parameter threaded through a public API and makes both call sites worse | 2 | indextree/src/traverse.rs:383:11:clone:cd451c308742 |
| rs:20 | trivial projection: the closure body is a single method call on the parameter (`\|n\| n.index0()`), so there is no predicate logic that can drift and the fix is a method path, not a name | 1 | indextree/src/arena.rs:673:20:closure:indextree::arena:ba0de736 |
| rs:20 | scope leak: a prod-only rule fired inside the integration test crate `indextree/tests/lib.rs` | 1 | indextree/tests/lib.rs:299:20:closure:indextree::tests::lib:9102f778 |

Counts sum to each rule's fp total (rs:11 2, rs:20 2).

## Phase 3 - reconciliation

| P1 id | rule | class (covered / detector-miss / threshold-miss / inventory-gap) | note |
|-------|------|------------------------------------------------------------------|------|
| P1-1 | #27 | detector-miss | No rs:27 finding anywhere in the repo. id.rs is 2213 lines / 77 KB with 51 `pub fn`, roughly 2x the next largest module; that is past any plausible "big module" cutoff, so the absence points at the detector (or at a hotness input the Rust fact set does not carry) rather than a threshold. |
| P1-2 | #27 | threshold-miss | arena.rs at 1008 lines is genuinely near a plausible cutoff, so unlike P1-1 this one is explained by a size bar alone. |
| P1-3 | #29 | covered | `indextree-macros/src/lib.rs:1` fired with "321 lines, 7 top-level items, no `//!` module doc". |
| P1-4 | #23 | covered | `arena.rs:646` fired at cognitive complexity 62 (threshold 15). |
| P1-5 | #23 | covered | `debug_pretty_print.rs:181` fired at 19. |
| P1-6 | #11 | covered | The five `checked_*` guard wrappers fired as one clone group (909, 1563, 1687, 1781, 1867), exactly the family named in phase 1. |
| P1-7 | #11 | detector-miss | The four panicking wrappers (`append` 1021, `prepend` 1212, `insert_after` 1309, `insert_before` 1449) have identical one-statement bodies modulo the delegate name and even share the panic literal, yet no group formed. Neither a size floor nor identifier normalization explains it: `Traverse::new` (a single struct-literal expression) did group at traverse.rs:518, and the P1-6 group 30 lines away normalizes exactly this kind of method-name difference. |
| P1-8 | #11 | detector-miss | `checked_append` (1052) / `checked_prepend` (1243) are 5-statement near-clones, larger than the P1-6 bodies that did fire. The only structural difference is a permuted argument slot in `insert_with_neighbors` (`last_child, None` vs `None, first_child`), which makes the pair T3-by-argument-permutation rather than strict T2, and the blind digest does not reach it. |
| P1-9 | #11 | detector-miss | Same shape as P1-8: `checked_insert_after` (1382) / `checked_insert_before` (1522) differ by one destructured field name and one argument permutation. |
| P1-10 | #11 | covered | The clone-block arm fired at id.rs:1745 / 1925 on the 5-statement free-walk, the exact block named in phase 1 (my anchors 1740 / 1919 were the enclosing fns). |
| P1-11 | #11 | detector-miss | `Display::fmt` (247) / `Debug::fmt` (278) are 25-line clones differing only in four format specs inside `write!`. The differing tokens sit inside a macro invocation, so the digest appears to treat the `write!` token stream opaquely instead of normalizing the format literal. |
| P1-12 | #11 | covered | The size_hint group fired at traverse.rs:273 / 552 / 601. It counted only the three `impl` copies; the two further copies inside the `new_iterator!` macro arms (87, 125) are invisible to it, so the group understates the fact by two. |
| P1-13 | #11 | detector-miss | `get` (59) / `get_mut` (103) are identical modulo the `ref` / `ref mut` binding mode, and `try_get` (79) / `try_get_mut` (126) likewise; no node.rs finding of any rule was produced. The `Traverse::new` precedent rules out a size floor, so the lead is that binding-mode mutability is not normalized in the pattern. |
| P1-14 | #11 | threshold-miss | The repeated unit in `Display for Node` (452-476) is a single if/else statement written five times, under the clone-block arm's ">=5 statements" floor. The arm did fire on a 5-statement block at id.rs:1745, so the bar, not the detector, is what excludes this. |
| P1-15 | #11 | detector-miss | `Default::default` (911) duplicates `Arena::new`'s struct literal verbatim, and `with_capacity` (68) is a third copy. Single struct-literal bodies do group elsewhere (traverse.rs:518 / 575), so the untested variable here is that `Arena::new` is a `const fn` in an inherent impl while `default` is a trait impl. |
| P1-16 | #11 | detector-miss | `insert_last_unchecked` (176) / `insert_first_unchecked` (205): same T3-by-argument-permutation shape as P1-8 and P1-9, plus a one-word difference inside the duplicated two-line `expect` string. |
| P1-17 | #36 | threshold-miss | Four `#[allow(clippy::redundant_closure_call)]` in a 681-line module is a density of 0.6%, under any per-module `#[allow]` bar; no rs:36 finding was produced anywhere. |
| P1-18 | #42 | detector-miss | `tests/remove.rs:6` is the cleanest possible instance: a `#[test] fn` whose body is two `let`s and one call, with no assertion and no helper. rs:20 reached `indextree/tests/lib.rs`, so test files are in the fact set; what failed is the test-function or verdict detection, not file enumeration. No rs:42 finding exists on any of the five test files. |
| P1-19 | #44 | detector-miss | `tests/lib.rs:1098` is `assert_eq!(NodeError::AppendSelf, NodeError::AppendSelf)`, a call-free constant expression asserted against itself, which is the rule's own definition. No rs:44 finding exists. |
| P1-20 | none | inventory-gap | Doc example on `append_new_node_unchecked` documents a different API. No rule covers a doc block whose example never calls the documented item. |
| P1-21 | none | inventory-gap | `NodeStamp::reuse` returns a value its only caller discards. The rs #37 arm is trait-with-one-impl only, so unexercised return surface has no home. |
| P1-22 | none | inventory-gap | `ActionStream::count` is written twice and never read. No dead-field rule in the Rust inventory. |
| P1-23 | none | inventory-gap | `tree!` emits `unsafe` at the call site, so it cannot be used from a crate declaring `#![forbid(unsafe_code)]`, which is what indextree itself declares. No rule reads generated-code constraints. |
| P1-24 | none | inventory-gap | Duplicated `checked_append(n5)` line in a test. #44 covers tautological assertions, not a repeated setup call that silently swallows a copy-paste. |
| P1-25 | none | inventory-gap | `mod regular_usage;` in stress_test.rs re-runs four tests in a second binary. No rule reads integration-test module wiring. |
| P1-26 | none | inventory-gap | Deprecated `count()` carries a 25-line doc block duplicated verbatim from `len()`. #11 reads code bodies, not doc comments. |
| P1-27 | none | inventory-gap | Hand-expanded match alternations the compiler already collapses. No rule covers redundant pattern alternatives. |

**Phase 3 totals:** 27 phase-1 sites: 6 covered, 21 misses (10 detector-miss,
3 threshold-miss, 8 inventory-gap).

### Findings the phase-1 list did not anticipate

Four real findings sit outside my phase-1 list, all in `traverse.rs` and all the
same fact: `Traverse` and `ReverseTraverse` are the same three-field struct with
`new` (518/575), `next_of_next` (527/584) and `next` (546/595) duplicated, the
last pair byte-identical. In phase 1 I recorded only their shared `size_hint`
(P1-12) and read the rest as an acceptable forward/backward mirror. The checker
is right and I was wrong: the module already contains the remedy, the
`new_iterator!` macro at traverse.rs:51, and this one pair is the only iterator
in the file that does not go through it. The fifth unanticipated finding,
`IndentedBlockState::as_str` / `as_str_leading` (dpp 31/42), is also real: with
`as_str_trailing_spaces` at :53 the same four-case table is written three times
and `as_str` is exactly `leading ++ trailing` in every case.
