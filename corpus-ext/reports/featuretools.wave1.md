# featuretools — wave 1

Repo: `<GAUNTLET_CORPUS_ROOT>\featuretools`
(prod tree = `featuretools/` excluding `featuretools/tests/`, ~24.7 kLoC).
Judged cold against `research/SUMMARY.md` §3 rule inventory (#1–31).

## Phase 1 — blind ideal sites

| id | site (path:line) | rule | claim | excerpt (<=3 lines) |
|----|------------------|------|-------|---------------------|
| P1-1 | featuretools/entityset/entityset.py:21 | #9 | Importing the entityset module silently rewrites a process-global pandas option; every downstream library in the host process loses chained-assignment warnings. | `pd.options.mode.chained_assignment = None  # default='warn'` |
| P1-2 | featuretools/feature_base/feature_base.py:15 | #9 | Module-level mutable registry written from a different module (`entityset.py:114`, `entityset.py:1667`) and read here at `:43` and `:469`. Nothing ever evicts, so every EntitySet ever constructed is retained for the process lifetime, and two EntitySets sharing an `id` silently overwrite each other. | `_ES_REF = {}` |
| P1-3 | featuretools/entityset/entityset.py:194 | #13 | `to_pickle`, `to_parquet` (`:213`) and `to_csv` (`:234`) are three forwarding wrappers over `serialize.write_data_description` whose only real content is the `format=` literal; `to_dictionary` (`:268`) is a one-line forward to `serialize.entityset_to_description`. | `        serialize.write_data_description(`<br>`            self,`<br>`            path,` |
| P1-4 | featuretools/entityset/entityset.py:496 | #11 | `get_forward_dataframes` and `get_backward_dataframes` (`:519`) are 22-line token-for-token clones differing only in `get_forward_relationships`/`get_backward_relationships`, `_parent_`/`_child_`, and `True`/`False`. Same clone shape again at `:542`/`:555`. | `    def get_forward_dataframes(self, dataframe_name, deep=False):` |
| P1-5 | featuretools/entityset/entityset.py:1035 | none | Tautology: `col != index or col != time_index` is true for every column unless `index == time_index`, so the "drop the index columns" filter never drops anything. The `else` branch two lines down shows the intent was `and`/`not in`. | `                    if col != df.ww.index or col != df.ww.time_index` |
| P1-6 | featuretools/entityset/entityset.py:1206 | none | `dfs_to_update` is initialized empty and never written; the loop at `:1236` that applies the last-time-index semantic tag is therefore dead code (the live path is the block at `:1220`–`:1234`). | `        dfs_to_update = {}` |
| P1-7 | featuretools/entityset/entityset.py:1144 | #19 | Membership test rebuilds a list of every queued dataframe name on each inner iteration, inside a `while` over the same queue — O(n·m) over the relationship graph. | `                        if df.ww.name not in explored and df.ww.name not in [`<br>`                            q.ww.name for q in queue`<br>`                        ]:` |
| P1-8 | featuretools/entityset/entityset.py:1089 | #19 | `list.pop(0)` used as a queue dequeue inside a `while` (again at `:1120`, and at `computational_backends/feature_set.py:195`) — each pop is O(n); `collections.deque` is already imported elsewhere in the package. | `                df_name = parent_queue.pop(0)` |
| P1-9 | featuretools/entityset/entityset.py:931 | #12 | `list(d.keys())[0]` / `list(d.values())[0]` builds a whole list to take one element; the idiom is `next(iter(d))`. Four occurrences in this function (`:931`, `:933`, `:954`, `:955`) and again at `computational_backends/feature_set.py:236`. | `            secondary_time_index = list(make_secondary_time_index.keys())[0]` |
| P1-10 | featuretools/entityset/entityset.py:914 | #12 | Identity comprehensions `[col for col in xs]` where `xs` is already a list — `list(xs)` or `xs` itself. Also `:885` and `:915`, and `synthesis/get_valid_primitives.py:71`–`:72`. | `            + [col for col in additional_columns]`<br>`            + [col for col in copy_columns]` |
| P1-11 | featuretools/entityset/entityset.py:1714 | #18 | `_get_or_create_index` narrates six labeled phases as comments ("Case 1" … "Case 6", lines 1714, 1716, 1725, 1731, 1737, 1741) inside a 34-line function — the cases want to be named predicates or a table. | `        # Case 1: user wanted to make index but did not specify column name` |
| P1-12 | featuretools/entityset/entityset.py:1631 | #6 | `_check_secondary_time_index` is named as a predicate but mutates the caller's list in place, appending the time-index name to the `columns` value it was handed. | `            if time_index not in columns:`<br>`                columns.append(time_index)` |
| P1-13 | featuretools/entityset/entityset.py:1639 | #6 | `_check_uniform_time_index` writes `self.time_type` — the first dataframe checked silently sets the entityset-wide time type, so validation order is load-bearing. | `        if self.time_type is None:`<br>`            self.time_type = time_type` |
| P1-14 | featuretools/entityset/entityset.py:750 | #23 | `normalize_dataframe` is 250 lines with 9 parameters, three interleaved concerns (validation, time-index derivation, secondary-time-index construction) and a `new_dataframe`/`new_dataframe2` rebinding dance at `:918`–`:949`. | `    def normalize_dataframe(`<br>`        self,`<br>`        base_dataframe_name,` |
| P1-15 | featuretools/entityset/entityset.py:1063 | #23 | `add_last_time_indexes` is 180 lines: two queue-BFS passes, a re-entrancy hack (`queue.append(dataframe); continue`) and a per-dataframe pandas pipeline, all in one body. | `    def add_last_time_indexes(self, updated_dataframes=None):` |
| P1-16 | featuretools/entityset/entityset.py:48 | #1 | The primary public input type is `dict[str -> tuple(DataFrame, str, str, dict, dict, bool)]` — a six-slot positional tuple decoded by `len()` at `:89`–`:98`. The same weak contract is restated verbatim at `computational_backends/calculate_feature_matrix.py:91` and `synthesis/dfs.py:50`. | `            dataframes (dict[str -> tuple(DataFrame, str, str, dict[str -> str/Woodwork.LogicalType], dict[str->str/set], boolean)]):` |
| P1-17 | featuretools/entityset/entityset.py:89 | #26 | Optional construction arguments are read out of a tuple by length probing rather than declared — six `if len(...) > n` lines in a row, so a caller who passes five elements silently gets `make_index=False`. | `            if len(dataframes[df_name]) > 1:`<br>`                index_column = dataframes[df_name][1]` |
| P1-18 | featuretools/feature_base/feature_base.py:87 | #6 | `get_name()` is a memoizing mutator: it writes `self._name` on first call. Same shape in `get_feature_names()` (`:92`, writes `self._names`) — both are on the hottest read path in the package. | `    def get_name(self):`<br>`        if not self._name:`<br>`            self._name = self.generate_name()` |
| P1-19 | featuretools/feature_base/feature_base.py:148 | #12 | Hand-rolled list copy: slice-copy the list, then append each element one at a time. `deps = list(self.base_features)` is the whole loop. | `        for d in self.base_features[:]:`<br>`            deps += [d]` |
| P1-20 | featuretools/feature_base/feature_base.py:420 | none | `__not__` forwards one argument to `NOT`, but `NOT` (`:437`) takes only `self` — any call raises `TypeError`. `__invert__` at `:451` calls it correctly, which is why this has never been caught. | `    def __not__(self, other):`<br>`        return self.NOT(other)` |
| P1-21 | featuretools/feature_base/feature_base.py:903 | #21 | "The groupby feature is the last element of `base_features`" is an unencoded invariant re-derived in four methods of one class: `__init__` appends it (`:903`), `copy` strips it with `[:-1]` (`:928`), `generate_name`/`generate_names` strip it again (`:936`, `:941`), and `get_arguments` filters it by name comparison (`:948`–`:952`). | `        base_features.append(groupby)` |
| P1-22 | featuretools/feature_base/feature_base.py:1017 | #11 | `FeatureOutputSlice.__init__` re-implements `FeatureBase.__init__` (`:19`–`:61`) by hand — eight attribute assignments copied instead of a `super().__init__` call, so it silently skips the base-feature and input-type assertions. | `    def __init__(self, base_feature, n, name=None):` |
| P1-23 | featuretools/feature_base/feature_base.py:1070 | #24 | Type dispatch by comparing a class's `__name__` to a string literal — invisible to any static tool, and matches any unrelated class also named `Series`. | `    if "Series" == type(feature).__name__:` |
| P1-24 | featuretools/feature_base/feature_base.py:804 | #14 | The five-name clump `(base_feature_names, relationship_path_name, parent_dataframe_name, where_str, use_prev_str)` is spelled out at `:805`, `:814`, and in both `primitives/base/aggregation_primitive_base.py:5` and `:23` — four signatures, one unnamed concept. | `        return self.primitive.generate_name(`<br>`            base_feature_names=[bf.get_name() for bf in self.base_features],` |
| P1-25 | featuretools/primitives/base/aggregation_primitive_base.py:9 | none | `parent_dataframe_name` is accepted by `generate_name` and `generate_names` (`:27`), forwarded at `:35`, and passed by `feature_base.py:808`/`:817` — and never read: the format string at `:14` has six placeholders for the six *other* values. A dead parameter threaded through four signatures. | `        parent_dataframe_name,` |
| P1-26 | featuretools/feature_base/feature_base.py:969 | none | `Feature.__new__` names its first parameter `self`; it is the class. Any reader (or tool) reasoning about `__new__` semantics is misled, and the method never calls `super().__new__`. | `    def __new__(`<br>`        self,`<br>`        base,` |
| P1-27 | featuretools/feature_base/cache.py:64 | #9 | Module-level singleton whose `enabled` flag and contents are mutated from another package: `synthesis/deep_feature_synthesis.py:160`–`:161` clears it and switches it on inside a constructor. Two concurrent DFS objects share one cache with no keying by owner. | `feature_cache = FeatureCache()` |
| P1-28 | featuretools/feature_base/cache.py:34 | #1 | `Optional[Union[List[Any], Any]]` collapses to `Any` — the annotation carries no information. `cache: defaultdict[dict]` at `:28` is also malformed (a `defaultdict` takes two parameters). | `    ) -> Optional[Union[List[Any], Any]]:` |
| P1-29 | featuretools/feature_base/utils.py:8 | #12 | `len(x)` used as a truth test on a set difference; `if template.semantic_tags - candidate.semantic_tags:` is the idiom. This is the single hottest predicate in DFS (called per feature × per candidate schema). | `    if len(template.semantic_tags - candidate.semantic_tags):` |
| P1-30 | featuretools/computational_backends/calculate_feature_matrix.py:372 | #14 | The eleven-name clump `(cutoff_time, chunk_size, feature_set, entityset, approximate, training_window, save_progress, no_unapproximated_aggs, cutoff_df_time_col, target_time, pass_columns)` appears in three signatures — `calculate_feature_matrix:51`, `calculate_chunk:372`, `parallel_calculate_chunks:697` — and is re-typed at both call sites (`:298`, `:317`). | `def calculate_chunk(`<br>`    cutoff_time,`<br>`    chunk_size,` |
| P1-31 | featuretools/computational_backends/calculate_feature_matrix.py:403 | #11 | The `update_progress_callback` closure is written out three times — `:403`, `:464` (nested one level deeper, inside a decorated closure inside a loop) and inline at `:811` — differing only in the multiplier (`len(cutoff_time[1])` vs `group.shape[0]` vs `result.shape[0]`). | `            def update_progress_callback(done):`<br>`                previous_progress = progress_bar.n`<br>`                progress_bar.update(done * len(cutoff_time[1]))` |
| P1-32 | featuretools/computational_backends/calculate_feature_matrix.py:294 | none | File handle on `os.devnull` opened and never closed; the `with` block at `:296` manages the tqdm iterator, not this file. Leaks one descriptor per `calculate_feature_matrix` call with a progress callback. | `        tqdm_options.update({"file": open(os.devnull, "w"), "disable": False})` |
| P1-33 | featuretools/computational_backends/calculate_feature_matrix.py:824 | none | `except Exception: raise` is a no-op clause; the `finally` alone has the same behaviour. It reads as if error handling exists. | `    except Exception:`<br>`        raise` |
| P1-34 | featuretools/computational_backends/calculate_feature_matrix.py:873 | none | `new_col_name` is bound only inside the `for` at `:854`; an empty `path` makes the return statement raise `UnboundLocalError`. The docstring promises "the name of this column" unconditionally. | `    return cutoffs, new_col_name` |
| P1-35 | featuretools/computational_backends/calculate_feature_matrix.py:923 | #6 | `init_ww_and_concat_fm` mutates the `ww_init_kwargs` dict its caller owns (and reuses at `:833`/`:568`), rewriting logical types in place while nominally just concatenating frames. | `                ww_init_kwargs["logical_types"][col] = replacement_type[current_type]` |
| P1-36 | featuretools/computational_backends/calculate_feature_matrix.py:251 | #19 | `all_approx_features` is loop-invariant but rebuilt from the whole approximate-feature trie on every iteration of the `for feature in features` loop at `:244`. | `        if approximate is not None:`<br>`            all_approx_features = {`<br>`                f for _, feats in feature_set.approximate_feature_trie for f in feats` |
| P1-37 | featuretools/computational_backends/calculate_feature_matrix.py:693 | none | A local string variable shadows the enclosing function's own name inside its body, so `scatter_warning` is not recursively callable and the name means two things in six lines. | `def scatter_warning(num_scattered_workers, num_workers):`<br>`    if num_scattered_workers != num_workers:`<br>`        scatter_warning = "EntitySet was only scattered to {} out of {} workers"` |
| P1-38 | featuretools/computational_backends/feature_set_calculator.py:214 | #18 | `_calculate_features_for_dataframe` is segmented by literal `# Step 1:` … `# Step 5:` comments (lines 214, 247, 272, 310, 356) across a 195-line body — five named phases with no names. | `        # Step 1: Get a dataframe for the given dataframe name, filtered by the given`<br>`        # conditions.` |
| P1-39 | featuretools/computational_backends/feature_set_calculator.py:134 | #19 | `i not in df[col]` inside a comprehension over `instance_ids`: quadratic, and `in` on a pandas Series tests the *index*, not the values, so this almost certainly computes the wrong `missing_ids` set as well. | `        missing_ids = [`<br>`            i for i in instance_ids if i not in df[target_dataframe.ww.index]`<br>`        ]` |
| P1-40 | featuretools/computational_backends/feature_set_calculator.py:795 | #2 | `frame.index` is a `pd.Index`; `pd.CategoricalDtype` is a dtype class, never an Index base — this isinstance can never be true, so the documented pandas-22501 workaround below it is dead. The sibling site `entityset.py:1517` gets the same check right via `.is_categorical`. | `                if isinstance(frame.index, pd.CategoricalDtype):` |
| P1-41 | featuretools/computational_backends/feature_set_calculator.py:736 | #24 | A function's `__name__` is overwritten with `str(id(func))` and then used to build the string key `"{column_id}-{funcname}"`, which is re-split with `"-".join(x)` at `:787`. Identifier identity is routed through address strings and string splitting on a separator that may occur in column names. | `                        funcname = str(id(func))` |
| P1-42 | featuretools/computational_backends/feature_set_calculator.py:456 | none | Five-branch `type(f) == Class` dispatch chain — exact-type equality (so subclasses fall through to `UnknownFeature`) where a declared `{class: handler}` table would be one lookup. | `        if type(f) == TransformFeature:`<br>`            return self._calculate_transform_features` |
| P1-43 | featuretools/computational_backends/feature_set_calculator.py:880 | #1 | `update_feature_columns` returns a `dict` when handed a dict and a `DataFrame` otherwise; the two callers (`:523`, `:873`) each know which, but the function's contract is unstatable. | `def update_feature_columns(feature_data, data):` |
| P1-44 | featuretools/computational_backends/feature_set_calculator.py:565 | #19 | `f.get_function()` is re-resolved inside the inner `for group in groups` loop although it depends only on `f`; hoisting it out of the group loop removes one lookup per (feature × group). | `                feature_func = f.get_function()` |
| P1-45 | featuretools/computational_backends/feature_set.py:44 | #7 | A four-line comment narrates a load-bearing protocol — "features do not support equality and so cannot be used as dictionary keys; the equality operator produces a new feature (always truthy)" — that nothing encodes. Callers who forget it (e.g. `synthesis/encode_features.py:159` `add = f == label`) get silent nonsense, not an error. | `        # Maps the unique name of each feature to the actual feature. This is necessary`<br>`        # because features do not support equality and so cannot be used as`<br>`        # dictionary keys.` |
| P1-46 | featuretools/computational_backends/feature_set.py:229 | #11 | `_get_use_previous`, `_get_where` (`:243`) and `_get_groupby` (`:250`) are the same isinstance-guard-else-sentinel shape three times, all existing only to feed `key_func` at `:165`. | `def _get_use_previous(` |
| P1-47 | featuretools/computational_backends/feature_set.py:169 | #24 | A class is turned into a string to be used as a grouping key; `f.__class__` itself is hashable and orderable via `__qualname__`. Grouping correctness now depends on `repr` formatting. | `                str(f.__class__),` |
| P1-48 | featuretools/computational_backends/utils.py:82 | #2 | `assert <non-empty string literal>` is always true — the intended error is unreachable and `datetime_round` falls off the end returning `None` for multi-unit frequencies. | `        assert "Frequency cannot have multiple temporal parameters"` |
| P1-49 | featuretools/computational_backends/utils.py:243 | #6 | `_validate_cutoff_time` renames columns on the caller's DataFrame with `inplace=True` (again at `:258`). A function documented as "Verify that the cutoff time is…" rewrites its argument. | `            cutoff_time.rename(`<br>`                columns={target_dataframe.ww.index: "instance_id"},`<br>`                inplace=True,` |
| P1-50 | featuretools/computational_backends/utils.py:153 | #6 | `create_client_and_cluster` deletes keys out of the caller's `dask_kwargs` dict (again at `:170`), so the caller's dict is silently different after the call — and `calculate_feature_matrix.py:830` then tests `"cluster" not in dask_kwargs` on the mutated object. | `            del dask_kwargs["diagnostics_port"]` |
| P1-51 | featuretools/computational_backends/utils.py:222 | #1 | The declared `CutoffTimeType` omits the tuple form that `calculate_feature_matrix.py:210` constructs and `_check_cutoff_time_type:305` branches on, and the `np.inf` float from `:191`. The one annotation in the module is wrong. | `CutoffTimeType = typing.Union[pd.DataFrame, str, datetime]` |
| P1-52 | featuretools/computational_backends/utils.py:215 | none | `import_or_none` returns `None` when the package is absent, so a missing `distributed` surfaces as `AttributeError: 'NoneType' object has no attribute 'Client'` rather than the actionable message `import_or_raise` exists to give. | `    distributed = import_or_none("distributed")`<br>`    Client = distributed.Client` |
| P1-53 | featuretools/synthesis/deep_feature_synthesis.py:399 | #18 | `_run_dfs` marks seven phases ("Step 1" … "Step 7", lines 399, 404, 434, 461, 467, 497, 519) as bare triple-quoted *expression statements* rather than comments — string literals evaluated and discarded, segmenting a 150-line method. | `        """`<br>`        Step 1 - Create identity features`<br>`        """` |
| P1-54 | featuretools/synthesis/deep_feature_synthesis.py:418 | #21 | The same five-line `allowed_paths` guard is written out four times in one method — `:418`, `:447`, `:481`, `:507` — always `if self.allowed_paths and tuple(new_path.dataframes()) not in self.allowed_paths: continue`. | `            if (`<br>`                self.allowed_paths`<br>`                and tuple(new_path.dataframes()) not in self.allowed_paths` |
| P1-55 | featuretools/synthesis/deep_feature_synthesis.py:409 | #11 | The backward-recursion loop (`:409`–`:432`) and the forward-recursion loop (`:472`–`:495`) are 24-line clones differing only in `get_backward_dataframes`/`get_forward_dataframes` and the loop variable name; `:442`–`:459` and `:502`–`:517` are a second such pair. | `        for b_dataframe_id, sub_relationship_path in backward_dataframes:` |
| P1-56 | featuretools/synthesis/deep_feature_synthesis.py:203 | #11 | Four consecutive `sorted([check_primitive(p, "<kind>", agg_dict, trans_dict) for p in xs])` blocks (`:203`, `:218`, `:232`, `:246`) differing only in the kind string and the source list — 54 lines that are one loop over four pairs. | `        self.agg_primitives = sorted(`<br>`            [`<br>`                check_primitive(` |
| P1-57 | featuretools/synthesis/deep_feature_synthesis.py:678 | #12 | `any(True for x in xs if pred(x))` reimplements `any(pred(x) for x in xs)` — seven occurrences in this module (`:678`, `:703`, `:725`, `:869`, `:879`, `:955`, `:1057`) plus `:343`. | `                if not any(`<br>`                    True for bf in matching_input if bf.number_output_features != 1`<br>`                ):` |
| P1-58 | featuretools/synthesis/deep_feature_synthesis.py:788 | none | The inner `for feat in deep_base_features:` loop's only statement is a `continue`, so it does nothing — the comment above (`:785`, "limits allowing direct features of agg_feats with where clauses") describes a filter that was never implemented. | `                for feat in deep_base_features:`<br>`                    if isinstance(feat, AggregationFeature) and feat.where is not None:`<br>`                        continue` |
| P1-59 | featuretools/synthesis/deep_feature_synthesis.py:751 | none | `all_direct_and_same_path_in_matching_input` is assigned only when the walrus at `:728` is truthy, but read at `:751` whenever `require_direct_input` is set and a groupby exists — reachable `UnboundLocalError` on the second and later `matching_input` iterations too, since neither name is reset per input. | `                    elif all_direct_and_same_path_in_matching_input:` |
| P1-60 | featuretools/synthesis/deep_feature_synthesis.py:928 | #12 | `functools.reduce(operator.iconcat, xs, [])` is a hand-rolled flatten of a list of lists; `list(itertools.chain.from_iterable(xs))` (or building the flat list directly in the comprehension above) is the idiom. | `        selected_features = functools.reduce(operator.iconcat, selected_features, [])` |
| P1-61 | featuretools/synthesis/deep_feature_synthesis.py:930 | #1 | `column_schemas` is variously a `list[ColumnSchema]`, the string `"all"`, or a `set()` sentinel, and the function compares it against `"all"` at `:932` and `:961` while iterating it at `:957`. One parameter, three unrelated types, no annotation. | `        column_schemas = column_schemas if column_schemas else set()` |
| P1-62 | featuretools/synthesis/deep_feature_synthesis.py:1246 | none | `check_primitive` binds `prim_dict`, `supertype`, `arg_name` and `s` inside two bare `if` blocks with no `else`; any `prim_type` outside the four literals raises `UnboundLocalError` at `:1265` instead of a diagnosable error. | `    if prim_type in ("transform", "groupby transform"):`<br>`        prim_dict = transform_primitive_dict` |
| P1-63 | featuretools/synthesis/deep_feature_synthesis.py:1324 | none | Truthiness-guarded cache read: `IdentityFeature.get_depth` returns `0` (`feature_base.py:492`), so depth-0 features never hit the cache and are recomputed every lookup. `is not None` is the correct guard. | `    if cached_depth := feature_cache.get(CacheType.DEPTH, hash_key):` |
| P1-64 | featuretools/synthesis/deep_feature_synthesis.py:1323 | none | Cache key is an unseparated concatenation of two free-form strings plus a list `repr`, so `(name="ab", df="c")` and `(name="a", df="bc")` collide into one entry. | `    hash_key = hash(f"{feature.get_name()}{feature.dataframe_name}{stop_at}")` |
| P1-65 | featuretools/synthesis/deep_feature_synthesis.py:160 | #9 | A constructor reaches out and mutates a package-global singleton — clearing every cached entry and force-enabling caching for the whole process — as a side effect of building a `DeepFeatureSynthesis` object. | `        feature_cache.clear_all()`<br>`        feature_cache.enabled = True` |
| P1-66 | featuretools/synthesis/deep_feature_synthesis.py:1079 | #14 | `_check_if_stacking_is_prohibited` and `_check_if_stacking_is_permitted` (`:1104`) share the clump `(f_primitive, primitive_class, primitive_stack_on_self, tuple_primitive_stack_on*)`, unpacked from one `primitive` object at `:1131`–`:1140` and re-passed positionally at `:1148` and `:1159`. | `def _check_if_stacking_is_prohibited(`<br>`    feature: FeatureBase,`<br>`    f_primitive: PrimitiveBase,` |
| P1-67 | featuretools/synthesis/dfs.py:12 | #14 | `dfs` has 31 parameters, of which 13 are forwarded verbatim to `DeepFeatureSynthesis(...)` (`:245`–`:261`) and 13 to `calculate_feature_matrix(...)` (`:283`–`:298`). Two callee signatures re-typed as one flat argument list. | `def dfs(`<br>`    dataframes=None,`<br>`    relationships=None,` |
| P1-68 | featuretools/synthesis/dfs.py:217 | none | The documented return order is reversed: the docstring says "list[FeatureBase], pd.DataFrame", the code returns `feature_matrix, features` (`:299`) — and the example at `:231` unpacks it the code's way. Docs and example disagree inside one docstring. | `        list[:class:`.FeatureBase`], pd.DataFrame:`<br>`            The list of generated feature defintions, and the feature matrix.` |
| P1-69 | featuretools/synthesis/get_valid_primitives.py:94 | none | The public return is `list(set(...))` twice over — iteration order of a set of classes is not stable across runs, so a documented "list of valid primitives" is nondeterministically ordered. | `    used_agg_prims = set(agg_primitives).difference(set(agg_unused))`<br>`    used_trans_prims = set(trans_primitives).difference(set(trans_unused))`<br>`    return list(used_agg_prims), list(used_trans_prims)` |
| P1-70 | featuretools/synthesis/get_valid_primitives.py:80 | #1 | Public API forwards an opaque `**dfs_kwargs` into `DeepFeatureSynthesis`; the docstring at `:37` has to state the exclusions in prose ("Should not include `max_depth`, `agg_primitives`, or `trans_primitives`") because the signature cannot. | `        **dfs_kwargs,` |
| P1-71 | featuretools/__init__.py:9 | none | The test package is imported into the shipped library's namespace at import time, pulling test modules (and their imports) into every user's process. | `from featuretools import tests` |
| P1-72 | featuretools/__init__.py:58 | #24 | Submodules are grafted into the package namespace by writing a string-built key into global `sys.modules`, so `featuretools.<anything>` may or may not exist depending on what is pip-installed. No static tool can see these names. | `        sys.modules["featuretools." + entry_point.name] = entry_point.load()` |
| P1-73 | featuretools/__init__.py:40 | #24 | Importing the package loads and *calls* arbitrary third-party callables from two entry-point namespaces, with `except Exception: pass` (`:45`, `:52`) hiding every failure. The two loops (`:40`, `:47`) are also a clone pair differing only in the argument passed. | `for entry_point in pkg_resources.iter_entry_points("featuretools_initialize"):`<br>`    try:`<br>`        method = entry_point.load()` |
| P1-74 | featuretools/__init__.py:4 | #26 | Six star imports (`:4`, `:6`, `:8`, `:11`, plus the chain through the `*/api.py` modules) define most of the top-level public API, and no `__all__` exists anywhere in the repo. Combined with P1-72 there is no literal statement of what `import featuretools` provides. | `from featuretools.entityset.api import *` |
| P1-75 | featuretools/primitives/__init__.py:69 | #24 | The package's own `globals()` is captured as a mutable registry and written at `:85`, so `featuretools.primitives.<Name>` exists only as an import side effect — invisible to readers, linters and type checkers. | `                scope = globals()` |
| P1-76 | featuretools/primitives/__init__.py:60 | #24 | Third-party primitives are discovered by `dir()` + `getattr` over an arbitrary loaded module — symbol harvesting with no declared contract. | `        for key in dir(loaded):`<br>`            primitive = getattr(loaded, key, None)` |
| P1-77 | featuretools/primitives/__init__.py:50 | #26 | The public primitive namespace is assembled by scanning setuptools entry points at import time rather than declared; with the star import at `:8` the module has no enumerable API. | `    for entry_point in pkg_resources.iter_entry_points("featuretools_primitives"):` |
| P1-78 | featuretools/primitives/__init__.py:1 | #29 | The heaviest entry point in the package — it executes third-party plugin code at import (`:88`) — has no module docstring and no cost declaration; the file opens on a lint suppression. | `# flake8: noqa`<br>`import inspect` |
| P1-79 | featuretools/primitives/utils.py:26 | #24 | The primitive registry is rebuilt on every call by reflecting over the package namespace with `dir` + `getattr`, so the result depends on import order and on whatever `_load_primitives` injected. | `    for attribute_string in dir(featuretools.primitives):`<br>`        attribute = getattr(featuretools.primitives, attribute_string)` |
| P1-80 | featuretools/primitives/utils.py:28 | #8 | The "is this a real primitive class" predicate (`isclass` + `issubclass` + truthy `.name`) is spelled out at four sites with slightly different exclusion rules: here, `utils.py:49`, `utils.py:347`, `primitives/__init__.py:63`. | `        if isclass(attribute):`<br>`            if issubclass(attribute, primitive_kind) and attribute.name:` |
| P1-81 | featuretools/primitives/utils.py:34 | #13 | `get_aggregation_primitives` and `get_transform_primitives` (`:39`) are public API whose entire content is choosing which base class to hand `_get_primitives`. | `def get_aggregation_primitives():`<br>`    return _get_primitives(featuretools.primitives.AggregationPrimitive)` |
| P1-82 | featuretools/primitives/utils.py:245 | #6 | `_get_summary_primitives` answers a question about a primitive's behaviour by grepping its own source text with `getsource` — a substring match on source is not a fact about behaviour, and it fails outright for primitives loaded from a zip or built dynamically. | `            "self.number_output_features =" in getsource(prim.__init__)` |
| P1-83 | featuretools/primitives/utils.py:206 | #1 | Annotation is both weak and wrong: bare `List` for the parameter, and the declared `Dict[str, int]` return is actually a dict of three nested dicts (see `:257`–`:268`). | `def _get_summary_primitives(primitives: List) -> Dict[str, int]:` |
| P1-84 | featuretools/primitives/utils.py:271 | #6 | Named `_check_input_types` and returns `None`; it actually mutates three caller-owned accumulator sets passed as bare `set` out-parameters. | `def _check_input_types(`<br>`    input_types: List[ColumnSchema],`<br>`    log_in_type_checks: set,` |
| P1-85 | featuretools/primitives/utils.py:297 | #12 | A conditional *expression* used as a statement to select which `append` to call, where `return_type.append(x if cond else None)` is the idiom. | `        return_type.append(`<br>`            str(primitive.return_type),`<br>`        ) if primitive.return_type is not None else return_type.append(None)` |
| P1-86 | featuretools/primitives/utils.py:330 | #12 | Hand-rolled `startswith`/`endswith` via slicing; `filename[0]` also raises `IndexError` on an empty basename. | `    if filename[:2] == "__" or filename[0] == "." or filename[-3:] != ".py":` |
| P1-87 | featuretools/primitives/utils.py:340 | #24 | Loads and executes a module from an arbitrary filesystem path, then harvests classes out of `vars(module)` by name at `:345` — the third distinct symbol-discovery mechanism in this one file. | `    spec = importlib.util.spec_from_file_location(module, filepath)`<br>`    module = importlib.util.module_from_spec(spec)`<br>`    spec.loader.exec_module(module)` |
| P1-88 | featuretools/primitives/utils.py:396 | #24 | Deserialization resolves a class from a `(name, top-level-module)` string pair by walking `__subclasses__` at `:418` — identifier lookup by string, with a runtime error as the only failure mode. | `        class_name = primitive_dict["type"]`<br>`        module_name = primitive_dict["module"]` |
| P1-89 | featuretools/primitives/utils.py:12 | #26 | Circular import: `primitives/__init__.py:9` imports this module, which imports back from `featuretools.primitives`. It only resolves because the star import at `__init__.py:8` runs first — a load-order dependency nothing states. | `from featuretools.primitives import NumberOfCommonWords` |
| P1-90 | featuretools/primitives/utils.py:1 | #29 | Public-API module (`list_primitives`, `summarize_primitives`, primitive serialization) with no module docstring. | `import importlib.util`<br>`import os` |
| P1-91 | featuretools/primitives/options_utils.py:11 | #26 | The option-key schema is a constant table hidden inside a function and rebuilt on every `_init_option_dict` call (`:177`) instead of being a declared module-level mapping. | `def _get_primitive_options():`<br>`    # all possible option keys: function that verifies value type`<br>`    return {` |
| P1-92 | featuretools/primitives/options_utils.py:56 | #23 | ~73 lines, four levels of nesting, three collections mutated in flight (`global_ignore_dataframes`, `global_ignore_columns`, per-option dicts) — worst complexity in the primitives scope. Its five parameters are also entirely unannotated despite carrying nested `dict[str, set[str]]` shapes. | `def generate_all_primitive_options(`<br>`    all_primitives,`<br>`    primitive_options,` |
| P1-93 | featuretools/primitives/options_utils.py:71 | #18 | Six labeled phase comments narrate one function body (`:71`, `:85`, `:98`, `:114`, `:117`, `:121`); more of the same at `:132`, `:167`, `:196`. | `            # Reconcile global options with individually-specified options` |
| P1-94 | featuretools/primitives/options_utils.py:73 | #8 | A "primitive key" is variously a class, an instance, or a lowercase name string, and the same three-way normalization is re-done at `:80`, `:140`, `:163` instead of being one key type. | `        if primitive in primitive_options and primitive.name in primitive_options:` |
| P1-95 | featuretools/primitives/options_utils.py:149 | #6 | A bare `assert` is load-bearing validation of user-supplied options on a public API path — it vanishes under `python -O`, leaving a downstream `IndexError`. | `                assert (`<br>`                    len(primitive.input_types[0]) == len(options)` |
| P1-96 | featuretools/primitives/options_utils.py:204 | #14 | The `(options, groupby)` pair recurs across four signatures — `:204`, `:240`, `:273`, `:281` — as two loose args for one concept ("which option view to use"). | `def column_filter(f, options, groupby=False):` |
| P1-97 | featuretools/primitives/options_utils.py:209 | #8 | The `groupby` → option-key-name mapping is written as four ternaries here and then re-derived with hardcoded strings inside `should_ignore_dataframe` (`:244`–`:256`). | `    include_cols = "include_groupby_columns" if groupby else "include_columns"` |
| P1-98 | featuretools/primitives/options_utils.py:270 | #12 | A list comprehension materialized inside `any()`, defeating short-circuit; same with `all([...])` at `:27`, `:286`, `:299`. | `    return any([should_ignore_dataframe(option) for option in options])` |
| P1-99 | featuretools/primitives/options_utils.py:286 | #12 | `if all(...): return True else: return False` — `return all(...)`. Duplicated verbatim in the sibling closure at `:298`–`:302`. | `            if all(`<br>`                [`<br>`                    column_filter(m, option, groupby)` |
| P1-100 | featuretools/primitives/options_utils.py:1 | #29 | The module implementing the entire primitive-options contract for DFS has no docstring; the option-key semantics exist only in the inline comment at `:12`. | `import logging`<br>`import warnings` |
| P1-101 | featuretools/primitives/base/primitive_base.py:52 | #1 | `__call__` on the base class of every primitive takes fully opaque `*args, **kwargs`, so no primitive's call signature is discoverable from types. | `    def __call__(self, *args, **kwargs):` |
| P1-102 | featuretools/primitives/base/primitive_base.py:55 | #12 | Lazy memoization hand-rolled as `try` / `except AttributeError`; an `AttributeError` raised *inside* `self._method` silently re-enters `get_function()` and re-runs the call. `functools.cached_property` is the idiom. | `            return self._method(*series_args, **kwargs)`<br>`        except AttributeError:`<br>`            self._method = self.get_function()` |
| P1-103 | featuretools/primitives/base/primitive_base.py:94 | #24 | Introspects the class's own signature and `getattr`s each parameter name off the instance, asserting the attribute exists — a name-based contract enforced only at runtime, by `assert`. | `        args = signature(self.__class__).parameters.items()` |
| P1-104 | featuretools/primitives/base/primitive_base.py:74 | #22 | `get_filepath` uses no `self` state at all — it reads the module-global `config`. A free function wearing a method's clothes; same for `flatten_nested_input_types` at `:154`, a `@staticmethod` on a class it never uses. | `    def get_filepath(self, filename):`<br>`        return os.path.join(config.get("primitive_data_folder"), filename)` |
| P1-105 | featuretools/primitives/base/primitive_base.py:111 | #23 | `get_description` nests four levels over `template` being `None`/`str`/`list`, duplicating the same `.format(*..., nth_slice=...)` call in two branches with a bounds check spliced between them. | `    def get_description(`<br>`        self,`<br>`        input_column_descriptions,` |
| P1-106 | featuretools/primitives/base/transform_primitive_base.py:19 | #11 | `generate_names` here and in `aggregation_primitive_base.py:23` are the same three-line body (`n = self.number_output_features`; call `generate_name`; suffix `[i]`), differing only in the argument list. | `    def generate_names(self, base_feature_names):`<br>`        n = self.number_output_features`<br>`        base_name = self.generate_name(base_feature_names)` |
| P1-107 | featuretools/config_init.py:34 | #24 | Log level resolved by `getattr` on the `logging` module from an environment-variable string; a typo in `FEATURETOOLS_LOG_LEVEL` becomes an `AttributeError` at *import* time. | `        LEVEL = getattr(logging, level.upper())` |
| P1-108 | featuretools/config_init.py:47 | #9 | Importing `featuretools` reconfigures process-global logging as a side effect: it strips existing handlers (`:38`) and sets `propagate = False` on three loggers the host application may own. | `initialize_logging()` |
| P1-109 | featuretools/config_init.py:16 | none | The loop variable `logger` is the logger *name* string, then rebound at `:35` to a `Logger` object — one name, two types, in one function, shadowing the module-level convention used everywhere else. | `    for logger_env, logger in logger_env_vars.items():` |
| P1-110 | featuretools/config_init.py:68 | #1 | Public config mutator takes an unannotated `values` and blind-`update`s the backing dict with no key validation, although `get` (`:62`) raises `KeyError` on unknown keys. | `    def set(self, values):`<br>`        self._data.update(values)` |
| P1-111 | featuretools/utils/recommend_primitives.py:74 | #9 | A module-level mutable list is the default argument and `:113` mutates it in place with `+=`. After one call with `include_time_series_primitives=False`, the time-series primitives are permanently excluded for every later call in the process — including calls that ask for them. Real bug. | `    excluded_primitives: List[str] = DEFAULT_EXCLUDED_PRIMITIVES,` |
| P1-112 | featuretools/utils/recommend_primitives.py:113 | #9 | The in-place `+=` that corrupts the shared default in P1-111; `excluded_primitives = excluded_primitives + TIME_SERIES_PRIMITIVES` fixes it. | `        excluded_primitives += TIME_SERIES_PRIMITIVES` |
| P1-113 | featuretools/utils/recommend_primitives.py:193 | #19 | `calculate_feature_matrix` — a full pipeline pass over the entityset — is invoked once per feature inside the loop over `features`; one batched call over the feature list replaces N passes. | `                matrix = calculate_feature_matrix([f], entityset)` |
| P1-114 | featuretools/utils/gen_utils.py:11 | #1 | Public exported helper (`utils/api.py:3`) whose entire signature is opaque `**kwargs`; the two keys it actually sets are buried in the body. | `def make_tqdm_iterator(**kwargs):` |
| P1-115 | featuretools/utils/gen_utils.py:35 | #12 | Hand-rolled recursive re-yield; `yield from find_descendents(sub)` is the stdlib idiom. | `    for sub in cls.__subclasses__():`<br>`        for c in find_descendents(sub):`<br>`            yield c` |
| P1-116 | featuretools/utils/entry_point.py:21 | #24 | The loop variable shadows the enclosing decorator function `entry_point` (`:8`) and is then rebound to the loaded object — three meanings for one name across nested scopes. | `            for entry_point in pkg_resources.iter_entry_points(name):`<br>`                entry_point = entry_point.load()` |
| P1-117 | featuretools/utils/entry_point.py:12 | #7 | A copy-paste placeholder docstring sits on the wrapper that implements the entry-point protocol; the real contract (`ep.on_call` / `on_error` / `on_return`) is documented nowhere in the repo. | `            """function_wrapper of greeting"""` |
| P1-118 | featuretools/utils/s3_utils.py:8 | #12 | Rebinds the builtin name `open` to smart_open's `open`, then uses that same name for a plain local-file open at `:11`/`:14`, so local paths silently route through smart_open. Duplicated verbatim at `:20`. | `    open = import_or_raise("smart_open", SMART_OPEN_ERR_MSG).open` |
| P1-119 | featuretools/utils/s3_utils.py:7 | #11 | `use_smartopen_es` and `use_smartopen_features` (`:19`) are the same read/write skeleton with different payload codecs, both controlled by a `read=True` boolean flag rather than being two named functions. | `def use_smartopen_es(file_path, path, transport_params=None, read=True):` |
| P1-120 | featuretools/utils/schema_utils.py:11 | #25 | The parameter named `cls` is never a class: for `"entityset"` it is a dict (`cls.get(...)`), for `"features"` an object with `.features_dict`. Two incompatible shapes behind one untyped, actively misleading name. | `def check_schema_version(cls, cls_type):` |
| P1-121 | featuretools/utils/wrangle.py:62 | #6 | The `re.match` result is used without a `None` check, so a malformed timedelta string raises `AttributeError: 'NoneType' object has no attribute 'groups'` instead of the `ValueError` the surrounding code (`:38`, `:70`) promises. | `        match = re.match(pattern, td)`<br>`        value, unit = match.groups()` |
| P1-122 | featuretools/utils/wrangle.py:47 | #24 | String-keyed attribute lookup `td.__dict__["n"]` used as a fallback for `td.n` — the same attribute read through two mechanisms in one branch. | `                value = td.__dict__["n"]` |
| P1-123 | featuretools/utils/wrangle.py:124 | #8 | Three one-line `str` predicates over one "what kind of path is this" concept (`_is_s3` `:124`, `_is_url` `:132`, `_is_local_tar` `:140`), each taking a bare `string`. A path-kind type collapses them and the four dispatch sites that consume them. | `def _is_s3(string):` |
| P1-124 | featuretools/utils/wrangle.py:12 | #23 | `_check_timedelta` is the module's complexity peak: six isinstance branches, three nested `try`/`except`, four separate `times` constructions, all returning differently-configured `Timedelta`s. | `def _check_timedelta(td):` |
| P1-125 | featuretools/utils/time_utils.py:69 | #9 | A constant conversion table is rebuilt on every call of a function used inside primitive hot paths (six importers of `convert_time_units`); it belongs at module scope. | `    unit_divs = {` |
| P1-126 | featuretools/utils/time_utils.py:34 | #7 | The docstring (`:15`–`:23`) states three mutually exclusive valid argument combinations, but only the all-three case is checked; supplying just `cutoffs` reaches `pd.date_range(periods=None, freq=None)` and fails opaquely. | `    if window_size is not None and num_windows is not None and start is not None:` |
| P1-127 | featuretools/utils/trie.py:60 | #6 | `get_node` is a mutator: on a miss it constructs and inserts a sub-trie. A `get_*` name that grows the structure — and the class docstring at `:20` advertises it as read-like ("Getting a key that has not been set returns the default value"). | `    def get_node(self, path):` |
| P1-128 | featuretools/utils/utils_info.py:56 | #6 | The docstring says "as a dict"; the function returns a list of 2-tuples, and both callers (`:35`, `:49`) rely on the list form. | `    "Returns system information as a dict"` |
| P1-129 | featuretools/utils/utils_info.py:11 | #9 | A module-level mutable list is the de-facto config for `show_info()`; any importer can append to it and change the output globally. | `deps = [` |
| P1-130 | featuretools/utils/plot_utils.py:52 | #7 | `save_graph` silently requires that `format_` was derived from `to_file` by `get_graphviz_format`; a `None` `format_` (the falsy-`to_file` branch at `:48`) makes `len(None)` raise. The four call sites (`entityset.py:1358`/`:1404`, `feature_visualizer.py:52`/`:134`) each re-establish the ordering by hand. | `    offset = len(format_) + 1  # Add 1 for the dot` |
| P1-131 | featuretools/utils/plot_utils.py:4 | #6 | A `check_*` predicate-sounding function shells out to the graphviz backend (a subprocess render at `:13`) and returns the imported module. | `def check_graphviz():` |
| P1-132 | featuretools/selection/selection.py:5 | #14 | `(feature_matrix, features)` is the same clump across all four public entry points (`:5`, `:31`, `:70`, `:102`) plus `_apply_feature_selection` (`:203`) — and every one is untyped. | `def remove_low_information_features(feature_matrix, features=None):` |
| P1-133 | featuretools/selection/selection.py:16 | #11 | `remove_low_information_features` is the one selector that does not route through `_apply_feature_selection`, so it silently skips the multi-output-feature handling the other three get (`:210`–`:219`) and returns a differently shaped feature list. | `    keep = [`<br>`        c`<br>`        for c in feature_matrix` |
| P1-134 | featuretools/selection/selection.py:174 | #11 | The bool/`Int64` → `float64` coercion block at `:174`–`:178` is duplicated verbatim at `:185`–`:189` for the other operand. | `        if pd.api.types.is_bool_dtype(more_complex_col) or isinstance(` |
| P1-135 | featuretools/selection/selection.py:185 | #19 | `less_complex_col` is re-selected and re-`astype`d on every outer iteration, so each column is converted O(n) times inside an O(n²) loop; converting `fm_to_check` once before the loops removes it. | `            if pd.api.types.is_bool_dtype(less_complex_col) or isinstance(` |
| P1-136 | featuretools/selection/selection.py:153 | #6 | User-input validation via bare `assert` in a public API — it vanishes under `python -O`, and the sibling validation nine lines up (`:144`) correctly raises `ValueError`. | `            assert (`<br>`                f_name in feature_matrix.columns`<br>`            ), "feature named {} is not in feature matrix".format(f_name)` |
| P1-137 | featuretools/selection/api.py:2 | #26 | Star re-export of a module with no `__all__`, so `featuretools.selection`'s public surface is whatever `selection.py` happens to bind — including `pd`, `Boolean`, `BooleanNullable` and the private `_apply_feature_selection`. Same pattern in the `demo/api.py` chain and `utils/__init__.py:2`. | `from featuretools.selection.selection import *` |
| P1-138 | featuretools/demo/flight.py:70 | #9 | A data-loading function permanently mutates global pandas display config for the whole process, undocumented in its docstring. | `    pd.options.display.max_columns = 200` |
| P1-139 | featuretools/demo/flight.py:63 | #6 | `load_flight` downloads over HTTP and prints to stdout; the docstring (`:19`–`:33`) documents neither, and `verbose=False` does not suppress the print. Same shape at `demo/weather.py:17`. | `    print("Downloading data ...")` |
| P1-140 | featuretools/demo/flight.py:323 | #11 | The local `convert` helper is byte-for-byte the body of `utils/gen_utils.py:69 camel_and_title_to_snake`, which is already imported by two other packages. | `    s1 = re.sub("(.)([A-Z][a-z]+)", r"\1_\2", name)`<br>`    return re.sub("([a-z0-9])([A-Z])", r"\1_\2", s1).lower()` |
| P1-141 | featuretools/demo/flight.py:163 | #18 | `_clean_data` is eight labeled phases in one function ("# Make column names snake case", "# Combine strings like…", "# Parse combined string as a date", "# Create a time index…", …), each an extractable step. | `def _clean_data(data):`<br>`    # Make column names snake case` |
| P1-142 | featuretools/demo/flight.py:181 | #12 | `.apply(lambda x: str(x))` is `.astype(str)`; the same lambda recurs at `:218`. | `    clean_data["scheduled_dep_time"] = clean_data["scheduled_dep_time"].apply(`<br>`        lambda x: str(x),`<br>`    ) + clean_data["flight_date"].astype("str")` |
| P1-143 | featuretools/demo/mock_customer.py:20 | #9 | A loader reseeds numpy's global RNG as a side effect, altering randomness for the entire process; the one-line docstring (`:18`) claims only "Return dataframes of mock customer data". | `    random.seed(random_seed)` |
| P1-144 | featuretools/demo/mock_customer.py:9 | #6 | Two boolean flags produce three unrelated return types — `pd.DataFrame` (`:70`), `EntitySet` (`:114`), `dict[str, DataFrame]` (`:116`) — and `return_single_table=True, return_entityset=True` silently ignores the second. | `def load_mock_customer(` |
| P1-145 | featuretools/demo/retail.py:61 | #11 | Two 60-character URL literals differing only by `.gz`, plus a duplicated `pd.read_csv(..., nrows=nrows, parse_dates=["order_date"])` in the `try` and the `except` (`:71`, `:74`). One base URL plus a suffix loop covers both. | `    csv_s3_gz = (` |
| P1-146 | featuretools/feature_discovery/feature_discovery.py:350 | #28 | The docstring example imports `lite_dfs`, which does not exist anywhere in the repo (verified by grep — only these docstring lines match). The real function is `generate_features_from_primitives`; the documented usage fails at import. | `            from featuretools.feature_discovery.feature_discovery import lite_dfs` |
| P1-147 | featuretools/feature_discovery/feature_discovery.py:138 | #28 | `get_matching_columns` does not exist in the repo, yet the docstrings for `_get_matching_features` and `_features_from_primitive` (`:195`) both import it in their examples. | `            from featuretools.feature_discovery.feature_discovery import get_matching_columns` |
| P1-148 | featuretools/feature_discovery/feature_discovery.py:302 | #6 | `_check_inputs` iterates `input_features` to validate it, exhausting a generator argument before `generate_features_from_primitives:369` copies it — the caller then silently gets zero features. A validator that consumes its input. | `    if not isinstance(input_features, Iterable):`<br>`        raise ValueError("input_features must be an iterable of LiteFeature objects")` |
| P1-149 | featuretools/feature_discovery/feature_discovery.py:311 | #12 | `isinstance` against `typing.List` rather than `list` — works only by accident of `typing`'s deprecated runtime aliases, and rejects tuples that would work fine. | `    if not isinstance(primitives, List):` |
| P1-150 | featuretools/feature_discovery/FeatureCollection.py:39 | #3 | Guard already implied by the callee's contract: `sort_features` (`:26`) opens with the identical `if not self.sorted`. | `            if not self.sorted:`<br>`                self.sort_features()` |
| P1-151 | featuretools/feature_discovery/FeatureCollection.py:61 | #9 | `reindex` is where six instance attributes first come into existence — none declared in `__init__` — so every getter at `:100`–`:120` raises `AttributeError` until `reindex()` has been called. `self.indexed` tracks this and is never checked. | `    def reindex(self) -> FeatureCollection:`<br>`        self.by_logical_type: Dict[` |
| P1-152 | featuretools/feature_discovery/FeatureCollection.py:68 | #26 | `by_depth` is built in `reindex` and never populated or read anywhere — a dead index in the middle of a live one. | `        self.by_depth: Dict[int, Set[LiteFeature]] = {}` |
| P1-153 | featuretools/feature_discovery/FeatureCollection.py:100 | #21 | Five getters (`:100`, `:103`, `:106`, `:113`, `:119`) are the same `self.by_X.get(k, <empty>)` expression, and `get_by_origin_feature_name` (`:109`) is the same thing spelled in two statements. | `        return self.by_logical_type.get(logical_type, set())` |
| P1-154 | featuretools/feature_discovery/FeatureCollection.py:122 | #11 | `flatten_features` and `flatten_primitives` (`:133`) are the same recursive walk over `base_features` with a `setdefault` accumulator; only the key/value extraction differs. | `    def flatten_features(self) -> Dict[str, LiteFeature]:` |
| P1-155 | featuretools/feature_discovery/LiteFeature.py:203 | #9 | Mutable default `[]` on a `@staticmethod` that then sorts it in place at `:218` — the shared default list is mutated, and any caller's list is silently reordered as a side effect of computing a hash. | `        base_features: List[LiteFeature] = [],` |
| P1-156 | featuretools/feature_discovery/LiteFeature.py:263 | #6 | The recursive call drops `deep`, so `get_dependencies(deep=True)` only reaches two levels; `get_origin_features` (`:270`) therefore misses the origins of depth-3+ features. Real bug. | `                dependencies = f.get_dependencies()` |
| P1-157 | featuretools/feature_discovery/LiteFeature.py:24 | #26 | `dataclasses.field(default_factory=...)` used on a class that is not a dataclass (`@total_ordering`, hand-written `__init__`), so these class attributes hold `Field` sentinel objects rather than a set/list. Dead and actively misleading; same at `:26`. | `    _tags: Set[str] = field(default_factory=set)` |
| P1-158 | featuretools/feature_discovery/LiteFeature.py:106 | #21 | Ten property setters (`:106`, `:123`, `:131`, `:139`, `:147`, `:155`, `:163`, `:171`, `:179`, `:195`) are the identical two-line `raise AttributeError("<x> is immutable")`; one read-only descriptor collapses ~30 lines. | `    @name.setter`<br>`    def name(self, _):`<br>`        raise AttributeError("name is immutable")` |
| P1-159 | featuretools/feature_discovery/LiteFeature.py:233 | #1 | `__eq__`/`__lt__`/`__ne__` are annotated to take `LiteFeature` and dereference `other._id` unguarded, so comparing to any other object raises `AttributeError` instead of returning `NotImplemented`. | `    def __eq__(self, other: LiteFeature):`<br>`        return self._id == other._id` |
| P1-160 | featuretools/feature_discovery/utils.py:16 | #9 | Two eager, expensive registry scans run at import time, and `primitives_map` is then referenced nowhere in the repo (grep: this line is the only hit). | `primitives_map = get_all_primitives()` |
| P1-161 | featuretools/feature_discovery/utils.py:50 | #9 | Unbounded `lru_cache` keyed on live `PrimitiveBase` instances — the cache holds a strong reference to every primitive object ever hashed for the process lifetime, and mutating a primitive after caching yields a stale hash. | `@lru_cache(maxsize=None)` |
| P1-162 | featuretools/feature_discovery/utils.py:74 | #3 | Reads `primitive.input_types[0]` with no `None` check, while `feature_discovery.py:97` asserts that exact field can be `None`. One of the two beliefs is wrong. | `    return_type = primitive.input_types[0]` |
| P1-163 | featuretools/feature_discovery/convertors.py:34 | #12 | Hand-rolled double-comprehension flatten, duplicated at `:88`–`:92` in the same file — while `feature_discovery/utils.py:80 flatten_list` (already imported by the sibling module) and `itertools.chain.from_iterable` both exist. | `        base_features = [`<br>`            feature`<br>`            for feature_list in [rfunc(x) for x in fb.base_features]` |
| P1-164 | featuretools/feature_discovery/convertors.py:33 | #25 | Three separate recursive closures across two modules are all named `rfunc` (here, `:165`, and `FeatureCollection.py:125`/`:136`) — a name carrying zero information about what is being recursed. | `    def rfunc(fb: FeatureBase) -> List[LiteFeature]:` |
| P1-165 | featuretools/feature_discovery/convertors.py:150 | #23 | `_convert_feature_to_featurebase` nests two mutually recursive closures (`get_base_features` calls `rfunc`, defined 15 lines below it) sharing a captured `cache`; the mutual recursion is invisible from either definition. | `    def get_base_features(` |
| P1-166 | featuretools/feature_discovery/__init__.py:1 | #26 | Empty `__init__.py` while every sibling package (`utils`, `selection`, `demo`) uses the `__init__` → `api.py` re-export convention; combined with the PascalCase module filenames (`LiteFeature.py`, `FeatureCollection.py`) the package's public surface is unstated. | *(file is 0 bytes)* |
| P1-167 | featuretools/utils/common_tld_utils.py:2 | #29 | A 250-entry public data table with no module docstring and no source or refresh provenance; the ordering invariant it depends on ("put longer TLDs first") is stated in a comment and enforced by nothing. | `COMMON_TLDS = [` |
| P1-168 | featuretools/entityset/timedelta.py:171 | #2 | The operands of `in` are reversed relative to the correct sibling at `:104`. `_Observations` is the one-character string `"o"`, so this is a substring test against a string, not membership in a unit collection — it happens to work only because no unit key is a proper substring of `"o"`, and it answers wrongly for an empty unit. | `for unit in self.get_units():`<br>`    if unit in self._Observations:` |
| P1-169 | featuretools/entityset/timedelta.py:113 | #6 | `check_value` checks nothing — it assigns `self.times` on all three branches and performs a lazy import; `__init__` depends on it running before `fix_units()`. | `def check_value(self, value, unit):` |
| P1-170 | featuretools/entityset/timedelta.py:48 | #1 | Documented three-unrelated-types parameter: `value` is a float, a str, or a dict, and `delta_obj` is either a `pd.Timedelta` or a `pd.DateOffset`; `get_value()` (`:92`) then returns either a scalar or the whole dict depending on internal state. | `value (float, str, dict) : Value of timedelta, string providing`<br>`    both unit and value, or a dictionary of units and times.` |
| P1-171 | featuretools/entityset/timedelta.py:73 | #12 | Index-based parallel-list walk that is `dict(zip(dict_units, dict_values))`; its mirror at `:154`–`:163` manually appends into two lists instead of `list(self.times.keys())` / `.values()`. | `all_units = dict()`<br>`for i in range(len(dict_units)):`<br>`    all_units[dict_units[i]] = dict_values[i]` |
| P1-172 | featuretools/entityset/timedelta.py:177 | #12 | Three consecutive hand-rolled builtins: `is_absolute` is `all(...)`, `has_no_observations` is `not any(...)`, and `has_multiple_units` is `len(...) > 1` inflated into an if/else returning literals. Callers then write `... is False` (`:107`, `:142`). | `def has_multiple_units(self):`<br>`    if len(self.get_units()) > 1:`<br>`        return True` |
| P1-173 | featuretools/entityset/timedelta.py:78 | #22 | Two classmethods that touch neither `cls` nor instance state — pure `str → str` helpers living in a class for no reason. | `@classmethod`<br>`def make_singular(cls, s):` |
| P1-174 | featuretools/entityset/relationship.py:80 | #21 | Six parent/child mirror-pairs across one class, each differing only by the token `parent`/`child`: `parent_dataframe`/`child_dataframe` (`:81`/`:88`), `parent_column`/`child_column` (`:91`/`:98`), `parent_name`/`child_name` (`:101`/`:114`, including the identical `"%s[%s]"` format). | `return self.entityset[self._parent_dataframe_name]` |
| P1-175 | featuretools/entityset/relationship.py:8 | #8 | The same four bare `str` names are the class's entire identity and re-appear as one group in `__init__`, `from_dictionary` (`:42`), `__eq__` (`:63`), `__hash__` (`:71`) and `to_dictionary` (`:117`). `from_dictionary` also reorders them when forwarding — a swap no type can catch. | `return cls(es, parent_dataframe, parent_column, child_dataframe, child_column)` |
| P1-176 | featuretools/entityset/relationship.py:124 | #19 | `_is_unique` does a full `get_forward_relationships` scan plus a list comprehension per call, and is invoked once per relationship from `_direction_name` inside the `RelationshipPath.name` comprehension (`:147`–`:150`) — quadratic in relationship count on the path-naming path. | `relationships = es.get_forward_relationships(self._child_dataframe_name)` |
| P1-177 | featuretools/entityset/relationship.py:192 | #12 | `__ne__` reimplements the Python 3 default. Defining `__eq__` here without `__hash__` also silently makes `RelationshipPath` unhashable. | `def __ne__(self, other):`<br>`    return not self == other` |
| P1-178 | featuretools/entityset/deserialize.py:40 | #16 | `kwargs["filename"]` is set for one parquet/pandas dataframe and never cleared, so every later `read_woodwork_table` in the same loop is handed the *previous* dataframe's filename. | `if format == "parquet" and df["loading_info"]["table_type"] == "pandas":`<br>`    kwargs["filename"] = df["name"] + ".parquet"` |
| P1-179 | featuretools/entityset/deserialize.py:86 | #6 | `empty_dataframe` mutates its caller-owned argument: `tags` aliases `col["semantic_tags"]` inside the passed-in description dict and `.remove` edits it in place. The `elif` also means a column tagged both `index` and `time_index` keeps the latter. | `if "index" in tags:`<br>`    tags.remove("index")`<br>`elif "time_index" in tags:` |
| P1-180 | featuretools/entityset/deserialize.py:144 | #7 | A security contract lives only as prose in a docstring — there is no untrusted-source parameter, no opt-in flag, nothing in the signature that makes the caller acknowledge it. | `NOTE: Never attempt to read an archived EntitySet from an untrusted source.` |
| P1-181 | featuretools/entityset/deserialize.py:165 | #24 | Capability probe by string identifier against a stdlib signature — introspecting `tar.extractall`'s kwonly parameter names instead of a version check or `try`/`TypeError`. | `if "filter" in getfullargspec(tar.extractall).kwonlyargs:` |
| P1-182 | featuretools/entityset/serialize.py:54 | #11 | Fourth copy of the same "is it S3 / is it a URL / else local" dispatch, each re-deriving `transport_params` identically: here, `entityset/deserialize.py:152`–`:162`, `feature_base/features_serializer.py:89`–`:104`, `feature_base/features_deserializer.py:96`–`:112`. | `if _is_s3(path):`<br>`    with tempfile.TemporaryDirectory() as tmpdir:` |
| P1-183 | featuretools/entityset/serialize.py:75 | #1 | Opaque `**kwargs` on a public-ish boundary: `format` is fished out by string key and the *rest* is forwarded to `df.ww.to_disk`, so the accepted key set is undocumented and unvalidated at both `write_data_description` (`:44`) and here. | `def dump_data_description(entityset, path, **kwargs):`<br>`    format = kwargs.get("format")` |
| P1-184 | featuretools/entityset/serialize.py:83 | #12 | The file handle shadows the path variable it was opened from, inside the same statement — `file` means two different things three lines apart. | `file = os.path.join(path, "data_description.json")`<br>`with open(file, "w") as file:` |
| P1-185 | featuretools/entityset/serialize.py:13 | #9 | A module-level mutable list presented as a config constant with no production reader — the only reference outside this file is `tests/entityset_tests/test_serialization.py:83` (verified by grep). | `FORMATS = ["csv", "pickle", "parquet"]` |
| P1-186 | featuretools/feature_base/features_deserializer.py:71 | #24 | Stringly-typed class lookup: an eight-entry name → class table resolved at `:136` from a JSON string, failing with a bare `RuntimeError`. `type` also shadows the builtin at `:135`. | `cls = self.FEATURE_CLASSES.get(type)`<br>`if not cls:`<br>`    raise RuntimeError('Unrecognized feature type "%s"' % type)` |
| P1-187 | featuretools/feature_base/features_deserializer.py:59 | #28 | The docstring's "Option 3" example is not valid Python — `with open(filepath, 'r') as :` has no target and the next line references an undefined `f`. Copy-pasting the documented usage raises `SyntaxError`. | `with open(filepath, 'r') as :`<br>`    feature_str = f.read()` |
| P1-188 | featuretools/feature_base/features_deserializer.py:147 | #15 | The whole deserializer object is handed to `check_schema_version`, which touches exactly one thing — `cls.features_dict["schema_version"]` (`utils/schema_utils.py:28`). The same callee is handed a plain dict from `deserialize.py:27`. | `def _check_schema_version(self):`<br>`    check_schema_version(self, "features")` |
| P1-189 | featuretools/feature_base/features_deserializer.py:3 | #25 | Delegation edge whose caller and callee share no token stem: `description_to_entityset` is aliased to `deserialize_es` at import, so the call site at `:85` gives no way to grep back to the definition. | `from featuretools.entityset.deserialize import (`<br>`    description_to_entityset as deserialize_es,`<br>`)` |
| P1-190 | featuretools/feature_base/features_serializer.py:106 | #6 | Memoizing getter that also lazily creates `self._primitives_dict`, an attribute never declared in `__init__` (only `_features_dict = None` is, `:68`). An empty `feature_list` leaves `_features_dict` falsy, so the "cache" re-runs on every call. | `def _feature_definitions(self):`<br>`    if not self._features_dict:`<br>`        self._features_dict = {}` |
| P1-191 | featuretools/feature_base/features_serializer.py:62 | #13 | Both public entry points of the serializer pair are one-line forwarders — `save_features` here and `load_features` at `features_deserializer.py:67` — each carrying a ~50-line docstring over a single delegation. | `return FeaturesSerializer(features).save(location, profile_name=profile_name)` |
| P1-192 | featuretools/feature_base/feature_visualizer.py:139 | #6 | `get_feature_data` reads as a query but is an accumulator: it mutates four caller-owned collections (`dataframes`, `groupbys`, `edges`, `primitives`) and returns only a node id and depth. Callers rely entirely on the side effects (`:61`–`:74`). | `def get_feature_data(feat, dataframes, groupbys, edges, primitives, layer=0):` |
| P1-193 | featuretools/feature_base/feature_visualizer.py:66 | #14 | The clump `(dataframes, groupbys, edges, primitives, layer)` is threaded verbatim through all three `get_feature_data` call sites (`:66`, `:200`, `:228`) plus the definition — a graph-accumulator object in all but name. | `_, max_depth = get_feature_data(`<br>`    feature,`<br>`    dataframes,` |
| P1-194 | featuretools/feature_base/feature_visualizer.py:62 | #1 | `edges` is a bare 2-tuple of lists disambiguated only by index — `edges[1]` is solid edges, `edges[0]` dotted (`:120`–`:126`). None of the six mutation sites says which is which. | `edges = ([], [])` |
| P1-195 | featuretools/feature_base/feature_visualizer.py:140 | #18 | Literal numbered steps `1)`–`5)` inside `get_feature_data`, each a distinct phase (register, unwrap slice, primitive node, edges, recurse). | `# 1) add feature to dataframes tables:` |
| P1-196 | featuretools/feature_base/feature_visualizer.py:216 | #11 | Lines `:216`–`:223` repeat the groupby-node construction already written at `:189`–`:196` (`child_name` via `replace(":", "--")`, `groupby_node`, two `edges` appends, `base_node` rebind); `:180`–`:183` / `:186`–`:189` are a second clone pair inside the same `if`/`else`. | `child_node = "{}:{}".format(groupby.dataframe_name, groupby_name)`<br>`child_name = child_node.replace(":", "--")` |
| P1-197 | featuretools/feature_base/feature_descriptions.py:43 | #23 | `generate_description` is ~104 lines with eight labeled phases, five isinstance dispatches, two `hasattr` probes, recursion at three points, and a mid-function rebind of the `feature` parameter (`:102`–`:103`) that changes what every later branch sees. | `def generate_description(feature, feature_descriptions, primitive_templates):` |
| P1-198 | featuretools/feature_base/feature_descriptions.py:51 | #18 | Eight section comments mark the phases of that one function: "# Check if identity feature:", "# Handle direct features", "# Get input descriptions", "# Remove groupby description…", "# Generate primitive description", "# Generate groupby phrase…", "# Generate where phrase", "# Join all parts of template". | `# Check if identity feature:` |
| P1-199 | featuretools/feature_base/feature_descriptions.py:45 | #11 | The "look up by object, else by unique name, with `or` fallback" predicate appears three times in this module: here, `:89`–`:95` for `primitive_templates`, and `:177`–`:180` in `get_aggregation_groupby`. | `if feature in feature_descriptions or feature.unique_name() in feature_descriptions:` |
| P1-200 | featuretools/feature_base/api.py:1 | #26 | Re-export hub with no `__all__`: the public surface is declared by a `# flake8: noqa` suppression plus import side effects, so nothing states which of the twelve names are intentional exports. | `# flake8: noqa`<br>`from featuretools.feature_base.feature_base import (` |
| P1-201 | featuretools/exceptions.py:1 | #13 | Middle-man `__init__`: the body is exactly the base-class call it overrides, so deleting it changes nothing. | `class UnknownFeature(Exception):`<br>`    def __init__(self, *args, **kwargs):`<br>`        Exception.__init__(self, *args, **kwargs)` |
| P1-202 | featuretools/primitives/standard/transform/datetime/utils.py:22 | #24 | Attribute looked up by a runtime-built name — and `country` was rebound at `:10` by `convert_to_subdivision`, so for `country="UK"` this does `getattr(holidays, "GB")` and silently drops the subdivision the `country_holidays` call above was careful to compute. `self.holidays` and `self.federal_holidays` can then disagree. | `self.federal_holidays = getattr(holidays, country)(years=range(1950, 2075))` |
| P1-203 | featuretools/primitives/standard/transform/binary/add_numeric_scalar.py:23 | #11 | T2 clone group of 14 files: `__init__` (store `value`, format `description_template`) + `get_function` (closure applying one operator) + `generate_name` (`"%s <op> %s"`). Members: `binary/{add,subtract,multiply,divide,modulo}_numeric_scalar.py`, `binary/{equal,not_equal,greater_than,less_than,greater_than_equal_to,less_than_equal_to}_scalar.py`, `binary/divide_by_feature.py`, `binary/modulo_by_feature.py`, `binary/scalar_subtract_numeric_feature.py`. | `def __init__(self, value=0):`<br>`    self.value = value`<br>`    self.description_template = "the sum of {{}} and {}".format(self.value)` |
| P1-204 | featuretools/primitives/standard/transform/datetime/day.py:29 | #11 | T1/T2 clone group of 17 files, each body a closure returning one `vals.dt.<attr>` access: `datetime/{day,days_in_month,day_of_year,hour,minute,month,quarter,second,weekday,year}.py` and `datetime/{is_leap_year,is_month_end,is_month_start,is_quarter_end,is_quarter_start,is_year_end,is_year_start}.py`. A table of (name, return_type, dt-attribute) written as 17 modules. | `def get_function(self):`<br>`    def day(vals):`<br>`        return vals.dt.day` |
| P1-205 | featuretools/primitives/standard/transform/binary/greater_than.py:37 | #11 | T1 clone group of 4 files: the 11-line categorical-dtype guard is byte-identical (comment included) in `binary/greater_than.py:37`–`:46`, `greater_than_equal_to.py:38`–`:47`, `less_than.py:38`–`:47`, `less_than_equal_to.py:38`–`:47`; only the final comparison operator differs. | `val1_is_categorical = isinstance(val1.dtype, pd.CategoricalDtype)`<br>`val2_is_categorical = isinstance(val2.dtype, pd.CategoricalDtype)` |
| P1-206 | featuretools/primitives/standard/aggregation/max_consecutive_false.py:22 | #11 | T2 clone group of 5 files: `max_consecutive_{true,false,negatives,positives,zeros}.py`. Same five-step groupby-cumcount algorithm and the same comment text — the typo "unchnaged" appears in all five — differing only in the boolean projection. | `not_equal = x != x.shift()`<br>`not_equal_sum = not_equal.cumsum()`<br>`consecutive = x.groupby(not_equal_sum).cumcount() + 1` |
| P1-207 | featuretools/primitives/standard/transform/time_series/rolling_max.py:96 | #11 | T2 clone group of 6 files: identical `__init__(window_length, gap, min_periods)` plus a `get_function` that re-indexes into a Series and forwards to `apply_rolling_agg_to_series`. Members: `rolling_{max,min,mean,std,trend,count}.py`; only the reducer argument varies. | `x = pd.Series(numeric.values, index=datetime.values)`<br>`return apply_rolling_agg_to_series(`<br>`    x, lambda series: series.max(),` |
| P1-208 | featuretools/primitives/standard/transform/time_series/expanding/expanding_max.py:61 | #11 | T2 clone group of 5 files: `expanding_{max,mean,min,std,trend}.py`, bodies differing only in the terminal reducer. | `x = pd.Series(numeric.values, index=datetime)`<br>`x = _apply_gap_for_expanding_primitives(x, self.gap)`<br>`return x.expanding(min_periods=self.min_periods).max().values` |
| P1-209 | featuretools/primitives/standard/transform/exponential/exponential_weighted_average.py:48 | #11 | T2 clone group of 3 files differing only in `.mean()`/`.std()`/`.var()`. Proof of copy-paste rather than convergence: `exponential_weighted_variance.py:59` still names its inner function `exponential_weighted_average`. | `def exponential_weighted_average(x):`<br>`    return x.ewm(` |
| P1-210 | featuretools/primitives/standard/transform/email/email_address_to_domain.py:29 | #11 | `email/email_address_to_domain.py:29`–`:43` is reproduced verbatim — both comments included, typo "propogate" and all — at `email/is_free_email_domain.py:36`–`:49`, instead of the latter calling the former. | `# if all emails are NaN expand won't propogate NaNs and will fail on indexing`<br>`if emails_df["email"].isnull().all():` |
| P1-211 | featuretools/primitives/standard/transform/nth_week_of_month.py:35 | #11 | `transform/nth_week_of_month.py:35`–`:43` and `datetime/is_first_week_of_month.py:35`–`:43` are byte-identical for nine lines and then differ only in the final comparison. Verified: `adjusted_dom` occurs in exactly these two files repo-wide. | `df["first_day"] = df.date - pd.to_timedelta(df["date"].dt.day - 1, unit="d")`<br>`df["dom"] = df.date.dt.day` |
| P1-212 | featuretools/primitives/standard/aggregation/num_consecutive_greater_mean.py:42 | #11 | `num_consecutive_greater_mean.py:42`–`:78` and `num_consecutive_less_mean.py:42`–`:78` are 37-line twins with identical structure and comments, differing only in `<=`/`>=` and the identifier `below_`/`above_mean_indices`. | `below_mean_indices = x[x <= x_mean].index.to_series()` |
| P1-213 | featuretools/primitives/standard/transform/latlong/cityblock_distance.py:58 | #11 | The same six-line LatLong unpack appears verbatim in `latlong/cityblock_distance.py:58`–`:63`, `latlong/geomidpoint.py:27`–`:32` and `latlong/haversine.py:57`–`:62`; it belongs next to `_haversine_calculate` in `latlong/utils.py`. | `latlong_1 = np.array(latlong_1.tolist())`<br>`lat_1s = latlong_1[:, 0]`<br>`lon_1s = latlong_1[:, 1]` |
| P1-214 | featuretools/primitives/standard/transform/natural_language/number_of_hashtags.py:41 | #26 | Six modules (`number_of_hashtags`, `number_of_mentions`, `punctuation_count`, `title_word_count`, `upper_case_count`, `whitespace_count`) each consist of class attributes plus an `__init__` that hands one regex literal to `CountString`. This is a (name, pattern, return_type) table written as six files. | `def __init__(self):`<br>`    pattern = r"((^#)|\s#)(\w*([^\W\d])+\w*)(?![#\w])"`<br>`    super().__init__(string=pattern, is_regex=True, ignore_case=False)` |
| P1-215 | featuretools/primitives/standard/transform/datetime/part_of_day.py:41 | #26 | A 24-entry static lookup table assembled by eight `for` loops over literal hour lists instead of being written as one dict literal. Nothing here depends on runtime state. | `tdict = dict()`<br>`tdict[pd.NaT] = np.nan`<br>`for hour in [4, 5]:` |
| P1-216 | featuretools/primitives/standard/transform/natural_language/num_unique_separators.py:33 | #9 | Module-level mutable list `NATURAL_LANGUAGE_SEPARATORS` (`:7`) used as a default argument and then stored on `self`, so every default-constructed instance aliases one shared list. The `assert` on the next line guards `None` but not aliasing. | `def __init__(self, separators=NATURAL_LANGUAGE_SEPARATORS):`<br>`    assert separators is not None, "separators needs to be defined"` |
| P1-217 | featuretools/primitives/standard/aggregation/num_true.py:28 | #9 | Mutable lists as class attributes, shared by every instance and every subclass; same at `percent_true.py:29`–`:30` and `skew.py:24`. Sibling primitives express "no stacking" with scalars, so these three are the outliers. | `stack_on = []`<br>`stack_on_exclude = []` |
| P1-218 | featuretools/primitives/standard/transform/savgol_filter.py:99 | #3 | `except Exception: raise` is a no-op — deleting the whole `try`/`except` leaves semantics byte-identical. Second instance of this pattern in the repo (see P1-33). | `except Exception:`<br>`    raise` |
| P1-219 | featuretools/primitives/standard/aggregation/num_false_since_last_true.py:33 | #3 | The `if x.empty` guard is implied by the `true_indices.empty` check six lines later: an empty `x` yields an empty `dropna()`, hence an empty `x[x]`, hence the same `np.nan`. The mirror primitive `num_true_since_last_false.py:31` omits the guard and behaves identically. | `if x.empty:`<br>`    return np.nan` |
| P1-220 | featuretools/primitives/standard/aggregation/num_consecutive_greater_mean.py:53 | #12 | `x.mean()` is recomputed four lines after being bound to `x_mean` at `:49`; the test should be `np.isnan(x_mean)`. Same in `num_consecutive_less_mean.py:53`. | `if np.isnan(x.mean()):`<br>`    return np.nan` |
| P1-221 | featuretools/primitives/standard/transform/natural_language/number_of_common_words.py:60 | #12 | Manual counter loop where `sum(1 for ... if ...)` is the idiom — and two sibling modules in the same package already write it that way (`num_words.py:41`, `upper_case_word_count.py:40`). | `num_common_words = 0`<br>`for w in words:`<br>`    if (w.lower().strip(punctuation) in self.word_set):` |
| P1-222 | featuretools/primitives/standard/transform/natural_language/punctuation_count.py:38 | #12 | Builds a 32-branch regex alternation of individually escaped characters where a character class does the same job: `"[" + re.escape(string.punctuation) + "]"`. | `pattern = "(%s)" % "\|".join([re.escape(x) for x in string.punctuation])` |
| P1-223 | featuretools/primitives/standard/transform/time_series/utils.py:164 | #1 | `window_length` and `gap` are `Union[int, str]`, a two-shape union every consumer must re-discriminate. That union is what forces `_check_window_length` (`:121`), `_check_gap` (`:138`), the `isinstance(gap, str)` forks at `:48` and `:225`, and the whole `apply_roll_with_offset_gap` path. The same untyped pair recurs in nine primitive `__init__`s. | `window_length: Union[int, str],`<br>`gap: Union[int, str] = 0,` |
| P1-224 | featuretools/primitives/standard/transform/absolute_diff.py:61 | #8 | The same four-element `fill_method` vocabulary is validated by an inline list literal at three sites — here, `percent_change.py:91`, `numeric/same_as_previous.py:55` — three copies of the list and three copies of the error message. | `if method not in ["backfill", "bfill", "pad", "ffill"]:`<br>`    raise ValueError("Invalid method")` |
| P1-225 | featuretools/primitives/standard/aggregation/time_since_first.py:48 | #8 | The same bare `unit: str` time-unit parameter, `.lower()`ed and never validated, at five sites: `time_since_first.py:48`, `time_since_last.py:48`, `avg_time_between.py:43`, `datetime/time_since.py:46`, `datetime/time_since_previous.py:38`. A bad unit surfaces only deep inside `convert_time_units` — while `latlong/haversine.py:43` validates its unit string, inconsistently. | `def __init__(self, unit="seconds"):`<br>`    self.unit = unit.lower()` |
| P1-226 | featuretools/primitives/standard/aggregation/max_count.py:37 | #21 | The `agg → isnan → np.nan → eq(agg).sum()` invariant is copied at `max_count.py:37`, `min_count.py:38`, `median_count.py:40`, with the `mean → isnan → np.nan` prefix again at `count_above_mean.py:37` and `count_below_mean.py:37` — five sites of one rule. | `xmax = x.max(skipna=self.skipna)`<br>`if np.isnan(xmax):`<br>`    return np.nan` |
| P1-227 | featuretools/primitives/standard/transform/natural_language/count_string.py:84 | #21 | The "apply `ignore_case`" invariant lives twice in one class: inlined in `__init__` for the regex branch, and again in `process_text` (`:96`) for the input text. The `if not is_regex` branch at `:80` routes through `process_text`; the `else` branch re-implements half of it. | `if ignore_case:`<br>`    self.pattern = self.pattern.lower()` |
| P1-228 | featuretools/primitives/standard/transform/savgol_filter.py:82 | #23 | Worst constructor in the primitives tree: a three-way arm over `(window_length, polyorder)` nullity, one arm containing a `try` wrapping a nested `if`-raise plus a library call, another being a two-clause `or` of two-clause `and`s — all to validate two parameters. | `elif (window_length is None and polyorder is not None) or (`<br>`    window_length is not None and polyorder is None`<br>`):` |
| P1-229 | featuretools/primitives/standard/transform/url/url_to_tld.py:31 | #6 | `get_function` — a pure accessor everywhere else in this subtree — writes instance state as its first statement, then defines a `url_to_domain` helper (`:33`–`:35`) that is a verbatim copy of `URLToDomain.get_function`'s body, regex literal included. | `self.tlds_pattern = r"(?:\.({}))".format("\|".join(COMMON_TLDS))` |
| P1-230 | featuretools/primitives/standard/transform/email/is_free_email_domain.py:30 | #29 | Hidden cost on an accessor: `get_function` performs disk I/O (`get_filepath` + `read_csv` of the free-domain list) with no docstring saying so. Every sibling `get_function` in the subtree is a pure closure factory, so callers have no reason to expect a file read. | `file_path = self.get_filepath(self.filename)`<br>`free_domains = pd.read_csv(file_path, header=None, names=["domain"])` |
| P1-231 | featuretools/primitives/standard/transform/is_null.py:22 | #13 | A closure whose body is a single forwarding call to the method it wraps. Same shape at `aggregation/first.py:22`, `last.py:22`, `is_unique.py:34`, `is_monotonically_increasing.py:28`, `is_monotonically_decreasing.py:28`, `cumulative/cum_sum.py:29`, `cum_max.py:29`, `cum_min.py:29` — while `aggregation/median.py:27` already shows the closure-free form (`return pd.Series.median`). | `def isnull(array):`<br>`    return array.isnull()` |
| P1-232 | featuretools/primitives/standard/aggregation/max_consecutive_false.py:24 | #18 | Six labeled step comments in a 17-line function, narrating each pandas call, replicated verbatim across all five `max_consecutive_*` files. | `# invert the input array to work properly with the computation`<br>`# find the locations where the value changes from the previous value` |
| P1-233 | featuretools/primitives/standard/transform/full_name_to_first_name.py:37 | #18 | Four labeled phase comments inside one 18-line function (`:37`, `:43`, `:45`, `:48`), each naming a step of a linear pipeline; `full_name_to_last_name.py:33`/`:36`/`:42` has three of the same. | `# remove any entries with just a title and a name` |
| P1-234 | featuretools/primitives/standard/transform/full_name_to_title.py:29 | #11 | The same non-trivial title regex is a literal in three files (`full_name_to_title.py:29`, `full_name_to_first_name.py:35`, `full_name_to_last_name.py:31`). `FullNameToTitle`'s entire body *is* that regex, so the other two re-implement rather than compose. | `pattern = r"([A-Z][a-z]+)\. "` |
| P1-235 | featuretools/primitives/standard/transform/natural_language/constants.py:1 | #29 | A 1009-line data module with no module docstring; its only provenance is a URL comment on the last line. Nothing at the top tells a reader this file is a 1000-word frozenset. | `from string import punctuation` |
| P1-236 | featuretools/primitives/standard/__init__.py:4 | #26 | The primitives package entry point is two star imports with `# flake8: noqa` at `:1` to silence the resulting lint; it transitively pulls ~200 primitive modules plus scipy, holidays and pandas, and declares neither the exported surface nor the cost. | `from featuretools.primitives.standard.aggregation import *`<br>`from featuretools.primitives.standard.transform import *` |
| P1-237 | featuretools/primitives/standard/transform/datetime/utils.py:24 | #19 | `to_df` rebuilds the full holidays DataFrame (sort + DataFrame construction + dtype cast over 125 years of dates) on every invocation, from three call sites: `date_to_holiday.py:51`, `distance_to_holiday.py:67`, `is_federal_holiday.py:40`. | `        holidays_df = pd.DataFrame(`<br>`            sorted(self.federal_holidays.items()),` |
| P1-238 | featuretools/primitives/standard/transform/datetime/season.py:38 | #19 | The five-element `seasons` list is rebuilt on every call and `pd.isna(dt)` is re-tested inside the per-row loop. | `        seasons = [` |
| P1-239 | featuretools/primitives/standard/aggregation/avg_time_between.py:60 | none | The result is multiplied by `1e-9` unconditionally, but the conversion to `int64` nanoseconds happens only inside the `isinstance(..., (pd.Timestamp, datetime))` branch — non-datetime numeric input is silently scaled by a billion. | `        return avg * 1e-9` |
| P1-240 | featuretools/primitives/standard/transform/savgol_filter.py:123 | #6 | Writes into the caller's array in place while nominally computing a filtered result. | `    x[mask] = np.interp(` |
| P1-241 | featuretools/primitives/standard/transform/natural_language/median_word_length.py:42 | none | Returns `None` implicitly when the input is not a `list`, while every sibling primitive in the package returns `pd.NA`/`np.nan` for the same condition — a silent type inconsistency inside one output column. | `        if isinstance(words, list):` |

**Coverage note.** Every prod file was read: `entityset/` (7), `synthesis/` (7), `feature_base/` (9),
`computational_backends/` (6), `feature_discovery/` (7), `utils/` (14), `selection/` (3), `demo/` (6),
`primitives/` (229, incl. all 15 subdirectories of `primitives/standard/`), plus `__init__.py`,
`config_init.py`, `exceptions.py`. `featuretools/tests/` was excluded as non-prod.

## Phase 2 — audit finding verdicts

1520 findings, judged as rule-classes (aggressive grouping per coordinator).
Each row = one rule-class: a representative site or two, the class size, and one
verdict. Rows split only where the verdict differs within a rule (#15, #2 noted
inline). Every finding line is accounted for by some row's count.
Totals: **real 798 / fp 722.**

| finding (path:line) | rule | tier | verdict | why |
|---------------------|------|------|---------|-----|
| primitives/standard/transform/binary/add_numeric_scalar.py:23 (+377 more) | #11 | indexed | real | Genuine T2/T3 copy-paste families in prod (binary scalars x14, ewm x3, rolling x6) and tests (`test_with_featuretools` x42, `test_order` x4). The clone-ratchet ideal applies. |
| primitives/standard/transform/natural_language/number_of_words_in_quotes.py:59 (527 total) | #22 | heuristic | fp | Fires on `get_function`/`generate_name`/`copy`/`column_schema` (framework polymorphic overrides mandated by PrimitiveBase/FeatureBase — cannot be free functions) and on pytest `test_*` methods (test-class grouping). Category error, exactly the "SRP-linter" caution. |
| entityset/entityset.py:1 (156 total, 84 prod/72 tests) | #29 | heuristic | real | Heavy modules (≥~250 lines) lacking a top docstring — entityset.py (1747), dfs (1328), constants.py (1009). REPORT-tier informational; the test-file subset is marginal but the modules are genuinely heavy. |
| entityset/entityset.py:586 add_dataframe (59 total) | #27 | indexed | real | Fan-in-weighted symbols in large modules (`add_dataframe` fan-in 132 in a 1747-line file) — precisely the symbol-purchase-price ideal. Informational, REPORT-only. |
| entityset/entityset.py:28 `self.reset_data_description()` x7 (58 total) | #21 | heuristic | fp | Fires on calls to an already-encapsulated method (`reset_data_description` from 7 mutators is correct design) and on trivial accessors (`self.base_features[0]`); these are not ad-hoc invariants awaiting encapsulation. In tests it flags `self.compare(...)` — already the shared helper. |
| synthesis/deep_feature_synthesis.py:796 _build_agg_features (prod, 30) | #15 | indexed | real | Genuine over-broad object passing: a whole DataFrame passed where only `.ww`/`.ww.name` is used; take the narrower value. |
| tests/.../test_rolling_primitive.py (tests, 47) | #15 | heuristic/indexed | fp | Test helpers and fixtures legitimately carry more attributes than any single assertion reads; narrowing them is noise. |
| feature_base/feature_base.py:362 __mul__ (33 total) | #25 | indexed | fp | Dominated by operator dunders delegating to a shared handler (`__mul__`→`_handle_binary_comparison`, `__setitem__`→`add_dataframe`) — no shared token stem by the nature of dunders, and the delegation is correct, not a misleading rename. |
| synthesis/get_valid_primitives.py:10 get_valid_primitives (28 total) | #6 | indexed | real | `get_*`/`read_*`/`load_*`/`get_feature_data` named as accessors but carrying effects (global-cache mutation, I/O, arg mutation) — the honesty ideal. Matches many P1 sites. |
| primitives/standard/transform/time_series/rolling_count.py:92 `(gap,min_periods,window_length)` x16 (24 total) | #14 | indexed | real | The same parameter group recurs across 16+ signatures and wants a type; genuine data clump. |
| config_init.py:68 Config.set (24 total) | #13 | indexed | real | Genuine one-line forwarders (`Config.set`→`_data.update`, `cache.clear_all`, `FeatureBase.get_function`, cumulative-primitive closures). |
| primitives/__init__.py:61 _load_primitives (18 total) | #24 | heuristic | real | `getattr`/`globals()`/string-built names — registry loaders, env-driven log level, test parametrization. Genuine dynamic identifier construction. |
| synthesis/get_valid_primitives.py:15 **dfs_kwargs (16 total) | #1 | heuristic | real | Opaque `**kwargs` and bare containers on public boundaries; genuine weak-boundary-types. |
| __init__.py:4 star import (29 total) | #26 | heuristic | real | Public API assembled by star imports / code-built registries with no `__all__`; genuine declaration-literalness (a couple of docs/ targets are repo-level scripts but still valid instances). |
| primitives/options_utils.py:205 column_filter (14 total) | #30 | heuristic | real | Genuine Demeter chains (3-hop attribute reaches); REPORT-only health metric, low value but correct. |
| feature_discovery/LiteFeature.py:56 (19 total: 15 proved + 4 heuristic) | #2 | proved | fp | All are boundary-defensive validation (`_build_ignore_columns` documents "Throws a TypeError"; `_check_inputs` raises ValueError) or shape discrimination — the isinstance IS the runtime enforcement of an unenforced annotation, not dead code. Several proofs also rode wrong oracle types (`Never`/`Unknown`) from the 459 unresolved imports. Matches the CLAUDE.md `reportUnnecessary*` unsoundness trap. |
| synthesis/deep_feature_synthesis.py:629 _build_transform_features (24 total) | #5 | indexed | fp | Proof-lift proposals on internal `bool` flags with "all 2 call sites" evidence — the documented over-narrowing FP engine (PA §8), and the call-graph is demonstrably incomplete here (459 unresolved imports), so "all callers" is an undercount. |
| synthesis/deep_feature_synthesis.py:882 base_names (9 total, 1 prod/8 tests) | #19 | heuristic | real | Genuine O(n·m): membership test on a list rebuilt inside a loop. Test instances lower-value but real. |
| selection/selection.py:6 (8 total) | #28 | indexed | fp | The named symbols DO resolve via `featuretools/__init__.py` re-exports (`featuretools.FeatureBase`, `featuretools.graph_feature`), and `data_description.json` is a runtime output artifact, not a repo path. The IDX engine missed the dynamic re-export chain (same unresolved-namespace degradation). |
| computational_backends/calculate_feature_matrix.py:156 (5 total) | #4 | indexed | fp | Closed-world unsound here: only 1 caller discovered (unresolved imports), and the checks are public-API defensive validation, not interprocedurally-dead. |
| feature_discovery/LiteFeature.py:203 hash (4 total, 1 prod/3 tests) | #9 | heuristic | real | Mutable default arguments (`base_features=[]` later sorted in place; fake-dataframe helpers). Genuine shared-mutable-state. |
| primitives/standard/aggregation/num_consecutive_greater_mean.py:49 (3 total) | #17 | heuristic | real | Genuine live-variable neck in long functions; REPORT-only split hint. |
| tests/feature_discovery/test_type_defs.py:89 (3 total) | #20 | heuristic | real | Same non-trivial lambda body repeated ≥2× in a module; name it once. |
| feature_base/feature_visualizer.py:140 (2 total) | #18 | heuristic | real | ≥2 labeled phases narrated inside one function (`get_feature_data` 5 phases; `_calculate_features_for_dataframe` Step 1–5). |
| primitives/standard/transform/absolute_diff.py:61 (1) | #8 | indexed | real | `fill_method` vocabulary validated by an inline list in 3 functions — encode as a type. |
| tests/utils_tests/test_config.py:28 (1) | #16 | heuristic | fp | A test's setup-then-assert shape, not a pure-compute-then-write mutation tail to split. |

## Phase 3 — reconciliation

My 241 phase-1 sites vs the audit. Grouped. Class of a miss is judged against
the rule I mapped the site to (matched same-rule within the file; ambient rules
#27/#29/#22/#30, which fire on nearly every large symbol/method, are not counted
as coverage of a specific defect).
Totals: **covered 42 / detector-miss 161 / threshold-miss 16 / inventory-gap 22.**

| P1 id | rule | class | note |
|-------|------|-------|------|
| P1-74,75,76,77,137,236 | #26 | covered | Star-import / code-assembled-API sites all caught by the #26 group (29 findings). |
| P1-78,90,100,167,235 + P1-30,114,183,194,28 | #29/#1/#14 | covered | Heavy-module top-loading and opaque-`**kwargs`/clump sites caught by #29/#1/#14. |
| P1-79,87,88,103,104,107,202 | #24 | covered | getattr/registry/env-string dynamic-id sites caught by the #24 group. |
| P1-18,192,128,179,84 | #6 | covered | Memoizing/effectful accessors caught by the #6 honesty group. |
| P1-38,195,155,159,173,231,203,207,209,224,229,230,132,61,70,110 | mixed | covered | Section-comments (#18), mutable-default (#9), classmethod-not-using-cls (#22-adjacent→#6), clone families binary/rolling/ewm (#11), fill_method (#8), effectful get_function (#6), immutable-eq (#1), forwarding setter Config.set (#13 caught the #1-mapped P1-110). |
| P1-3,81,191,201 | #13 | detector-miss | Shallow-wrapper detector caught 24 others but missed the `to_pickle/parquet/csv` trio, `get_agg/trans_primitives`, `save_features`, and the no-op `UnknownFeature.__init__`. |
| P1-9,10,19,29,57,60,85,86,98,99,102,115,118,142,149,163,171,172,177,184,220,221,222 | #12 | detector-miss | **#12 (idiom catalog) produced zero findings repo-wide** — every hand-rolled-stdlib site I flagged is a miss. |
| P1-14,15,92,105,124,165,197,228 | #23 | detector-miss | **#23 (cognitive complexity) fired zero** — none of the worst functions (`normalize_dataframe`, `_check_timedelta`, `savgol` ctor, `generate_description`) surfaced. |
| P1-45,117,126,130,180 | #7 | detector-miss | **#7 (comment-borne protocol) fired zero** — the "caller must ensure"/placeholder-docstring/security-note contracts were not detected. |
| P1-150,162,218,219 | #3 | detector-miss | **#3 (contract-implied guard) fired zero** — the redundant `if not self.sorted` and `except: raise` guards not detected. |
| P1-4,22,31,46,55,56,106,119,133,134,140,145,154,182,196,199 | #11 | detector-miss | Cross-function / cross-module clones the AST-clone detector didn't group: entityset `get_forward/backward`, `FeatureOutputSlice` re-impl, progress-callback closures, dfs recursion loops, transport-dispatch x4, flight/gen_utils `convert` dup. |
| P1-204,205,206,208,210,211,212,213,234 | #11 | threshold-miss | Primitive clone families the detector caught siblings of (binary/rolling/ewm) but whose normalized bodies fell below its similarity bar: datetime accessors x17, categorical-guard x4, max_consecutive x5, expanding x5, email dup, nth_week, num_consecutive twins, latlong unpack, full-name regex. |
| P1-12,13,35,49,50,82,95,121,127,131,136,139,144,148,156,169,190,240 | #6 | detector-miss | Honesty detector caught the accessor-name-with-effects pattern but missed `check_*`/`_validate_*`/`get_node`/`check_graphviz` that mutate args, do subprocess I/O, or mutate state, the `re.match`-without-None, and in-place-write "compute" functions. |
| P1-1,2,27,65,108,111,112,125,129,138,143,151,160,161,185,216,217 | #9 | detector-miss | Shared-mutable detector caught 1 prod site (`LiteFeature.hash`) and missed the rest: `pd.options`/RNG/logging global mutation, `_ES_REF`/`feature_cache` module registries, module-level lists, import-time scans, `lru_cache` on live instances. |
| P1-7,8,36,39,44,113,135,176,237,238 | #19 | detector-miss | Linear-op-in-loop detector keys on `list.insert/remove/in`; missed `pop(0)` queues, loop-invariant rebuilds, re-resolved calls, and per-call table/DataFrame rebuilds. |
| P1-16,43,51,83,101,170,223 | #1 | detector-miss | Weak-types detector caught `**kwargs`/bare containers but missed tuple-in-dict positional contracts, dict-or-DataFrame returns, a wrong `CutoffTimeType`, and `Union[int,str]` two-shape unions. |
| P1-23,41,47,72,73,116,122,181,186 | #24 | detector-miss | Dynamic-id detector caught `getattr(x, str)` but missed `type(x).__name__ ==`, `str(id(func))` as a name, `str(class)` as a key, `sys.modules[...]=` assignment, `__dict__[...]`, and `getfullargspec` name-probing. |
| P1-17,89,91,152,157,166,200,214,215 | #26 | detector-miss | Declaration-literalness caught star imports but missed code-assembled tables (option schema, `part_of_day` dict, six regex-as-table modules), `field()` on a non-dataclass, tuple-length arg decode, empty `__init__`, and `api.py` with no `__all__`. |
| P1-21,54,153,158,174,226,227 | #21 | detector-miss | Distributed-invariant detector fired 58× on trivial expression repetition but missed the real class-scoped invariants: groupby-last-element, `allowed_paths` guard x4, immutable-setter x10, parent/child mirror x6, agg→isnan x5, ignore_case-twice. |
| P1-24,66,67,96,193 | #14 | detector-miss | Data-clump detector missed the 5-name `generate_name` clump, the stacking-args clump, the 31-param `dfs` fan-out, and the visualizer accumulator group. |
| P1-80,94,97,123,175,225 | #8 | detector-miss | Primitive-obsession detector caught 1 (`fill_method`) and missed the primitive-key normalization, groupby-key mapping, path-kind predicates, relationship 4-string identity, and `unit:str`-at-5-sites. |
| P1-11,53,93,141,198,232,233 | #18 | threshold-miss | Section-comment detector fired twice but missed equally-clear phase runs: `# Case 1..6`, `Step 1..7` docstring-expressions, options phases, `_clean_data`, feature-descriptions, max_consecutive steps, full-name phases. |
| P1-40,48,168 | #2 | detector-miss | The always-false `isinstance(index, CategoricalDtype)`, `assert "<string>"`, and reversed-`in` — real dead/wrong checks the oracle missed (CategoricalDtype unresolved; assert-string and operand-order out of its model). |
| P1-120,164,189 | #25 | detector-miss | Name-consistency detector fired on dunder delegations but missed the real ones: `cls`-that-isn't-a-class, `rfunc` x3, `deserialize_es` alias hiding the callee. |
| P1-146,147,187 | #28 | detector-miss | Doc-path detector flagged resolvable symbols (fp) yet missed the genuinely-broken ones: docstrings importing nonexistent `lite_dfs`/`get_matching_columns` and a `SyntaxError` example. |
| P1-178 | #16 | detector-miss | Mutation-tail (`kwargs["filename"]` leak across loop iterations) fired only on a test; missed this real deserialize bug. |
| P1-188 | #15 | detector-miss | Wallet-param fired 77× but missed the whole-deserializer-passed-for-one-field case. |
| P1-5,6,20,25,26,32,33,34,37,42,52,58,59,62,63,64,68,69,71,109,239,241 | none | inventory-gap | No rule covers these: tautologies (`col != a or col != b`), dead code (`dfs_to_update`, empty `for: continue`), reachable `UnboundLocalError`, no-op `except: raise`, shadowed names, truthiness-guarded cache read, unseparated cache key, dead `parent_dataframe_name` param, reversed-order docstring, `import tests` into the shipped package, silent unit/scale/`None`-return bugs. |
