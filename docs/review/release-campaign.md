# Public release campaign

The one status doc. Every decision lands in `docs/review/decisions.tsv` under
a `release/*` phase. The gate ladder runs once, fresh, at the close.

## Frame

Close the gaps between this tree and a public release, make the repository
public, cut v0.2.0. Three forks were put to the maintainer and answered:

| Fork | Call |
| --- | --- |
| Two Python corpus repos are private, so no Python number reproduces | Swap both for public trees and re-measure |
| `sightline` is taken on crates.io (`bgshacklett`, 2026-07-19) | Ship under a suffixed crate name. The ty fork's git dependency blocks a publish, so this campaign picks and reserves the name and writes the row that unblocks it |
| Where the campaign stops | Flip the repository public and cut v0.2.0 |

## Waves

A boundary is a constraint, not a checkpoint. Wave 2 measures walls, so wave 1
must be merged and every other worker gone.

| Wave | Units | Why the boundary sits here |
| --- | --- | --- |
| 1 | A, B, C, E, F, G | Disjoint file ownership, so all six run at once |
| 2 | The corpus sweep and the four generated tables | A wall measured beside three cargo builds is not a wall |
| 3 | Release plumbing, version, changelog, close ladder, flip, tag | Needs every unit landed |

## Units

Each returns `PASS` / `ISSUES` / `BLOCKED` with evidence. A worker that writes
gets its own worktree, except A, which needs the live root's sibling layout.

| Unit | Owns | Done when |
| --- | --- | --- |
| A corpus-select | `crates/xtask/corpus.toml`, `corpus/*.toml`, the two new clones | Two public Python trees replace `lol-predictor` and `ROFL-File-Information` at matched role and size, every row carries a `url`, and an audit of each new tree resolves its python-env |
| B docs | `README.md`, `SECURITY.md`, `CONTRIBUTING.md`, `.github/ISSUE_TEMPLATE/`, the environment section of `docs/reference.md` | An audit's code execution is stated where a user meets it, a copy-paste CI job runs, the install channel names the crate, and the cache directory and `SIGHTLINE_CARGO_TARGET` are documented with how to clear them |
| C output-surface | `crates/sightline/src/{cli,main}.rs`, `verbs/explain.rs`, `crates/core/src/render/sarif.rs`, the oracle event printers, their rows in `docs/reference.md` | `sightline explain` with no argument prints the roster, a switch silences the pass lines, and the SARIF driver and rules carry the metadata GitHub renders |
| E third-party | `crates/xtask/src/`, `THIRD-PARTY.md` | An xtask stage builds the attribution file from `cargo metadata` and the registry license files, `check` fails when it is stale, and the file covers every crate linked into the binary |
| F ext-paths | `corpus-ext/` | No absolute path of one machine is left in a tracked file, and a script did the edit |
| G edge-tie | Nothing (read-only) | A verdict, with `file:line`, on whether a platform edge-tie flip can move a finding's `site.symbol` and so unseat a ratchet baseline across platforms |

## Rulings this campaign makes

- Attribution is generated by an xtask stage, not `cargo about`. It stays in
  the gate and adds no tool a contributor must install.
- `rust-version` names the pinned toolchain, the only version this workspace
  is ever built at.
- The campaign log is `docs/review/decisions.tsv`, phase `release/*`. No second
  ledger.
- A code worker runs the verification its brief names, never `check --slow`.
  The ladder is run once, at the close.

## Gate ladder, run once at the close

```
cargo xtask surface
cargo xtask check
cargo xtask check --slow
sightline audit . --json      # the own audit, clean of new findings
gh release view v0.2.0        # after the tag: three archives and the smoke job
```

## Status

| Wave | State |
| --- | --- |
| 1 | closed. A, B, C, E, F merged, G answered |
| 2 | closed. J, H, K, L, N merged; polarity is the blocking axis, cc-receipt cut, the splice batching reads ids, and the sweep's numbers are committed |
| 3 | open: dist installers, version to 0.2.0, CHANGELOG, the crates.io reservation, the close ladder, the flip, the tag |

## What wave 2 settled about the gate

The two rules that blocked unconditionally were both unjudged. Both are now
priced, and neither blocks.

| Rule | Judged | Where it landed |
| --- | --- | --- |
| #44 tautological-assertion | 48/82, then 7/33 on a fresh pool after narrowing | REPORT |
| #31 boundary-contracts | 8/110, the lowest row in the table | cut, id retired |

The narrowing of #44 read 48/52 on the pool that shaped it. The fresh round
priced it at 7/33, and that gap is the reason a round draws a new seed.

`sightline audit` also stopped crashing: one module-id defect answered every
tree three units found panicking, nine of them, and no corpus count moved.

Wave 2 still owes: the corpus sweep and the four generated tables,
`corpus/profile-sqlglot.json` (the pole swap left `profile` with no
reference), and the splice-order fix proved by a corpus diff.
