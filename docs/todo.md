# To-do

Next moves, imperative, one per row, in value order. A row leaves when its bar
is met. A cut rule's burial and its evidence live in `docs/review/decisions.tsv`.

## Next

| Do | When / bar | Evidence |
| --- | --- | --- |
| **Match an exclude glob the same way on every platform.** `core::walk::excluded` runs a config pattern through `pytext::fnmatch`, which normcases pattern and path on Windows and neither elsewhere. A pattern whose case differs from the paths it targets excludes different files on the two platforms, which moves finding counts under a shared baseline key. `py_provers::hotness` already reaches for the case-sensitive `fnmatchcase` for this reason | a fixture whose pattern differs in case from its target audits the same file set on both platforms; corpus fire rates unmoved | `crates/core/src/pytext/glob.rs:130`, `crates/core/src/walk.rs:183`, `crates/py-provers/src/hotness.rs:238` |
| **Write the two deferred rulers as xtask subcommands.** Phase 9 left the recall round and the gauntlet round sourcing (the candidate pool and the selection) to the retired tool's scripts; that tree is gone, so each capability is gone until it is written here. A recall round driven from this repo or a fourth gauntlet round is the trigger. The third deferral, the cut receipt, closed the other way: the Python-only `cc-receipt` was cut, and a cut's price is `surface` plus the diff | the subcommand lands with the round that needs it, and the round it serves is priced on both sides | `docs/review/decisions.tsv`, the phase-9 deferral row and the `release/cc-receipt` row |
| **Pin the rs toolchain per audited tree.** `oracle/cargo.rs:pinned` embeds this repo's `sightline.toml`, so the workspace's own audits print two off-pin notes (its toolchain is 1.97.1, the corpus environment 1.98.0). Read the audited tree's `[tool.sightline.rust-toolchain]` first, embedded table as the fallback | own-audit headers clean of off-pin notes; corpus fire rates and walls unmoved | `../sightline-history/phase9/receipts/xtask-bench.md`, open rows |
| **Re-home the comments that name the retired tree.** Three classes, around eighty lines: module headers reading `Port of <name>.py (codemap 3.3)`, layer-shape pointers reading `dump_layers.py:<fn>`, and test comments naming REF as the source of a pinned expected value. Each records where something came from, and the tree it points at is gone. Each should say what its own code does, and a pinned value should name the probe's number, not its author | `git grep -nE "codemap\|dump_layers\|Port of .*\.py\|REF" crates` returns only `--since REF` | this file; the retirement receipt's survivor list |
| **Upstream the search-path canonicalization into the ty fork.** ty canonicalizes `extra-paths` but not `src_roots`, and its dedup compares strings, so one directory can reach the file interner under two spellings; the port canonicalizes the root at `Oracle::new` to meet it. The fork fix removes the workaround | the fork dedups by `is_same_file`; `Oracle::new`'s canonicalize call deleted; CI green on the three platforms | `../sightline-history/phase9/receipts/release.md`, "Not met" section |
